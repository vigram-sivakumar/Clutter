/**
 * Chrome Block Components
 *
 * Extracted chrome components for complex blocks.
 * Simple blocks (paragraph, heading, quote, code, divider) remain inline in BlockChromeWrapper.
 * Complex blocks (checklist, toggle, field) get their own components.
 */

export { FieldChrome } from './FieldChrome';
