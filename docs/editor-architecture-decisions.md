# Editor Architecture Decisions

## Purpose and scope

This is **not** an ADR and not a research document. It is a concise lookup layer over the Markdown editor's architecture Q&A — the semantic-inline construct thread specifically (parser design, grammar, resolution semantics, the shared interaction contract). Its only job is to let a reader, including a cold-start Claude session, answer *"is X already decided, and under what conditions?"* in under a minute, without reading the full research archive.

The full reasoning, worked examples, counter-examples, and scenario walkthroughs behind every entry below live in [`docs/editor-research/`](./editor-research/README.md) — **that is the evidence layer; this is not.** Entries here are deliberately terse. When an entry's "why" isn't enough to settle a disagreement, follow its link rather than re-deriving the reasoning from scratch.

**Relationship to ADRs**: none of this is an ADR yet, by design (per the working decision on how to document this phase of work). Once this architecture stabilizes, the small number of genuinely foundational, durable commitments here (candidates: CodeMirror 6 as the engine, Markdown as sole canonical source with no second document model, one shared interaction mechanism for semantic inline constructs) should graduate into real ADRs in `docs/adr/`, matching the scale of `ADR-017`/`ADR-020`. The much larger volume of fine-grained grammar/precedence/interaction decisions below is not expected to ever need that treatment individually.

**Earlier, separate phase of work not covered here**: the Markdown editor behavioral specification (`clutter-editor-interaction-spec-v0.2.md`), the three architecture-blocking-decision resolutions, the architecture proposal, and the bounded implementation plan are prior, self-contained deliverables with their own internal decision/open-item summaries. This log does not re-capture them — it picks up from the semantic-inline construct Q&A that followed.

## Status key

- **Locked** — settled; treat as a fixed constraint for any further work in this area.
- **Recommended, not locked** — a considered, evidenced recommendation, but not yet a product/architecture commitment. Do not build against it as if it were Locked without checking whether it's since been promoted.
- **Superseded** — an earlier position that was explicitly reversed. Kept visible on purpose so nobody re-discovers the same dead end.
- **Open** — a genuine, named unresolved question. Not silently decided by omission.

---

## Verified against installed APIs (2026-08-18)

No CodeMirror/Lezer package is installed in this repo yet (`apps/app/package.json` has no `@codemirror/*`/`@lezer/*` dependency as of this check) — everything below was verified against the packages' own published source/type declarations on the npm registry, not against code running in this repo. Re-confirm after the actual `npm install` pins versions, as a cheap sanity check, not because drift is expected.

Validated, mutually compatible version set: `@codemirror/view@6.43.9`, `@codemirror/state@6.7.1`, `@codemirror/lang-markdown@6.5.2`, `@codemirror/commands@6.11.0`, `@codemirror/autocomplete@6.20.3`, `@lezer/markdown@1.7.2`, `@lezer/common@1.2.3`. `@codemirror/lang-markdown@6.5.2`'s own declared peer ranges (`@lezer/markdown: ^1.0.0`, `@lezer/common: ^1.2.1`, `@codemirror/state`/`@codemirror/view: ^6.0.0`, `@codemirror/autocomplete: ^6.7.1`) are all satisfied by this set.

- **`MarkdownConfig`, `InlineParser`, `BlockParser` shapes confirmed exactly as assumed** — `InlineParser.parse(cx, next, pos): number`, `before`/`after` as plain string fields naming another parser. No corrections to the parser-architecture decision.
- **GFM's `Autolink` inline parser is registered under the literal name `"Autolink"`, with no `before`/`after` of its own** — confirmed by reading `@lezer/markdown`'s own source. This resolves the previously-open item: the `@`-family parser should be registered `after: "Autolink"` directly; no ambiguity remains.
- **GFM's `Strikethrough` parser is registered `after: "Emphasis"`** — confirmed real (not just inferred precedent) evidence that delimiter-based custom inline constructs are already proven to compose correctly with `Emphasis` in the shipped grammar.
- **No dedicated "node group" feature exists in `@lezer/markdown`** — confirmed the package "delegates to Lezer's core infrastructure" rather than offering its own tagging mechanism. **Recommendation, not a spec requirement**: the shared interaction layer's "is this any semantic-inline node" query should be a plain check of `node.type.name` against a small, fixed `Set` of Clutter's own node type names (`WikiLink`, `Tag`, `Mention`, `PropertyToken`, …) — simpler than wiring a custom `NodeProp`, and sufficient given the fixed, known set of kinds. A custom `NodeProp` (attached via `defineNodes`'s per-node `props`) remains available later if the set ever needs to be dynamically extensible, which nothing today requires.
- **`Decoration.replace`, `Decoration.widget`, `WidgetType`, and `EditorView.atomicRanges` all confirmed to exist with the assumed shapes.** One precision worth recording: `atomicRanges` affects cursor-motion and deletion commands, but does **not** prevent programmatic selection changes — doesn't contradict anything locked, just a detail worth knowing before relying on it for anything beyond keyboard interaction.
- **`atomicRanges` should be applied to tokens at rest, but deliberately *not* to ordinary Live-Preview-hidden inline marks (bold/italic/etc.).** This wasn't previously stated as sharply: at-rest atomic whole-token deletion is the *desired* behavior for semantic tokens (already Locked), but the same combination on an ordinary hidden formatting marker would reintroduce the "whole range deletes in one step" problem the original audit already flagged as a bug pattern to avoid. Both use `Decoration.replace`/`WidgetType`; only tokens additionally register in `atomicRanges`.
- **Decoration position stability via `RangeSet.map(changeDesc)` confirmed real** — not an assumed mechanism.

## Reference vocabulary

Concepts reused across many entries below; defined once here rather than re-explained per entry.

- **Reveal-on-engagement**: the shared lifecycle for every semantic inline construct. *At rest*: rendered as a collapsed/atomic widget. *Engaged*: raw Markdown revealed, ordinary character-level editing applies. Engagement is **derived**, not stored — see next entry.
- **Engagement is derived from selection**: a construct is engaged if and only if the current CM6 selection (including a zero-width caret) lies strictly within its syntax range. There is no engagement flag anywhere. This is why undo/clipboard/accessibility all "just work" for these constructs without bespoke code — anything defined purely in terms of selection inherits CM6's already-correct handling for free.
- **The one-way pipeline**: Stored Markdown → Parsed syntax → Semantic resolution → Display label → User activation. One direction only — there is no arrow from display back to stored. **Serialization** is the one exception, and it only ever fires once, at insertion time (e.g. autocomplete acceptance) — never as an ongoing re-derivation of already-existing text.
- **The shared semantic descriptor**: `{ kind, resolve, displayLabel, activate, validate, serialize }`, supplied per construct kind. `resolve`, `validate`, and `activate` are each **optional per kind** — not every kind needs all three meaningfully (e.g. a tag leans on `resolve`, `@due` leans on `validate`, neither needs both). `resolve()` returns **resolved / unresolved / ambiguous**, never a bare boolean — silently picking a candidate on ambiguity is never acceptable.
- **Lenient reader, strict writer**: Clutter's parser tolerates non-canonical-but-valid existing text (extra whitespace, `.`/`..` in paths, unnecessary-but-valid escapes) and never rewrites it just because it was read. Clutter's own writer always produces normalized, canonical text. Applied repeatedly across path normalization, alias trimming, and WikiLink escaping.

---

## Foundational principles (Locked)

All originate in the earlier architecture-proposal phase; restated here because every later document in this archive depends on them.

- **Markdown remains the sole canonical source of truth at all times.** No second/parallel rich-document model is ever introduced as a competing source of truth.
- **No second, independent parser.** Clutter-specific syntax is recognized through the exact same parse as CommonMark/GFM — never a regex/scanner pass running alongside it.
- **Clutter syntax extends `@lezer/markdown`'s public `MarkdownConfig` mechanism** (`parseBlock`/`parseInline`/`defineNodes`/`props`) — the same mechanism GFM's own Table/TaskList/Strikethrough/Autolink extensions are built on, confirmed against the library's own documentation, not assumed. See [parser-architecture.md](./editor-research/clutter-editor-parser-architecture.md).
- **Distinct Lezer node types per semantic kind** (`WikiLink`, `Tag`, `Mention`, `PropertyToken`, …) — not one generic `SemanticInline(kind=...)` node. A shared node-group/prop tags the family for the interaction layer's generic queries; grammar stays per-kind. See [shared-semantic-inline-model.md](./editor-research/clutter-editor-shared-semantic-inline-model.md).
- **One shared interaction mechanism for every inline semantic construct** — rendering, engagement, selection, deletion, clipboard, undo/redo, IME, and accessibility are shared infrastructure; only resolution, display label, activation, validation, and serialization are type-specific. Explicitly scoped to **inline** constructs — a future block-shaped construct (e.g. a full transclusion) is not assumed to fit this mechanism. See [shared-token-interaction-contract.md](./editor-research/clutter-editor-shared-token-interaction-contract.md).
- **Native CommonMark/GFM meaning must never be compromised.** A Clutter extension may only claim text that would otherwise remain literal — amended below with a precision the original one-line rule was missing.
- **Plain Markdown links (`[text](url)`) remain ordinary Live Preview marks**, not semantic tokens — validated, not assumed: link display text is exactly the content users routinely edit character-by-character, which atomic treatment would actively harm.
- **Images participate in the semantic-token/reveal-on-engagement family**, unlike plain links — validated for a different, firmer reason: an image's at-rest form is an actually-rendered widget, not styled text with hidden markers, which is structurally a token-family concern regardless of how often alt-text gets edited.

---

## Parser precedence & grammar

- **CommonMark/GFM-always-wins needs one addition, not just "ordering."** *Locked.* Where a Clutter trigger is a strict prefix of a built-in trigger (`[[` vs `[`, `![[` vs `![`), `before`-ordering (e.g. `before: "Link"`) is necessary but **not sufficient** — the extension's own matcher must also verify no valid built-in continuation (`(` or `[`) follows its closer, or a genuine CommonMark link like `[[Project A]](url)` would be silently misinterpreted. Where trigger characters share a start character but not a start *position* (`@mention` vs. GFM email autolink, whose match begins before the `@`), ordering alone (`after: Autolink`) is sufficient. See [precedence-validation.md](./editor-research/clutter-editor-precedence-validation.md).
- **`#tag` vs. ATX heading resolves automatically**, no extension logic needed — CommonMark's own space-required rule already prevents collision; block- and inline-level parsing are separate phases, so they never compete for the same dispatch point.
- **WikiLink requires `before: "Link"` plus the continuation-lookahead check above.** *Locked.* The same pattern applies identically to a future `![[embed]]` vs. `Image`.
- **WikiLink grammar and escaping — full formal rules.** *Locked.* Summary: backslash escapes exactly one following character, reusing CommonMark's own escapable-punctuation set (**semantic-rule reuse, not delegation** — the WikiLink parser's own scan must implement escape recognition itself; the generic `Escape` node never gets an independent turn inside an already-claimed WikiLink range — this was an imprecision in the first pass, corrected explicitly). First unescaped `|` is the separator, and only the first — further unescaped pipes are just alias text. First unescaped `]]` always terminates (lazy match, not greedy). No WikiLink nesting — an inner `[[` is never treated as a nested start. The parser never produces a partial node: either a complete, validly-closed structure is found, or the whole span falls through to ordinary text. See [wikilink-grammar.md](./editor-research/clutter-editor-wikilink-grammar.md).
- **Canonical serialization escapes every literal `]`, not only `]]`-forming pairs.** *Locked, corrected from an ambiguous first phrasing.* A naive "only escape adjacent `]]` found in the data" rule is unsafe, not merely non-minimal — a lone trailing `]` (e.g. `path = "A]"`) can silently combine with the closer written immediately after it (`[[A]]]` mis-parses), a boundary case invisible to a scan of the data alone. Escaping every `]` unconditionally is both simpler (context-free) and actually correct. See [wikilink-grammar-corrections.md](./editor-research/clutter-editor-wikilink-grammar-corrections.md).
- **Round-trip invariant, precisely stated.** *Locked, corrected from an overstated first version.* `parse(serialize(v)) == normalize(v)` — **not** `== v` for arbitrary unnormalized input. The writer trims whitespace as part of serialization, so exact equality only holds when `v` was already normalized. Canonical-serialization stability (repeated serialization doesn't drift) still holds, since `serialize()` is a pure function of the decoded value alone, never of prior raw text.

- **Highlight delimiter length — widened to any run `>= 2`, opening/closing lengths need not match (2026-08-27).** *Locked.* Supersedes the construct's original `~~`-mirroring "exactly 2, triple-run rejects" guard: `highlightSyntax.ts`'s `Highlight` parser now scans the full contiguous `=` run at each candidate position (the same primitive `@lezer/markdown`'s own built-in `Emphasis` scanner uses — confirmed against the installed `@lezer/markdown@1.7.2` source), gated at `>= 2` rather than a fixed width of exactly 2. `===text===`, `====text====`, and mismatched pairs like `==text===`/`===text==` all produce one flat `Highlight` node whose two `HighlightMark`s are exactly whatever run was actually matched on each side — never a fixed 2, never a swallowed/stray leftover character. **Deliberately not full `Emphasis`-style asymmetric-length *peeling* into nested sub-nodes**: that peeling lives in `@lezer/markdown`'s internal `resolveMarkers`, hard-gated behind an identity check against the library's own two singleton `Emphasis` delimiter objects — there is no public extension point that applies it to a custom `DelimiterType`. It's also not the same kind of ambiguity to begin with: `Emphasis`/`StrongEmphasis` are two distinct node types to peel into by length parity; `Highlight` has exactly one node type regardless of run length, so mismatched-length pairs are already fully and consistently handled by the generic (non-`Emphasis`) path in `resolveMarkers` with zero further change — it wraps the full open range and full close range as-is, requiring only that both delimiters share the same `type`, which every `Highlight` delimiter does. No renderer, decoration, marker-DOM, engagement, or CM6 navigation change was needed: both `delimitedInlineRenderer` (at rest) and `revealedMarkerRanges` (engaged) already resolve marker ranges from the actual parsed node, never a hardcoded 2-character assumption — confirmed by mounting a real `EditorView` before writing any code. See `inlineLivePreviewRegion.test.ts`'s `==text== (Highlight)` describe block for the regression coverage (symmetric runs of every length, asymmetric pairs, single-`=`/unflanked-run rejection, and independent-region composition).

- **Concealed marker inflated `.cm-line` to 30px at rest (24px once engaged) — fixed via `line-height: 1` (2026-08-27).** *Superseded 2026-08-27 (same day) — see the selection-geometry entry below.* The `line-height: 1` fix itself was correct and is preserved in spirit (concealed markers must not inflate the line box), but the near-zero-`font-size` technique it was patching a side effect of was replaced later the same day by an atomic-box-plus-`transform` technique, which does not have an inherited-line-height mismatch to begin with (`line-height: inherit` is safe once `font-size` is also `inherit`). Kept below, unedited, as the historical record of that investigation. **This is a general CSS box-model finding, not a WebKit/Tauri-specific one** — deliberately not recorded in `docs/tauri-webkit-editor-issues.md`, which is scoped to behavior proven to differ from Chromium; this bug was never shown to be engine-specific (Chromium could not be verified this session — see below), and its mechanism (an inline box's inherited `line-height` contributing "half-leading" space around its own near-zero-font-size content, independent of glyph size) is standard CSS2.1 inline-formatting-context behavior, not a browser quirk. Measured in real Safari: a formatted line's own `.cm-line.getBoundingClientRect().height` was `30px` at rest and `24px` once engaged, against an intended `24px` (`--lh-body`) in both states; a plain line with no marker spans measured `24px` throughout. **Root cause, proven by direct measurement:** `.cm-marker--concealed` overrides `font-size` down to a near-zero value but never declared its own `line-height`, so it inherited `.cm-content`'s absolute `line-height: 24px` unchanged — diffing every child of the line between at-rest and engaged states showed the concealed marker's `font-size` as the *only* difference, and it's the only thing that correlates with the 30px/24px split. **Proven to predate the `font-size: 0.05px` WebKit navigation fix**: reproduced identically at `font-size: 0` (the original pre-fix value) and `font-size: 1px`, via temporary injected overrides, never touching the production file until the actual fix was confirmed — this bug has nothing to do with which tiny font-size value the WebKit navigation fix (above, and `docs/tauri-webkit-editor-issues.md`) happens to use. **Isolating the exact contributor:** adding `line-height: 1` while leaving `font-size: 0.05px` completely unchanged restored `24px` on every construct tested (`==`, `**`, `~~`, `*`, nested `===**bold**===`), with the marker's own rect measured unchanged (still `0` height, still a fraction-of-a-pixel wide) — `line-height: 1` touches only the inherited-line-height mismatch, never concealment. **Fix**: `.cm-marker--concealed { font-size: 0.05px; line-height: 1; }` — one additional declaration on the existing rule, `font-size` untouched. **Verified in real Safari after the fix** (production CSS, no injected overrides): every construct measures `24px` at rest, engaged, and after; the full 13-line ArrowDown/ArrowUp regression corpus (`docs/tauri-webkit-editor-issues.md`) still sweeps with zero skips in both directions, including the previously fragile `***~~Italic bold strikethrough~~***` transition; all 8 variable-length Highlight cases measure `24px` and navigate cleanly; marker concealment (invisible at rest, reveals on engage, re-conceals on leaving) is unaffected. **Chromium was not verified with real confidence this session** — the real Chrome extension wasn't connected, and the available sandboxed Chromium automation pane could not reliably build a multi-line test document (a previously-noted limitation of that pane); only the built CSS asset's `line-height: 1` declaration was confirmed to parse correctly there, which is not a geometry or behavioral check. Given the standard-CSS nature of the mechanism, there is no specific reason to expect this differs in Chromium, but that expectation is not yet independently confirmed. Regression coverage: `apps/app/src/features/markdown/editor/markerConcealedLineHeight.test.ts` (a source-level assertion that the rule declares both properties — jsdom cannot compute real layout, so this is a tripwire against reverting the fix, not a layout-correctness test).

- **Concealed marker's near-zero `getClientRects()` height zeroed out `drawSelection()`'s selection-background rectangle for the whole line — fixed by switching to an atomic-box `transform` technique (2026-08-27).** *Superseded 2026-08-27 (same day, next entry below) — the `transform: scaleX()` fix here correctly solved the vertical-geometry bug it targeted, but `transform` never changes an element's *layout* box, only its painted one (confirmed at the CSS-spec level), so the marker still reserved its full original glyph width in the line's horizontal layout — every concealed marker on a line pushed later content right by its own (invisible but still-reserved) width, compounding with each additional marker. Reproduced directly: a document with `*x*`/`**x**`/`***x***`/`****x****` on consecutive lines rendered each line's visible content at a progressively larger left offset. Kept below, unedited, as the historical record of that investigation — its vertical-geometry fix and measurements are still accurate; only the technique as a whole is superseded.* **Bug**: Cmd+A (`selectAll` — a plain `{from: 0, to: doc.length}` selection; CM6 selection semantics themselves are not in scope here and were not touched) on a document whose first line starts with a concealed marker painted no selection-background rectangle at all for that line, even though the line was genuinely selected — reproduced and measured in this session's Chromium-based browser pane, no WebKit-specific dependency assumed or required. Markers were confirmed to stay concealed throughout (`.cm-marker--concealed` count unchanged, `.cm-marker`/`.cm-marker--concealed` counts equal before/during/after Cmd+A) — the earlier appearance of "raw Markdown becomes visible under Ctrl+A" (screenshot-driven starting hypothesis) is **not** a decoration-class change; `isTokenEngaged` (`codemirror/semanticToken/tokenEngagement.ts`) only reveals a construct when the selection is strictly *contained inside* that construct's own node range, which a document-spanning selection never is for an ordinary inline construct. That appearance was this same rendering bug in disguise. **Root cause**: CM6's `drawSelection()` does not use native `::selection` painting — it computes its own `.cm-selectionBackground` rectangles in JS (`rectanglesForRange` in `@codemirror/view`) by querying `coordsAtPos`/DOM `Range.getClientRects()` at selection boundaries and deriving each rectangle's `top`/`bottom` from those boundary rects. The prior concealment technique (`font-size: 0.05px; line-height: 1`, previous entry) left the marker's own `getBoundingClientRect().height` measured at `0` (confirmed directly: `.cm-marker--concealed` elements' `getClientRects()` returned `height: 0` uniformly). When a selection boundary landed on/inside such a marker, that `0`-height rect fed directly into the line's selection-rectangle height — measured directly: the selection layer's first box (`.cm-selectionLayer .cm-selectionBackground`) had `height: "0px"` for exactly the line starting with a concealed marker, while an ordinary line in the same selection measured its correct `72px`(3-line block)/`19px` height. **Fix**: give the marker a normal, non-degenerate content-box height instead of trying to suppress both dimensions via `font-size`. `.cm-marker--concealed` is now `display: inline-block; font-size: inherit; line-height: inherit; transform: scaleX(0.001); transform-origin: left center;` — the box lays out at the run's real size (so its content-box height always matches its context, header or body text alike, with nothing further needed for `line-height` correctness) and is squashed to near-zero *width* only, only visually, after layout, via `transform`. `transform` was confirmed load-bearing specifically because of `display: inline-block`: a plain `inline` element ignores `transform` entirely per spec (verified directly — identical rule with `display` left at its default measured an *unscaled* `getBoundingClientRect().width`, close to the marker's natural glyph width). Measured after the fix: marker `getBoundingClientRect()` is `{height: 24, width: ≈0.0145}` for a 2-character marker at a 24px line-height context (down from the predecessor's `0.05px`-ish width, and no longer `height: 0`); every `.cm-line` in a 4-line test document measures the correct `24px`, matching the previous entry's fix (no reintroduced inflation). A `width: 0; overflow: hidden` alternative was tried and rejected: forcing `width: 0` on the atomic box makes the browser wrap the marker's own multi-character text one glyph per internal line, which *doubled* the box's own height (measured `48px` instead of `24px` for `**`) — the same inflation failure mode this whole investigation started from, just triggered by width instead of font-size. **Verified in the real Clutter webapp** (not just an isolated harness, per this repo's standing testing rule) in the browser pane's Chromium engine: Cmd+A on a 4-line document (`**Bold**` / `*Italic*` / `***Italic bold***` / `**bold `` `code` `` _italic_ ~~strike~~**`) paints a selection-background rectangle for every line including the one starting with a marker, with zero `height: "0px"` boxes in `.cm-selectionLayer` (down from 2 of 3 boxes at `0px` before the fix); ArrowUp/ArrowDown single-steps through all 4 lines with zero skips in either direction (this fix does not rely on any near-zero `font-size` value, so it does not merely avoid the WebKit vertical-motion bug the predecessor's `0.05px` was tuned against — it structurally cannot trigger it, since `font-size` is never shrunk at all); ArrowRight single-steps character-by-character through a concealed marker's own text (DOM Range offsets identical to the predecessor technique at each step — `display: inline-block` does not turn the marker into an atomic caret-motion unit); Shift+End selection through a line containing five different marker/construct types (bold, code, italic, strikethrough) paints one continuous non-zero-height selection rectangle; a partial mouse-driven selection with a boundary landing immediately at a marker edge measures a correct non-zero-height (`19px`) rectangle; Cmd+A followed by typing replaces the full document correctly (selection-state semantics are a CM6 `EditorState` concern entirely orthogonal to this CSS-only fix — nothing here can affect `selection.main.from`/`.to`); normal (unselected) at-rest and engaged rendering is visually unchanged from before the fix. Chromium-verified this session (browser pane); not independently re-verified in Safari/WebKit, though the fix's mechanism (an atomic box's own content-box height, plus a spec-standard `transform`) is not WebKit-specific and the change is, if anything, less exposed to the WebKit near-zero-`font-size` navigation bug than its predecessor, not more. Regression coverage: `apps/app/src/features/markdown/editor/markerConcealedLineHeight.test.ts` was rewritten (source-level tripwire only, same jsdom limitation as before) to assert the new technique's declarations (`display: inline-block`, `font-size`/`line-height: inherit`, `transform: scaleX(...)`) and to guard against reverting to either rejected alternative (a shrunk `font-size` or a `width: 0` box).

- **Concealed markers switched from `Decoration.mark` + CSS metric-hacking to `Decoration.replace()` + a dedicated widget (`ConcealedMarkerWidget`) — the CM6-native mechanism for concealment (2026-08-27, later same day).** *Locked.* **Why the entire `Decoration.mark`-based line of fixes above was replaced, not just further tuned:** both prior CSS techniques failed for the same underlying, structural reason — a *mark* decoration can only ever style the marker's own real glyphs, never replace or resize them, and a real multi-character text run's width and height are governed by the same font metrics. Shrinking `font-size` fixed width but broke height (`getClientRects()` → `0`, breaking `drawSelection()`'s selection-rectangle math — the previous-previous entry). Keeping `font-size` normal and using `transform: scaleX()` fixed height but broke width, because CSS transforms never change an element's layout box, only its painted one — the marker still reserved its full glyph width in the line's horizontal layout, so lines with more/longer concealed markers rendered progressively more indented (the previous entry, and the bug the user's screenshot in this session named directly: `*Italic*`/`**Bold**`/`***Italic bold***`/`****Bold****` each pushed further right than the last). No CSS declaration on a *mark* decoration can fix both axes at once, because both are contending for control of the same real text run.

  **Fix**: `inlineLivePreviewParticipants.ts`'s `delimitedInlineRenderer` now emits `Decoration.replace({widget: new ConcealedMarkerWidget(markerClass)})` for each marker range (open and close) instead of `Decoration.mark({class: ...})`, for the same five constructs as before (Emphasis, StrongEmphasis, Strikethrough, Highlight, InlineCode). This is CM6's own documented mechanism for this exact job — the `Decoration.replace()` JSDoc states it "replaces the given range with a widget, or simply hides it," and the official examples page names concealment as its paradigm use case, in contrast to `Decoration.mark`'s own documented purpose (styling, e.g. syntax highlighting) — this migration was, in that sense, moving *onto* the primitive CM6 designed for concealment, off one that was never intended for it. `ConcealedMarkerWidget` (`apps/app/src/features/markdown/editor/codemirror/highlight/ConcealedMarkerWidget.ts`) renders an **empty** `<span>` — no marker text at all, unlike the retired techniques — carrying `cm-marker cm-{construct}-marker cm-marker--concealed` for continuity with existing per-construct concealed-marker queries. Because the widget is a *new*, independently-styled element rather than the marker's own glyphs, its box is authored independently of any font metric: `.cm-marker--concealed` is now simply `display: inline-block; width: 0; height: 1em;` — two previously-irreconcilable declarations (a real, uncontested `width: 0` and a normal, non-degenerate `height`) that no longer compete, because there is no glyph run underneath either of them to reserve space for.

  **How the widget gets correct vertical metrics in every context (body, headings, nested content):** `height: 1em` is deliberately relative, not a pixel value, and `font-size` is left unset on the rule entirely — the widget inherits whatever `font-size` its actual rendering context provides (16px body text, or a heading's own larger `tok-heading1`-`tok-heading6` font-size when a marker is nested inside one), so `1em` scales correctly with zero per-context CSS. This does not need to equal the *line's* full `line-height` exactly: `drawSelection()` takes the min/max across every rect it queries on a line, so as long as this box's height is non-zero and roughly glyph-sized (not wildly larger, which would risk the same inflation the `line-height: 1` fix above exists to prevent), the line's neighboring real text already establishes the correct bounding rectangle. `display: inline-block` is required for `height` to apply at all — a plain `inline` element's box height is derived from its content and `line-height`, not a settable property, and this element deliberately has no content to derive one from (an empty `inline-block` left at `height: auto` collapses to `0` per CSS2.1 §10.6.7 — "no in-flow content" — which is exactly why `height: 1em` is load-bearing, not decorative).

  **How marker hit-testing works, and why this does not repeat the reason Clutter originally moved *away* from `Decoration.replace()` for this family** (`docs/markdown-dom-structure-agreement.md` §5.5/§7.1, `inlineLivePreviewParticipants.ts`'s own prior doc comment): that original concern was about a **bare** `Decoration.replace({})` — no widget, no DOM node at all, nothing to click. `ConcealedMarkerWidget` is a real, positioned, zero-width DOM element, not an absence — and `WidgetType`'s default `ignoreEvent()` returns `true`, meaning **by default a widget's events, including `mousedown`, never reach CM6's own click-to-position handling at all** (confirmed by reading `@codemirror/view` source directly — the same fact `WikiLinkWidget.ignoreEvent`'s own doc comment already established for a different reason). `ConcealedMarkerWidget.ignoreEvent()` overrides this to unconditionally return `false`, letting `mousedown` reach CM6's built-in handling. Verified directly, not assumed: a real click at the widget's own on-screen coordinate (in the real Clutter webapp, not just the sandbox) resolved to the correct document position and engaged the construct, every time tried, across five different construct kinds and multiple nesting depths.

  **`EditorView.atomicRanges` is not, and was not made, applicable to this family — confirmed directly, not merely carried over by assumption.** Unlike the genuinely-atomic widget family (`WikiLink`/`Tag`/`Date`, `widgetReplaceRenderer`), `delimitedInlineRenderer`'s `ParticipantDecoration` return value has no `atomic` field for the marker ranges — the same participant-owned-fact mechanism that already governed this before the migration (`inlineLivePreviewRegion.ts`'s `buildDecorations`) is unchanged, so there was no new call site that could have silently added atomicity. Regression coverage added (`inlineLivePreviewRegion.test.ts`, "every `ConcealedMarkerWidget`-backed participant never registers as atomic, at rest") and Backspace/Delete were independently re-verified against real CM6 commands and, separately, live in the real webapp (`Delete` at a concealed marker's own boundary removed exactly one character — `**bold**` → `*bold**` — not the whole marker).

  **Reconciles `docs/obsidian-vs-clutter-editor-audit.md`'s prior "do not migrate markers to widgets" recommendation (§1, §24(G))** — that pass's PROVEN source facts (`moveByChar` is decoration-blind; `WidgetTile.coordsIn` collapses interior widget positions to one of two edge rects) are unchanged and correct, but its *inference* from those facts to a predicted user-visible degradation was never checked against reveal-on-engagement, and does not hold once it is: **any** position inside a concealed construct's range satisfies `isTokenEngaged`'s containment check, which reveals the *entire* construct as real text on the very same render — so which collapsed sub-position a vertical-motion landing resolves to inside an about-to-be-revealed marker is not a user-observable outcome. Verified directly: ArrowUp/ArrowDown through lines starting with 1/2/3/4-character concealed markers, and through a heading and nested list items containing them, swept with zero skips in both directions, both in an isolated CM6 sandbox and in the real Clutter webapp. See that document's own superseding note (top banner and end of §24(G)) for the full reconciliation.

  **Verified in the real Clutter webapp** (browser pane, Chromium engine — not just the isolated sandbox used to first validate the approach), across a document exercising every construct kind, adjacency, nesting, heading, and list context named in this session's test plan:
  - *Horizontal layout (the bug this migration fixes):* every `.cm-line`'s left edge measured identical (`425.828125px`) across eleven lines of 1/2/3/4-character markers, a heading, and nested-list content — zero progressive indentation, directly reproducing and resolving the reported screenshot. Every `.cm-marker--concealed` widget measured `{width: 0, height: 16 or 28}` (28 inside the heading, confirming context-relative `1em` scaling), never `0` height.
  - *Selection:* Cmd+A painted a selection-background rectangle for every line with zero `height: "0px"` boxes (down from the predecessor's 2-of-3); all 36 concealed markers stayed concealed throughout (no decoration-class change from selection breadth, confirming the "markers reveal under Ctrl+A" appearance from the original bug report was the selection-rectangle bug, not a real reveal). Shift+ArrowDown+Shift+End selection starting outside a construct and extending across a full concealed line painted a correct, continuous, non-zero-height selection region without revealing the spanned marker. A mouse-drag starting exactly at a concealed marker's own pixel and extending into its content produced a single correct, non-zero-height selection box and engaged the construct.
  - *Caret/navigation:* 10 consecutive ArrowDown, then 10 consecutive ArrowUp, from the first line through the last (crossing every marker-length variant, the heading, and both list levels) landed on the correct line every single time, zero skips either direction. Shift+Home/Shift+End selected the exact raw construct text (`**bold**`).
  - *Editing:* `Backspace` at a line-join boundary removed exactly the newline (no marker touched); `Delete` at a concealed marker's own start boundary removed exactly one character (`**bold**` → `*bold**`), not the whole marker; both were confirmed reversible via `Cmd+Z`. `Decoration.mark`+widget composition (nested nesting: `***bold italic***`, `~~***bold _italic_ `` `code` ``***~~`) and heading-embedded/list-embedded markers all rendered and engaged correctly.
  - *Hit-testing:* covered above.
  - Not independently re-verified in real Safari/WebKit this session (Chromium-only, browser pane) — nothing in the fix's mechanism (an empty widget's own CSS box, plus `WidgetType.ignoreEvent`, both ordinary/documented CM6 behavior) is WebKit-specific, and it structurally cannot trigger the original WebKit near-zero-`font-size` vertical-motion bug at all, since no `font-size` is ever shrunk anywhere in this technique. Copy/paste was not independently re-verified this session (synthetic clipboard events are blocked by this browser pane's OS-level clipboard permission) — unaffected by construction, since CM6's clipboard serialization reads `state.sliceDoc()` directly, never the rendered DOM, for every decoration type including this one.

  **Files changed**: `apps/app/src/features/markdown/editor/codemirror/highlight/ConcealedMarkerWidget.ts` (new); `inlineLivePreviewParticipants.ts` (`delimitedInlineRenderer`'s marker-decoration construction, plus doc comments); `MarkdownEditor.css` (`.cm-marker--concealed` rule and its doc comment, replacing the `scaleX`/near-zero-`font-size` techniques entirely — no CSS from either predecessor remains); `markerConcealedLineHeight.test.ts` (rewritten: CSS-rule structural tripwire for the new declarations, plus new DOM-footprint and `ConcealedMarkerWidget` unit tests); `inlineLivePreviewRegion.test.ts` (updated the one assertion that expected concealed markers to carry real text, added the explicit not-atomic regression test, updated the `visibleText` helper's doc comment); `headingContentClassing.test.ts`/`tableDecoration.test.ts`/`horizontalRuleDecoration.test.ts`/`wikiLinkLivePreview.test.ts` (one-line doc-comment corrections, no behavior change — these already worked correctly under the new technique without modification, since an empty widget contributes no text for their own `textContent`-walking helpers to need special-casing). Regression coverage: `markerConcealedLineHeight.test.ts` (rule structure + widget unit tests), `inlineLivePreviewRegion.test.ts` (marker-span shape at rest, not-atomic), `markerBoundaryBehavior.test.ts` (Backspace/Delete/Home/End/Shift-selection via real CM6 commands — unchanged by this migration, still passing, since none of it depended on which concealment technique was in use).

---

## Shared semantic-inline interaction contract

- **Engagement mechanics reconcile "atomic" and "arrow-key-enterable."** *Superseded 2026-08-23 — see below.* ~~A one-position hop treats the token as a single atomic unit for that keypress; a *second* press once the caret is already at the boundary steps inside. Same mechanism at two distances, not two mechanisms.~~
- **The hop lands on the boundary facing the direction of approach, never the opposite side.** *Superseded 2026-08-23 — see below.* ~~The previously "flagged for prototype confirmation" felt-experience was resolved: `ArrowLeft` toward a token from its right (with or without intervening whitespace/text) must land the caret at the token's right/near boundary, engaging it from the right — never jump clean through to the far/left boundary. Symmetrically for `ArrowRight` from the left. A hop never makes the caret appear to have "passed through" the token to its far side in one press.~~
- **Keyboard-only activation gap.** *Superseded 2026-08-23 — see below.* ~~The boundary-entry gesture gets a keyboard user into editing, but does not itself invoke `activate()`. A defined key (e.g. Enter, when the caret sits adjacent to an at-rest token) must trigger the same action a click would, or keyboard-only users can edit every token but never activate one.~~
- **Selection touching a token from outside never partially reveals it.** *Superseded 2026-08-23 — see below.* ~~A selection gesture that starts before or ends after a token's range sweeps it in as one atomic unit; a gesture that would land partway inside snaps outward to the nearest boundary. Prevents a drag across a token-dense line from exploding every token into raw text at once.~~
- **Superseding decision (2026-08-23): CodeMirror owns cursor and selection behavior; Clutter does not intercept Arrow keys or reposition the cursor.** The four entries above described a bespoke ArrowLeft/ArrowRight hop mechanism (`semanticToken/tokenKeymap.ts` and its per-kind wrappers) and a matching drag-selection atomic-sweep correction (`semanticToken/tokenSelectionSnap.ts` and its wrappers). Both were removed — the keymap in commit `58c7d9d7` ("Reset Markdown editor keyboard behavior to CodeMirror defaults"), the selection-snap wiring and the Alt-click cursor-repositioning special case in a follow-up cursor/selection behavior audit and reset. A full audit at removal time found no genuine product requirement neither mechanism was actually serving: with Live Preview decorations (the collapsed at-rest widgets these mechanisms existed to compensate for) currently dormant, both had degraded into overriding CM6's own correct default cursor/selection placement on what is, today, fully ordinary editable raw Markdown text. If/when Live Preview decorations for these constructs are re-enabled, whether an equivalent mechanism is genuinely needed again is an open question to re-decide then, evaluated fresh against CM6's actual default behavior at that time — not reinstated by default. Plain click activation (open a WikiLink, resolve a date, toggle a task checkbox) is unaffected — that remains, as product interaction rather than cursor behavior. `semanticToken/tokenSelectionSnap.ts` and the per-kind `*SelectionSnap.ts` files are left in place, unwired, per this codebase's existing convention for dormant modules (see the many dormant `*Decoration.ts` files already following that pattern) — not deleted, since deletion would foreclose the question above rather than leave it open.
  - **Note, added after the emphasis slice shipped (2026-08-24):** this trigger has not fired for Bold/Italic. `emphasisLivePreview()` (below) is now enabled, but the ArrowLeft/ArrowRight hop and drag-selection atomic-sweep mechanisms this entry describes were never part of that family's scope to begin with — they existed only for the atomic semantic-token family (WikiLink/Tag/Date/Task), which is not affected by the emphasis slice and remains dormant (see "Currently wired vs. dormant," below). The open question above is still open, and still specifically about the semantic-token family.
  - **The open question fired, narrowly, for WikiLink's concealed folder-prefix (2026-09-09) — then was reversed the same day.** `wikilink/wikiLinkLivePreview.ts` briefly concealed a WikiLink's folder-qualified path segment (e.g. the `Projects/Design/` in `[[Projects/Design/My Note]]`) even while engaged — real, non-atomic document text that CM6's default ArrowLeft/ArrowRight motion steps through one keystroke at a time with no visible caret movement in between (arrow-key-stuck-at-`[[` investigation: `EditorState.selection` was confirmed, via live `document.getSelection()` instrumentation, to advance correctly the whole time — nothing was actually stuck at the model level; the *experience* of crossing several silently-no-op keystrokes is what read as "stuck"). The fix at the time was a `Prec.highest` ArrowLeft/ArrowRight keymap (`wikilink/wikiLinkConcealedPrefixNavigation.ts`) scoped to hop over exactly that concealed range.
    - **Superseded, same day, by simplification rather than by further narrowing the keymap.** A full audit of every construct in the codebase found WikiLink's folder-prefix concealment was the *only* case anywhere that violated the reveal-on-engagement contract ("engaged" ⇒ complete raw source, nothing hidden) — every other construct routed through `isTokenEngaged`/`isPhysicalLineEngaged` (headings, emphasis/strong/strikethrough/highlight/inline-code, blockquote, table rows, Tag, Date, embeds, images) reveals its full real text once engaged, with concealment reserved strictly for the at-rest state. (Task checkboxes are a separate, deliberately different case — they never reveal source at all, by an explicit, independent product decision predating this one — not a byproduct of this contract and not touched by this reversal.) Given that, the folder-prefix concealment was the anomaly, not the keymap's narrowness: removing the concealment removes the need for any bespoke Arrow-key handling at all, restoring "CodeMirror owns cursor and selection behavior" without a carve-out. `wikilink/wikiLinkConcealedPrefixNavigation.ts` and its test were deleted; `wikilink/wikiLinkLivePreview.ts`'s engaged branch now renders the complete raw source (`[[`, the full path, `|alias` if present, `]]`) unconditionally, same as Tag/Date's engaged contract. At the time, the WebKit bare-text-adjacency fix (`ENGAGED_WIKILINK_MARK`, wrapping engaged text in a `Decoration.mark`) was kept unchanged as unrelated to folder-path concealment specifically.
    - The minimal case `[[Text]]`'s own reported extra-ArrowRight-press symptom was *not* assumed fixed by this reversal — it was re-tested independently as a separate question, since folder-prefix concealment cannot explain a slash-free path's behavior. Re-tested live in the real webapp (Chromium browser pane): stepping ArrowRight/ArrowLeft through `before [[Text]] after` advanced exactly one real character per keystroke in both directions, no skips — could not reproduce the reported symptom there. Not independently re-verified in the native Tauri/WebKit app.
    - **`ENGAGED_WIKILINK_MARK` removed as dead code, same day, once its sole justification no longer applied.** The mark's only documented purpose was ever the WebKit bare-text-adjacency caret workaround, not styling (no `class`/`attributes`) — with folder-prefix concealment gone and no caret problem observed, there was nothing left for it to justify. Removed along with `buildEngagedDecorations` (the function that constructed it), which itself collapsed to always returning `[]` once the mark was gone — the engaged branch in `buildDecorations` now just returns early, decorating nothing. Final shape: at rest → atomic widget; engaged → completely undecorated raw text, ordinary CM6 defaults for cursor, selection, and editing, with zero WikiLink-specific mechanism of any kind.
- **No trap state.** *Locked.* Leaving an engaged token is just continuing to move the cursor or clicking elsewhere — never a required special action.

See [shared-token-interaction-contract.md](./editor-research/clutter-editor-shared-token-interaction-contract.md) for the full state model, selection/deletion/undo/clipboard mechanics, and the accessibility baseline.

---

## WikiLink resolution, aliases, and display

- **Display-label precedence, corrected from the originally proposed ordering.** *Locked.* **Local alias > target's primary frontmatter alias > filename** — not "filename > target aliases" as first proposed. Putting filename above target aliases would mean a page's alias never actually changes how it displays anywhere unless every reference is hand-edited, defeating much of the point of having page-level aliases. This does **not** conflict with "editing frontmatter never rewrites references" — that guarantee is about the stored buffer; display is a pure, always-re-derived render, the same "derived data is disposable" pattern already governing every decoration in this architecture. See [semantic-resolution-model.md](./editor-research/clutter-editor-semantic-resolution-model.md).
- **Bare-reference display is live and dynamic, never written back.** *Locked.* Renaming a target's primary alias changes what every bare (no-local-alias) reference to it shows, with zero buffer mutation anywhere. A reference carrying an explicit local alias is completely unaffected, forever, unless a user edits it directly.
- **Alias-based resolution (`[[Alpha]]`) is supported as a fallback**, tried only after a literal path match fails. *Locked.* The Markdown is never auto-rewritten to the canonical path on resolution — resolution never causes a write, without exception.
- **Ambiguous alias/mention matches must produce an explicit ambiguous resolve state, never a silent pick.** *Locked.* Silently choosing a candidate risks opening or editing the wrong entity without the user realizing — a materially worse failure mode than an honest "please disambiguate."
- **Path normalization**: vault-relative, `/`-separated, no `.`/`..`, no extension, in Clutter's own canonical writer — but the parser remains tolerant of reading non-canonical-but-valid variants without ever rewriting them. *Locked.*
- **Autocomplete acceptance is insertion-only, never navigation.** *Locked.* Accepting a WikiLink completion — the `+ Create "X"` option, an existing-page option, via click, Enter, or the `|` alias-boundary key — only inserts (and, for `+ Create`, creates) the reference; it must never make the newly-referenced/created page the active view, and the editor keeps focus. The only thing that opens/navigates to a WikiLink is an explicit `activate()` on an already-rendered token (a genuine click on the token itself, or the keyboard adjacent-activation path) — never a side effect of the insertion mechanism that produced it.

---

## Atomic inline widgets and text-decoration composition

- **A Markdown construct that renders as an atomic inline-level box (`display: inline-flex`/`inline-block`/`inline-table`, or an atomic replaced element) does not receive an ancestor's `text-decoration-line` painting for free.** *Locked, discovered 2026-09-09 via `~~[[WikiLink]]~~`.* `text-decoration-line` is not an inherited CSS property, and its rendering only propagates through ordinary (non-atomic) inline descendants — per spec, an atomic inline-level box is where propagation stops, regardless of nesting depth above or below it. `.tok-wikilink` needs `display: inline-flex` for its icon+title layout, so `~~[[Page]] normal link~~` struck through "normal link" but never the WikiLink widget, confirmed via direct DOM/computed-style inspection (`.tok-wikilink`'s own `text-decoration-line` computed to `none` while its `.tok-strike` ancestor's computed to `line-through`) — genuine correct DOM nesting, not a decoration-composition/precedence bug.
- **Two CSS-only fixes were tried before the token-level mechanism below; the second (descendant selectors) shipped briefly and is now superseded too.** *Superseded 2026-09-09 (later the same day) — see "Inline formatting composition at the token level" below.* (1) `text-decoration-line: inherit` on `.tok-wikilink` alone works only when the widget is a *direct* child of the decorated mark — `inherit` reads only the immediate parent's computed value, so it breaks the moment one more mark intervenes (`~~**[[Page]]**~~`'s immediate parent is `.tok-strong`, whose own `text-decoration-line` is `none`). Extending `inherit` to every intermediate content-mark class (`.tok-strong`/`.tok-emphasis`/`.tok-highlight`) does fix arbitrary depth (verified), but was rejected as the general fix — it changes the shared, unrelated marks' own contract on behalf of one atomic widget's layout choice. (2) **What shipped, then was superseded**: a `.cm-editor .tok-strike .tok-wikilink { text-decoration-line: line-through; }` descendant-selector rule, added only to WikiLink's own CSS block, matching at any nesting depth because a descendant combinator ignores whatever sits between ancestor and descendant. Verified live across `~~[[Page]]~~`, `~~**[[Page]]**~~`, `**~~[[Page]]~~**` (reversed order), and `~~==**[[Page]]**==~~` (three levels) — all correct — with `.tok-strong`/`.tok-emphasis`/`.tok-highlight` completely untouched. The icon (an inline `<svg>`) never receives the line either way — SVG content doesn't paint text-decoration lines regardless of the property value — which reads as correct (the line strikes the text label, not the glyph), not a gap; this fact is unaffected by the supersession.
- **A second, independent decorating ancestor was found the same way: a completed task's own line-level strikethrough.** *Superseded 2026-09-10 — see "Inline formatting composition at the token level" below; the "line-level, not tree-derivable" premise here was wrong.* `- [x] text [[Page]]` struck through "text" but not the WikiLink, for the exact same atomic-box reason — but the decorating rule here was `.cm-editor .cm-line:has(.cm-task-checkbox[aria-checked='true'])` (`taskCheckboxDecoration.ts`'s completed-task styling), a completely separate mechanism from `~~~~` Strikethrough, not routed through `.tok-strike` at all. Fixed at the time with a `.cm-editor .cm-line:has(.cm-task-checkbox[aria-checked='true']) .tok-wikilink { text-decoration-line: line-through; }` rule, reasoned to be permanent because "task-completion is line-level state, not a syntax-tree ancestor." **That premise was never actually verified and turned out to be false**: `@lezer/markdown`'s own `TaskList` extension parses a task's entire remaining line content as `Task`'s own inline children (confirmed directly against the installed source — `TaskParser.finish` builds `Task[TaskMarker, ...cx.parser.parseInline(leaf.content.slice(3), ...)]`), so a WikiLink/Tag/Date on a task line is a genuine syntax-tree descendant of that line's `Task` node, exactly as tree-derivable as any delimited-mark ancestor. Both the CSS rule and the "line-level, not tree" reasoning are superseded below.
- **Superseded generalization**: *Superseded 2026-09-09 — see "Inline formatting composition at the token level" below.* ~~the general principle for any future atomic-box inline widget is to add one `.tok-<decorating-context> .tok-<widget>` descendant-selector rule per decorating context it needs to support.~~ That still enumerates one CSS rule per (widget × decorating-context) pair — exactly the "matrix of selectors" the replacement mechanism exists to avoid.

## Inline formatting composition at the token level

- **Inline Markdown formatting must compose at the final rendered inline token/span level.** *Locked, 2026-09-09.* When multiple Markdown constructs overlap, participating rendered segments receive all applicable semantic and formatting classes directly, on their own root element — never reconstructed via CSS ancestor selectors or an enumeration of nesting/order combinations. This supersedes both CSS-only fixes above: a widget-family construct's own element now carries e.g. `tok-strike` directly (`<span class="tok-wikilink tok-strike">`), so the plain, unqualified `.tok-strike { text-decoration-line: line-through; }` rule (no descendant selector at all) already applies to it — correct at any nesting depth, order, or combination by construction, not by enumeration.
- **Mechanism**: `collectActiveInlineClasses(node)` (`inlineLivePreviewParticipants.ts`) walks a node's ancestors, collecting the content class of every enclosing delimited-mark construct (Emphasis, StrongEmphasis, Strikethrough, Highlight, InlineCode, Link, Autolink), using `isDelimitedMarkConstruct`'s existing structural test (two identically-named `*Mark`-suffixed children — the same fact `delimitedInlineRenderer` and `wikiLinkLivePreview.ts`'s engagement-widening already keyed off, now shared rather than duplicated) — never a list of node names, which is what makes it depth/order/combination-agnostic for free. `widgetReplaceRenderer` (Tag/Date) and `wikiLinkLivePreview.ts` (WikiLink) each call this once per token and thread the result into `TagWidget`/`DateWidget`/`WikiLinkWidget`'s new `extraClasses` constructor parameter, applied via `classList.add('tok-<construct>', ...extraClasses)` in `toDOM()`.
- **Deliberately pure and tree-only**: `collectActiveInlineClasses` takes only a syntax node — no `EditorState`/`EditorView`, no knowledge of engagement, selection, or completed-task line state. Adding a new delimited-mark construct never requires touching this function, only the one `contentClass` entry its own participant registration already needs; adding a new widget-family construct (a future URL widget, mentions, ...) means threading `extraClasses` through that one widget's constructor, following the exact same pattern as Tag/Date/WikiLink — never a new CSS selector.
- **The principle generalizes beyond syntax-tree-derived formatting: active visual/decorator state is composed directly onto the rendered element that paints the text, whether that state comes from the syntax tree (`tok-*`) or from editor/task state (`cm-*`) — *for plain, unclassed text*.** *Locked, 2026-09-10; narrowed 2026-09-13 (see below).* Completed-task strikethrough (`- [x] ...`) is the second instance of this principle, not a special case of it: `isNodeOnCompletedTask(node, state)` (`taskEngagement.ts`) walks a node's ancestors looking for an enclosing `Task` node and reads its `TaskMarker` child's own raw text (`state.sliceDoc`) to distinguish `[x]` from `[ ]` — genuinely tree-derivable, per the corrected premise above, just needing `state` in addition to the tree (unlike `collectActiveInlineClasses`, which needs neither) because "checked or not" lives in the marker's *text*, not its node *name*. `TASK_COMPLETED_CLASS` (`'cm-task-completed'`, following the existing `cm-*` = editor/CodeMirror-state naming convention already established by `cm-task-checkbox` — deliberately not `tok-task-completed`/`task-completed`/`is-task-completed`, which would either misname it as syntax identity or invent a new convention) is composed for plain text by a separate, narrowly-scoped `ViewPlugin` (`taskCompletedContentDecoration.ts`), which gives that plain text a mark to compose onto: a single `Decoration.mark({class: TASK_COMPLETED_CLASS})` over the checked `Task` node's whole content range (`taskMarker.to` to `task.to`), at default precedence (an ordinary content mark, not a widget, composing the same way `.tok-strike`'s own content mark already does) — deliberately **not** a participant of `inlineLivePreviewRegion.ts`'s shared reveal-on-engagement traversal, since `Task` must stay out of that mechanism's scope (per that file's own doc comment) to avoid entangling task rendering with inline-construct engagement semantics. `EditorView.atomicRanges`/checkbox interaction/task persistence/task syntax are all untouched — only how "is this checked" reaches rendered text changed.
- **Reversed 2026-09-13: the widget family (WikiLink/Tag/Date) no longer also composes `TASK_COMPLETED_CLASS` directly onto its own root.** `widgetReplaceRenderer` (Tag/Date, `inlineLivePreviewParticipants.ts`) and `wikiLinkLivePreview.ts` (WikiLink) used to call `isNodeOnCompletedTask` themselves and concatenate `TASK_COMPLETED_CLASS` alongside `collectActiveInlineClasses`'s own classes, following the "compose directly onto the rendered element" principle literally. In practice this double-applied the class for one logical occurrence — the widget's own root *and* `taskCompletedContentDecoration.ts`'s ancestor mark, which already wraps the widget in the real editor, both carried `cm-task-completed` — and that duplication broke styling that isn't idempotent under double application the way `text-decoration-line` is (e.g. `opacity`, or `color-mix(in srgb, currentColor 50%, transparent)`, which compounds when the same rule matches on two nested ancestors). The fix removes the widget-level composition entirely rather than deduplicating in CSS: `taskCompletedContentDecoration.ts`'s ancestor mark is now the *sole* source of `cm-task-completed` for every construct, including the widget family, restoring the pre-2026-09-10 shape for this one class while keeping `collectActiveInlineClasses`'s own tree-derived composition (which never had this problem — a widget occurrence has exactly one ancestor delimited-mark chain, not two independent wrappers of the same range) untouched.
- **The old `.cm-line:has(.cm-task-checkbox[aria-checked='true'])` line-level rule, and its WikiLink-specific descendant-selector escape hatch, are both removed.** `.cm-task-completed { text-decoration-line: line-through; }` is now a plain, unqualified rule — no `:has()`, no per-construct descendant selector — exactly the same shape as `.tok-strike`'s own rule, verified harmless and correct when both land on the same element (`class="tok-strike cm-task-completed"`; text-decoration is idempotent, doesn't double-render). Deliberately drops the old rule's `color: var(--foreground-tertiary)` dimming for plain task text — not required for correct rendering, and re-adding it would need a deliberate specificity decision against `.tok-wikilink`/`.tok-tag`/`.tok-date`'s own `color` (both single-class selectors on the same element, resolved by source order, not overridden by inheritance the way it was under the old ancestor-only rule) — left as an open follow-up if the dimmed appearance is wanted back.
- **Future inline constructs** (a bare-URL widget, mentions, or any other inline widget) must participate in `collectActiveInlineClasses`'s tree-derived composition for their own node, threading the result through that widget's constructor the same way — but must *not* additionally read task/editor-state (`isNodeOnCompletedTask` or similar) and compose it directly onto their own root; per the 2026-09-13 reversal above, any occurrence-level editor/task-state class that also needs an ancestor content mark (for plain text) belongs solely on that ancestor mark, never duplicated onto a nested widget inside it.

---

## Fenced code — native `codeLanguages` nested parsing and highlighting (2026-09-10)

- **Native CM6/`@lezer/markdown` mechanism confirmed and adopted — no custom parser, no custom highlighting engine.** *Locked.* `@lezer/markdown`'s `FencedCode` node already carries `CodeMark`/`CodeInfo`/`CodeText` children; `parseCode({codeParser, htmlParser})` (its own exported nested-parsing extension) mounts a language's parser onto `CodeText` as an overlay, keyed off `CodeInfo`'s text. `@codemirror/lang-markdown`'s `markdown({codeLanguages, defaultCodeLanguage})` option builds exactly this `codeParser` from a `LanguageDescription[]` (via `LanguageDescription.matchLanguageName`) and wires `parseCode` internally — confirmed by reading both packages' installed source directly (`@lezer/markdown@1.7.2`, `@codemirror/lang-markdown@6.5.2`), not assumed. `markdownLanguage.ts` now passes `codeLanguages: fencedCodeLanguageDescriptions` into the existing `markdown({...})` call — the only change to that call site.
- **Registered language set — a small, fixed, curated list, not a dynamic registry.** *Locked, for now.* `fencedCode/fencedCodeLanguages.ts`: JavaScript/JSX (`js`/`jsx`/`mjs`/`cjs` aliases), TypeScript/TSX (`ts`/`tsx`), JSON, CSS, HTML (`htm`), Python (`py`) — `@codemirror/lang-javascript`, `-json`, `-css`, `-html`, `-python` added as **direct** dependencies of `apps/app` (previously only `lang-html`/`lang-javascript` existed transitively, pulled in by `lang-markdown` itself for embedded-HTML support, never used for fenced code). No product requirement named a specific set; this list was a deliberate, disclosed judgment call, not derived from an existing spec.
- **Each `LanguageDescription` is built via `support` (eager), not `load` (lazy).** *Locked, with an explicit tradeoff recorded, not a silent shortcut.* `LanguageDescription`'s lazy `load()` path exists specifically for dynamic-import code-splitting, but the concrete `Language` object it eventually resolves to is exactly what the highlighting-scoping mechanism below needs synchronously at editor-setup time — reconciling true laziness with scoped highlighting would need a load-then-reconfigure orchestration step (a `Compartment` reconfigured once each dynamic import resolves) with no existing precedent in this codebase, for a fixed five-language set that isn't large or user-configurable. Eager `support` was judged the smaller, more native-feeling change for this first pass; revisit if this list grows into something that should be paginated or user-configurable, or if initial-bundle size becomes an actual, measured problem.
- **Highlighting is scoped per nested language — never a global `syntaxHighlighting()`.** *Locked, and the crux of this decision.* `@lezer/markdown`'s base parser bakes its own highlighting tags (`heading1`-`6`, `emphasis`, `strong`, `strikethrough`, `monospace`, ...) into every Markdown document's tree unconditionally — confirmed directly in `@lezer/markdown`'s own source (`markdownHighlighting = styleTags({...})`, applied to the base `parser`'s `NodeSet` at module load, not opt-in). A plain, unscoped `syntaxHighlighting(defaultHighlightStyle)` would therefore recolor Markdown syntax that already has a dedicated Live Preview decoration owner — exactly the duplicate-ownership hazard `createEditorView.ts`'s doc comment records `markdownHighlighting()` (the old Clutter extension of the same name) being retired for. `HighlightStyle`'s own `scope` option (a concrete `Language` instance; confirmed against `@codemirror/language`'s installed source, not just its `.d.ts`) restricts a highlighter to node types belonging to that language's own tree, never the outer Markdown tree it's mounted inside — `fencedCode/fencedCodeHighlighting.ts` builds one `HighlightStyle.define(defaultHighlightStyle.specs, {scope: language})` per registered language (reusing `defaultHighlightStyle`'s own tag→class specs rather than inventing new colors, per the "recommended, don't hand-roll" pass of this decision), wired in `buildEditorExtensions.ts` alongside `markdownLanguageExtension()`.
- **Verified the overlay-mount mechanics directly, not assumed from documentation.** The nested tree `parseCode`/`parseMixed` produces is attached via `NodeProp.mounted` as an *overlay* on the enclosing `FencedCode` node — confirmed by reading `@lezer/common`'s `MixedParse.advance()` source directly. A plain top-down `Tree.iterate()` does **not** descend into it (confirmed: `TreeNode.nextChild`'s mounted-tree handling only substitutes for the non-overlay case) — position-resolving calls (`resolveInner`, `NodeType`-scoped `enter()`) do enter it, which is exactly the mechanism `@lezer/highlight`'s own `highlightTree`/`HighlightBuilder` uses internally to switch highlighters at a mount boundary (confirmed by reading its source: it checks `cursor.tree.prop(NodeProp.mounted)` and `mounted.overlay` explicitly). `fencedCodeLanguages.test.ts` resolves at a concrete position inside the fenced body rather than asserting nested node names show up in a flat tree walk, for this reason.
- **Verified live in the real Clutter webapp** (browser pane, Chromium engine, not just the parser-level test suite): a `` ```js `` fence with `const`/`function`/`return`/string-literal content rendered with distinct token colors (keywords, string, identifiers) purely from this mechanism; a `` ```py `` fence in the same document highlighted independently and correctly; a heading, a bullet list, and a `[[WikiLink]]` elsewhere in the same document rendered through their existing, unaffected Live Preview decorations, with zero visual bleed from the scoped highlighters. No console errors. Full existing suite (`apps/app`: 292 files / 4768 tests) passed unmodified alongside the 12 new tests for this change.
- **Explicitly out of scope for this pass, deliberately deferred, not overlooked**: concealing the ```` ``` ```` fence/info-string at rest, a visible language label, a container/background, a copy button, and any bespoke engagement/keymap behavior for fenced code. This phase proves the native parsing+highlighting pipeline only; presentation is a separate, later layer over the same tree, following this codebase's existing reveal-on-engagement pattern (see the "Concealed markers..." entries above) if and when it's built.

## Fenced code — fence concealment via the shared `liveMarkDecoration` mechanism (2026-09-10, same day)

- **Built directly on `headingMarkerDecoration.ts`'s own mechanism (`liveMarkDecoration`) — no new decoration architecture, no widget.** *Locked.* `highlight/fencedCodeMarkerDecoration.ts` conceals a `FencedCode` node's own `CodeMark` children (open + close fence, backtick or tilde — both parse to the same node names, confirmed against `@lezer/markdown`'s `isFencedCode`, so no fence-character-specific logic was needed) the exact same way heading conceals `HeaderMark`: `Decoration.replace({})`, no widget, since collapsing a range to nothing needs none. `CodeInfo` (the language string) and `CodeText` (the code body) are never part of this decoration's mark ranges, mirroring heading's own "hide only the marker, leave the inline content alone" split.
- **A requested `opacity: 0`/CSS-only concealment was rejected in favor of the existing `Decoration.replace()` mechanism — surfaced explicitly, not silently substituted.** The task that produced this decision initially specified CSS opacity for concealment. `liveMarkDecoration.ts`'s own doc comment already documents, with a direct browser reproduction, why every CSS-based hide (`display:none`, and by the same mechanism `opacity:0`/`color:transparent`) leaves a real, live text node in the DOM that still participates in native click hit-testing and double-click word-selection — the exact bug class heading and every inline marker in this codebase already paid to fix once. Blockquote's `QuoteMark` is the one documented exception to `Decoration.replace()`, and it keeps real CSS-concealed text (`color: transparent`, not `opacity`) for a reason fenced code doesn't share: a reserved-width gutter column. Fenced code has no equivalent need, so `Decoration.replace()` — not blockquote's exception — is the correct template.
- **Engagement mode is `'node-range'` (selection contained by the whole `FencedCode` span), not `'physical-line'`** — the one place this construct's engagement genuinely differs from heading/blockquote's own default. A fence's own marker line can be many lines away from where the caret actually sits once real code content exists between the two fences; the product requirement ("caret anywhere inside the code content reveals both fences together") needs whole-node containment (`isTokenEngaged`, the same query WikiLink/Tag/Date already use), not proximity to the marker's own line. Verified directly: a caret placed on the opening fence's own line, on the closing fence's own line, and several lines deep inside the code body all reveal both fences together; a selection merely touching the block from outside (anchored before it, extending partway in) does not reveal it — `isTokenEngaged`'s containment (not overlap) semantics, inherited for free.
- **Verified live in the real Clutter webapp** (browser pane, Chromium engine): a `` ```js `` fence concealed to bare `js` + highlighted code at rest, revealed `` ```js `` and the closing `` ``` `` together the instant the caret entered the code content, and re-concealed the instant it left; a `~~~py` fence behaved identically; `def` stayed correctly highlighted (Python scope) throughout both states, confirming this decoration and `fencedCodeHighlighting.ts` compose without interference. Inspected the live DOM directly: `<div class="cm-line"><span class="cm-marker cm-fenced-code-marker">\`\`\`</span>js</div>` — a bare `.cm-line`/`.cm-marker` span over real source text, the same shape every other marker in this editor already produces, no new wrapper element anywhere. No console errors.
- **Tests** (`highlight/fencedCodeMarkerDecoration.test.ts`, mirroring `headingMarkerDecoration.test.ts`'s mounted-`EditorView` style): at-rest concealment, reveal from inside the code body, reveal from each fence's own line, re-collapse on leaving, containment-vs-overlap, tilde fences, an unclosed-at-EOF fence (guards against double-concealing a single real `CodeMark` as both "open" and "close" when no closing line was ever matched), `CodeInfo` never concealed, no-info-string fences, and a DOM-shape assertion that every `.cm-fenced-code-marker` is a plain `<span>` over real text. Full existing suite (293 files / 4779 tests) passed unmodified alongside the 11 new tests.

## Fenced code — container styling, friendly language label, and Copy button (2026-09-10, third pass same day)

- **Container background/border/rounded-corners reuses `blockquoteLineDecoration.ts`'s own corner-rounding technique verbatim — one flat line class, not a begin/middle/end triple.** *Locked.* `highlight/fencedCodeBlockLineDecoration.ts` applies a single `Decoration.line({class: 'cm-code-block-line'})` to every physical line a `FencedCode` node owns (open fence line, every content line including genuinely blank interior ones, close fence line) — line ownership determined the same way `blockquoteLineDecoration.ts` already does (probe each visible line's first non-whitespace position, ask the tree for the nearest owning ancestor), not by walking the node's own `[from, to)` range directly. The task that requested this named a `cm-code-block-begin`/`cm-code-block`/`cm-code-block-end` three-class scheme; that was deliberately not what got built — `.cm-content` already renders `.cm-line` elements as flat, direct document-order siblings, so CSS sibling combinators alone (`.cm-code-block-line + .cm-code-block-line` zeroes top border/radius for "has a preceding line," `:has(+ .cm-code-block-line)` zeroes bottom border/radius for "has a following line") derive first/middle/last from plain DOM adjacency, with zero JS-side position tracking — the literal "existing equivalent convention" the task asked to be found and reused, not a new one invented to match its own draft naming.
- **The friendly language label and the Copy button are each their own decoration, neither folded into `fencedCodeMarkerDecoration.ts`** — different engagement/lifetime shapes (the label reveal-on-engages with the block; the Copy button is unconditional, persistent chrome, like the container itself), so combining them into one file would have coupled two independently-varying concerns for no shared benefit, contrary to `docs/implementation-rules.md` §2's "never add a facade method whose entire body is an unconditional forward" spirit applied to decorations: each stays independently testable and independently removable.
- **The language label reuses the semantic-token family's "atomic widget at rest, plain text engaged" shape (`widgetReplaceRenderer` in `inlineLivePreviewParticipants.ts`, used by Tag/Date) — but is deliberately NOT registered through that shared participant map.** *Locked.* That map's engagement is hardwired to the decorated node's *own* range (`isTokenEngaged` on the same node passed to the renderer) — confirmed by reading `inlineLivePreviewRegion.ts`'s own doc comment, which records heading content needing exactly this same escape hatch for exactly this same reason (heading's engagement scope is line/marker-based, not "this inline span's own range"). `CodeInfo`'s desired engagement scope is its *enclosing* `FencedCode` node — reveal must happen the instant the caret is anywhere in the code body, not only when it's inside `CodeInfo`'s own few characters — the identical mismatch. `highlight/fencedCodeLanguageLabelDecoration.ts` is therefore a small, standalone `ViewPlugin` (not a participant registration) that calls the *same* `isTokenEngaged(FencedCode)` query `fencedCodeMarkerDecoration.ts` already uses, so the label and the fence markers reveal/conceal in exact lockstep by construction — never observed to desync, because there is only one engagement query, not two independently-computed ones.
- **Friendly names (`js` → `JavaScript`) resolve via `LanguageDescription.matchLanguageName` — the exact same lookup `@codemirror/lang-markdown`'s own `getCodeParser` uses internally to resolve `codeLanguages`** (confirmed against its installed source, already cited in the earlier fenced-code entry above) — `fencedCode/fencedCodeLanguageLabel.ts` never re-derives its own alias table; the only new data is a small canonical-name → display-string map (`javascript` → `JavaScript`, etc.), since `LanguageDescription` has no display-name field of its own (confirmed against `@codemirror/language`'s installed types). An unrecognized language (edge case named in the task) falls back to the raw, as-typed info text rather than `null` or a placeholder — "unknown language fails gracefully," per the task's own requirement, showing what the user actually wrote rather than nothing.
- **The Copy button is inserted (`Decoration.widget({widget, side: 1})`), never a replacement — the same insertion mechanism `imageLivePreview.ts`/`embedLivePreview.ts` already use for auxiliary UI attached to real content** — positioned at `CodeInfo.to` (or the opening `CodeMark`'s own end, absent an info string) on the block's first physical line, then pulled into the container's visual top-right corner via `position: absolute` scoped to that line's own `position: relative` — the same escape-hatch technique `blockquoteLineDecoration.ts`'s own `::before` bar already uses to render as a block-spanning visual element with no wrapper anywhere. Renders unconditionally, never routed through `isTokenEngaged` — persistent block chrome, the same category as the container's own background/border.
- **The button itself reuses `NoteEmbedWidget.ts`'s own interactive-widget shape exactly**: a real `<button>`, `mousedown`+`click` both `preventDefault()`+`stopPropagation()` so a click never moves the caret, touches the selection, or otherwise reaches editor state, `ignoreEvent()` returns `false` (the button's own listeners already fully own its events; nothing needs CM6's default handling suppressed). `.cm-media-control` (`mediaPresentation/MediaFloatingControls.css`) supplies the icon-button chrome unmodified — the same visual system Image/PDF/note-embed controls already use, not a fenced-code-specific button style.
- **First use of `navigator.clipboard` anywhere in this codebase** — confirmed by search before writing it, per "investigate the cleanest existing clipboard API/pattern already used in the app; do not build a custom abstraction if one already exists" (none existed to reuse). `writeText`'s rejection path is explicitly handled (`.then(onFulfilled, onRejected)`, the `onRejected` branch a deliberate no-op) rather than left as an unhandled promise rejection — confirmed necessary, not theoretical, by a real `NotAllowedError` surfacing in this session's own sandboxed browser pane (clipboard write permission denied there, a known limitation this doc's earlier entries already hit for clipboard *reads*) — the fix was verified to actually suppress the console error on a subsequent click after reload, not merely written and assumed correct.
- **The clipboard payload is re-resolved from a stable anchor position (`fencedCodeFrom`) at click time, never a captured `CodeText` range — a real staleness bug caught by this session's own tests, not merely reasoned about.** `FencedCodeCopyButtonWidget.eq()` compares only `fencedCodeFrom` (so CM6 reuses the existing DOM node and its closure across a per-keystroke decoration rebuild whenever the block's own start hasn't moved — avoiding an unnecessary click-target flash back to the plain icon on every keystroke). The first implementation instead had `getCode`'s closure capture `CodeText`'s `{from, to}` directly at construction time; a test that edits the code body *after* the widget is first rendered, then clicks Copy, caught that this captured range goes stale the moment the body grows/shrinks without the block's own start moving — the overwhelmingly common edit pattern (typing more code). Fixed by re-resolving the enclosing `FencedCode` fresh from `fencedCodeFrom` at click time (walk up from a resolved position to the node whose own `.from` still matches) and reading its *current* `CodeText` child then, rather than trusting anything computed at an earlier point in time. `fencedCodeCopyButtonDecoration.test.ts`'s "reads the current content, not stale content, after the code body is edited" case is the regression test for exactly this.
- **Verified live in the real Clutter webapp** (browser pane, Chromium engine): a `js` fence rendered a continuous rounded container (background/border/corners) with correct syntax highlighting inside it, a "JAVASCRIPT" label and Copy icon both visible at rest; engaging the block reverted the label to raw `js` text and revealed the fences, exactly matching `fencedCodeMarkerDecoration.ts`'s own existing behavior; an unrecognized `rust` info string rendered as an uppercase "RUST" fallback label (CSS `text-transform`, not a second display-string path); an empty fenced block rendered its own container and Copy button with no error; clicking Copy on both a populated and an empty block correctly invoked `navigator.clipboard.writeText` (visible as the expected `NotAllowedError` in this sandboxed pane, confirming the real code path ran) with zero unhandled-rejection console errors after the fix above; two separate fenced blocks in the same document each got their own independent container/label/button with no cross-talk. DOM inspected directly: no wrapper element around any block, `.cm-line`/`.cm-marker`/`.cm-media-control` are the only element shapes involved, consistent with every other construct in this editor.
- **Tests**: `fencedCodeBlockLineDecoration.test.ts` (line-class coverage: multi-line, blank interior line, single-line body, two independent blocks, unclosed-at-EOF), `fencedCodeLanguageLabelDecoration.test.ts` (friendly-name-at-rest, raw-text-when-engaged, re-collapse, unknown-language fallback, no-info-string renders no label, a second language), `fencedCode/fencedCodeLanguageLabel.test.ts` (the lookup function in isolation: canonical names, aliases, case-insensitivity, whitespace-suffix stripping matching `codeLanguages`' own normalization, unrecognized-language fallback, empty-string → `null`), `fencedCodeCopyButtonDecoration.test.ts` (one button per block, multiple independent blocks, exact-content copy with newlines preserved, empty-block copy, the stale-content regression above, no caret/selection/document side effects from clicking, tilde fences, unclosed-fence-while-typing). Full existing suite (297 files / 4803 tests) passed unmodified alongside the 24 new tests.

## Fenced code — per-block independence, right-aligned header, and registry-owned canonical labels (2026-09-10, fourth pass same day)

- **The one-flat-class-plus-CSS-sibling-adjacency corner-rounding technique (adopted from `blockquoteLineDecoration.ts` in the third pass) was wrong for fenced code, confirmed by direct reproduction — fixed by deriving first/last-line identity from each `FencedCode` node's own boundary positions instead of DOM adjacency.** *Locked, supersedes the third pass's `fencedCodeBlockLineDecoration.ts` design.* Blockquote's own use of `.cm-quote-line + .cm-quote-line` / `:has(+ ...)` is correct because DOM adjacency between two `.cm-quote-line` elements *always* means "the same quote" for that construct. That assumption does not transfer to fenced code: two independent, back-to-back `FencedCode` blocks with **no blank line between them** are legal CommonMark (a closing fence immediately followed by an opening fence needs no separator) and produce two adjacent same-class `.cm-code-block-line` elements that are *not* the same block — reproduced directly (three adjacent fenced blocks rendering as one merged eight-line container) before being accepted as a real bug, not assumed from the request alone. Fix: every owned line still carries the shared `cm-code-block-line` class, but the line containing its *owning node's own* `.from` additionally gets `cm-code-block-line--first`, and the line containing that *same node's* `.to` gets `cm-code-block-line--last` — computed once per line from `nearestFencedCode(...)`'s returned node, never from a neighboring line's class. A single-line-body block's one line correctly carries both modifiers at once (the two CSS rules touch disjoint edges, so no special-casing is needed for the overlap). `fencedCodeBlockLineDecoration.test.ts` gained a dedicated regression test for the exact adjacency failure mode (three back-to-back blocks, zero blank lines, asserting each gets its own independent `--first`/`--last` pair) — the shape of test the third pass's original suite didn't have, since its "two separate blocks" case always left a blank line between them and never actually exercised the bug.
- **The friendly language label moved from its natural in-flow position (wherever `CodeInfo` sits — hard against the opening fence, on the left) to `position: absolute`, right-aligned immediately left of the Copy button.** *Locked.* No shared wrapper/flex container was introduced for the pair — the label and button remain two independent decorations (per the third pass's own rationale for keeping them that way, unchanged here), and the label's CSS offset (`right: calc(var(--height-sm) + var(--space-8))`) is computed against `.cm-media-control`'s own fixed, already-tokenized width rather than anything requiring runtime measurement or a shared layout container. This is a pure CSS/positioning change — neither decoration's engagement logic, DOM node shape, or construction moved.
- **Canonical display names now come directly from `LanguageDescription.name` — the second `canonical name → display string` lookup table introduced in the third pass was removed, not merely deprecated.** *Locked, corrects the third pass.* `fencedCode/fencedCodeLanguages.ts`'s own `name` fields changed from lowercase ids (`javascript`, `typescript`, `json`, `css`, `html`, `python`) to their canonical display casing (`JavaScript`, `TypeScript`, `JSON`, `CSS`, `HTML`, `Python`) — safe for `codeLanguages` matching because `LanguageDescription.of`'s own constructor lowercases `name` when building its internal alias list (confirmed against the installed `@codemirror/language` source), so a typed `js`/`javascript` info string still matches identically; only the *value* a successful match returns changed, from the lowercase id to the display form. `fencedCode/fencedCodeLanguageLabel.ts` simplified to `matched.name` directly — the `FRIENDLY_NAME_BY_CANONICAL_NAME` map it previously carried is gone entirely, per the explicit instruction that the registry introduced for `codeLanguages` should be the single source of truth for both parsing-time matching and display, not a second table shadowing it.
- **JSX and TSX display as `JavaScript`/`TypeScript` respectively — a fact about this registry's existing shape, determined and documented rather than assumed.** *Locked.* Neither has its own `LanguageDescription` entry: `jsx` is registered as an alias of the `JavaScript` entry (parsed via `javascript({jsx: true})`), `tsx` as an alias of `TypeScript` (`javascript({jsx: true, typescript: true})`) — this was already true before this pass, just not previously stated as the answer to "what does JSX display as." Introducing distinct `JSX`/`TSX` `LanguageDescription` entries later would be a legitimate registry change (a real product decision about whether JSX/TSX deserve their own identity, not merely a parser variant of JS/TS), not something the label function should special-case around this registry's current structure.
- **Verified live in the real Clutter webapp** (browser pane, Chromium engine): three fenced blocks (`` ```js ``, `` ```html ``, `` ```py ``) typed back-to-back with **zero blank lines** between any of them rendered as three visually distinct cards, each with its own independent top/bottom border and rounded corners — confirmed via direct DOM inspection (`Array.from(document.querySelectorAll('.cm-line')).map(l => l.className)` showed three independent `--first`/plain/`--last` triples, not one continuous run.) Each card's header showed its label immediately left of the Copy icon, both right-aligned (`JavaScript`/`HTML`/`Python`, not `js`/`html`/`py`). Engaging the first block correctly reverted its label to raw `` ```js `` text and revealed both fences while the other two cards remained fully unaffected (still concealed, still independently bordered) — engagement/reveal behavior confirmed unchanged. Clicking Copy produced zero console errors (the third pass's rejection-handling fix still holds). No wrapper element was introduced anywhere; `.cm-line`/`.cm-marker`/`.cm-media-control` remain the only element shapes involved.
- **Tests**: `fencedCodeBlockLineDecoration.test.ts` gained four new cases (multi-line block's exact first/middle/last assignment, single-line-body block's both-at-once assignment, the three-adjacent-blocks-zero-blank-lines regression, and a class-list-level double-check that no line is spuriously marked both `--last` and the following block's `--first`); `fencedCodeHighlighting.test.ts` updated for the new canonical casing (`d.name === 'JavaScript'`/`'Python'`, was `'javascript'`/`'python'`) with no change to what it actually verifies. `fencedCodeLanguageLabel.test.ts`'s own black-box expectations (`js` → `JavaScript`, etc.) were already written against the *output*, not the registry's internal casing, so needed no changes at all — confirming the refactor was genuinely internal. Full existing suite (297 files / 4807 tests) passed unmodified alongside the 4 new tests.

## Fenced code — container migrated to `EditorView.blockWrappers` (2026-09-10, fifth pass same day)

- **`fencedCodeBlockLineDecoration.ts`'s entire `Decoration.line`-based container (background/border/corners directly on `.cm-line`) is superseded, not refined, by `fencedCodeBlockWrapper.ts` — a native `EditorView.blockWrappers` provider.** *Locked.* This followed a dedicated architecture investigation (prompted by the margin-on-`.cm-line` selection bug two passes earlier) that established a hard rule: `.cm-line` is CodeMirror-owned layout/selection infrastructure and must never carry fenced-code-specific classes or container styling of any kind — not just margin, everything. `EditorView.blockWrappers` (confirmed against the installed `@codemirror/view@6.43.9` source and changelog — introduced `6.39.0`, nesting-rank support added `6.43.0`) is CM6's own native mechanism for exactly this need: a `BlockWrapper.create({tagName, attributes}).range(from, to)` makes CM6 itself render every `.cm-line` (and block widget) starting inside that range as the actual DOM children of a real wrapper element it builds during the same tile-construction pass that builds `.cm-line`s — not a decoration, not a CSS trick, a genuine parent CM6 creates and owns. `.cm-code-block` (the wrapper) now carries 100% of the container's visual presentation (`margin-block`, `background`, `border`, `border-radius`); `.cm-line` carries zero fenced-code classes, zero inline styles, zero layout-affecting CSS from this feature — confirmed directly via DOM inspection, both in a mounted jsdom `EditorView` (`fencedCodeBlockWrapper.test.ts`) and the real webapp.
- **No `ViewPlugin` needed — confirmed directly against the installed source, not assumed from the facet's type signature alone.** `EditorView.blockWrappers`'s facet input accepts either a `RangeSet<BlockWrapper>` or a `(view: EditorView) => RangeSet<BlockWrapper>` function; `DocView.updateDeco()` (which runs from both the constructor and `update()` — i.e. every relevant view update) evaluates that function form itself (`state.facet(blockWrappers).map(v => typeof v == "function" ? v(this.view) : v)`). Providing `EditorView.blockWrappers.of((view) => …)` directly is therefore sufficient; CM6 re-invokes it exactly when needed, the same recomputation guarantee a `ViewPlugin`'s own `update()` would otherwise reimplement by hand.
- **Two independent, back-to-back `FencedCode` blocks (no blank line between them) now render as two genuinely separate `.cm-code-block` elements with zero adjacency logic of any kind** — a `BlockWrapper`'s own `[from, to)` range (one per node) is what CM6 uses to decide where one wrapper ends and the next begins, so the entire class of bug the superseded `--first`/`--last`-derived-from-DOM-adjacency version needed a dedicated fix for (two prior passes ago) cannot occur here structurally, not just by careful implementation.
- **Language label and Copy button decorations are unchanged in mechanism, only in what they position relative to** — both still insert/replace via `Decoration.widget`/`Decoration.replace` at document positions inside the first line, exactly as before; only the CSS `position: relative` anchor moved from `.cm-code-block-line` to `.cm-code-block` (the wrapper), which is visually identical since the wrapper's own top edge coincides with the first line's top edge either way. Fence-marker conceal/reveal (`fencedCodeMarkerDecoration.ts`) is completely untouched — it operates on ranges inside the wrapped content and has no dependency on the container mechanism.
- **Verified live in the real Clutter webapp**: three fenced blocks (`` ```js ``, `` ```python ``, `` ```css ``) typed back-to-back rendered as three independent cards with real gaps; DOM inspection confirmed the exact requested structure (`<div class="cm-code-block"><div class="cm-line">…` with zero `.cm-code-block` class or inline style on any `.cm-line`); syntax highlighting, labels, and Copy buttons all correct; clicking each block engaged/disengaged it correctly and independently of the other two; normal in-block editing (typing, undo) worked correctly.
- **A real, narrow, pre-existing selection bug was found during this verification, unrelated to the `blockWrappers` migration** — not introduced by it, not fixed by it, and out of this pass's scope per the task's own "do not redesign those features" instruction: a mouse drag whose *release point* lands near a block's closing fence over-extends the resulting selection to consume the closing `` ``` `` marker, deleting more than was visually dragged. Isolated precisely: a drag confined to the *middle* of a 9-line block (both endpoints far from either fence) selects and deletes exactly the dragged text, correctly, every time; a drag ending anywhere near the closing fence — even when the block was already stably engaged, ruling out reveal-triggered reflow as the cause — consistently over-extends to include the closing marker. This points at `fencedCodeMarkerDecoration.ts`'s reuse of `liveMarkSelectionSnap.ts`'s `'node-range'`-mode snap-to-boundary logic (`highlight/liveMarkSelectionSnap.ts`, scoped to `select.pointer` transactions) as the likely mechanism, not at anything this pass touched. Flagged for a separate, dedicated investigation rather than fixed opportunistically here.
- **Tests**: `fencedCodeBlockWrapper.test.ts` (new, replacing `fencedCodeBlockLineDecoration.test.ts`) — wraps every line of a block in a real `.cm-code-block` parent; zero fenced-code classes or inline styles ever land on `.cm-line`; two back-to-back blocks get two independent wrappers (3+3 lines, not 6 merged); three blocks separated by blank lines each get their own wrapper; plain text outside a block is never inside a wrapper; an unclosed fence and an empty fenced block both still wrap correctly without throwing; tilde fences wrap the same as backtick fences. Full existing suite (297 files / 4806 tests) passed unmodified.

## Fenced code — `margin` banned from the entire layout path, not just `.cm-line`; visual card restored to `Decoration.line` (2026-09-10, sixth pass same day)

- **Direct interactive testing overturned half of the previous pass's theory: `margin` broke CM6 cursor/navigation even when applied to `.cm-code-block` (the wrapper), not only when applied directly to `.cm-line`.** *Locked, corrects the fifth pass.* The fifth pass moved the entire visual card (background/border/radius/margin) onto the `blockWrappers`-created `.cm-code-block` element, reasoning that a wrapper CM6 doesn't measure for line-geometry purposes should be free of the risk margin posed on `.cm-line` itself. Real testing found that reasoning incomplete: `margin` specifically — not `Decoration.line`, not background/border/radius, not even `padding` — is what corrupts CM6's cursor/navigation bookkeeping, and it does so regardless of which element in the fenced-code layout path carries it. The corrected, narrower hard rule: **no `margin` anywhere in the fenced-code layout path, on any element** — `.cm-code-block` included. `padding`, tested directly on both the wrapper and the line, does not reproduce the problem on either.
- **Responsibility split, restated precisely**: `.cm-code-block` (the `blockWrappers` structural parent) owns grouping and inter-block spacing only — `padding-block` (real, uncollapsed — unlike margin, sibling padding never collapses, so two adjacent wrappers' own `--space-12` sums to a real visible gap), never background/border/radius/margin. `.cm-code-block-line` (`Decoration.line` classes on `.cm-line`, reinstated from `highlight/fencedCodeBlockLineDecoration.ts`) owns the visual card — background, left/right border, `--first`/`--last`-gated top/bottom border and radius, horizontal `padding-inline`. Decorating `.cm-line` was never actually the architectural problem the investigation two passes ago worried about (blockquote/table/horizontal-rule all do it safely, still true) — only `margin`, on either element, was.
- **The `--first`/`--last` computation is now provably simpler than its original, pre-wrapper incarnation, not just relocated**: because `fencedCodeBlockWrapper.ts`'s `blockWrappers` grouping already keeps two independent, back-to-back `FencedCode` blocks in two separate DOM subtrees *before* the line decoration runs, `--first`/`--last` only need to place the correct border/radius within one already-correctly-grouped block — they no longer also have to prevent two different blocks' lines from reading as one merged run, which the original (pre-`blockWrappers`) version of this same file had to solve for itself. Same algorithm, smaller job.
- **Language label and Copy button re-anchor to the owning `.cm-code-block-line` (via its own `position: relative`), not `.cm-code-block`** — visually identical either way, since both widgets are only ever inserted on the first line and that line's own top edge coincides with the wrapper's top edge regardless of which element the CSS anchors to; the change is purely about keeping the anchor on the element that actually owns the visual box being anchored against.
- **Verified live in the real Clutter webapp, confirmed directly by the user**: three fenced blocks (`` ```js ``, `` ```python ``, `` ```css ``) rendered as three independent cards with real gaps; DOM inspection confirmed the exact responsibility split (`.cm-code-block` — no visual class, `padding: 12px 0px`, `margin: 0px`; `.cm-code-block-line` — `margin: 0px`, real padding/background/border-radius); clicking each block engaged/disengaged it correctly; editing inside the second block worked correctly; syntax highlighting, label, and Copy button all correct.
- **Tests**: `fencedCodeBlockWrapper.test.ts` updated — the "never attaches classes to `.cm-line`" case (no longer true by design) replaced with a check that the wrapper itself never carries a line-level visual class and never carries an inline style. New `fencedCodeBlockLineDecoration.test.ts` (mirroring the pre-`blockWrappers` version's shape, mounted together with `fencedCodeBlockWrapper()`): correct `--first`/middle/`--last` assignment across a multi-line block, single-line-body block carrying both modifiers, two back-to-back blocks each getting their own correct pair, every visually-carded line confirmed to be a real DOM child of its own `.cm-code-block` (not a sibling), a genuinely blank interior line staying part of the card, and an unclosed fence not throwing. Full existing suite (298 files / 4812 tests) passed unmodified alongside the 6 new/updated tests.

## Fenced code — selection-vs-card-background contrast, CSS-only transparency (2026-09-10, seventh pass same day)

- **Root cause investigated at the CM6 source level before any fix, per explicit instruction: no guessed `z-index`.** CM6's own `drawSelection()` renders the selection highlight through the public `layer()`/`LayerView` mechanism, the same one `.cm-cursorLayer` uses. `LayerView.setOrder`'s `zIndex = (above ? 150 : -1) - pos` formula (`above: false` for the selection layer) puts `.cm-selectionLayer` at a **deliberately negative** z-index — selection is designed to paint *behind* ordinary static content, not in front of it, so an opaque `.cm-code-block-line` background (itself ordinary static content, no z-index of its own) legitimately painted over it. This is expected CM6 behavior surfacing on the first opaque-background block-level decoration in this codebase, not a CM6 bug or a regression from any prior pass.
- **Fixed via the two-step plan the user set going in: try a CSS-only transparency experiment first, escalate to a native `layer()`-based fix only if that's insufficient.** *Locked, dark theme; accepted-with-known-limitation, light theme — see below.* `.cm-code-block-line`'s background changed from opaque to `color-mix(in srgb, var(--surface-secondary) 55%, transparent)`, directly precedented by CM6's own `highlightActiveLine()` (`.cm-activeLine`), which already uses an 8-digit alpha-channel color for exactly this reason — coexisting with the negative-z-index selection layer is a known, intended pattern for CM6-adjacent block decorations, not a novel workaround.
- **Light theme needed a second, deliberately constrained pass**: `--surface-secondary` resolves identically to `--surface-root` in light theme (confirmed via `getComputedStyle` — both `#f9f8f7`/`neutral-50`), so the dark-theme fix produced a resting-state card with effectively zero contrast against the page background in light mode. Per explicit instruction (existing token only, no new token invented, stop and report honestly rather than tweak indefinitely if still not clearly good), light theme was switched to `color-mix(in srgb, var(--surface-tertiary) 55%, transparent)` instead. Result, reported as-is: resting-state contrast improved significantly; selection-on-card contrast improved but remained **marginal**, not "clearly good" — left as a known, disclosed limitation rather than iterated further.
- **The native `layer()` approach (rendering the code-block background itself through `layer()`, ordered above the selection layer) remains the documented fallback** if full-fidelity light-theme selection contrast is wanted later; not built, since the CSS-only fix was accepted as "close to zero architectural complexity" per the user's own framing of the two-step plan.
- **Scope discipline**: no CM6 selection rendering changed, no `z-index` added anywhere, no new decoration/extension — this pass is a background-color value change on the existing `Decoration.line` class from the prior pass, confined to `MarkdownEditor.css`.
- **Committed** as a single commit bundling the transparency change for both themes.

## Fenced code — language label reverted to left/in-flow position, Copy button hover-reveal (2026-09-10, eighth pass same day)

- **Placement-and-visibility correction only, per explicit instruction to modify the smallest existing implementation necessary — no new DOM structure, widget, decoration, or container.** Both changes are CSS-only, confined to `MarkdownEditor.css`; `fencedCodeLanguageLabelDecoration.ts` and `fencedCodeCopyButtonDecoration.ts`/`FencedCodeCopyButtonWidget.ts` are untouched — inspected first and confirmed the label widget already replaces `CodeInfo` at its own real document position (just after the concealed opening fence, where the user originally typed the language), so nothing about *where the widget is inserted* needed to change, only how it's styled.
- **Language label**: reverted from `position: absolute` (top-right, offset to sit beside the Copy button — the prior pass's placement) back to normal inline flow at its own document position — left-aligned, no `position`, no `top`/`right` offset, no `display: flex`/fixed `height`. Still resolves full canonical names (`js` → `JavaScript`, `py` → `Python`, etc.) via the same existing `fencedCodeLanguages.ts` normalization, unchanged. Verified live with two adjacent blocks (`` ```js ``/`` ```py ``): both labels render `position: static`, left-aligned at the block's own content inset, correct full names.
- **Copy button**: hidden at rest (`opacity: 0; pointer-events: none`), revealed via `opacity: 1; pointer-events: auto` on `.cm-code-block:hover` or `.cm-code-block:focus-within` reaching the button through an ordinary CSS descendant selector — no new container needed since the existing `blockWrappers`-created `.cm-code-block` wrapper (unchanged from the fifth pass) already contains every line of the block. `:focus-within` (not just `:hover`) is defensive: it keeps the button visible for as long as focus lingers inside the block, covering any interaction path where the pointer isn't hovering but something inside the block still has focus. `pointer-events: none` at rest keeps the invisible button from intercepting clicks meant for the code beneath it, without blocking keyboard `Tab` reachability.
- **Verified live in the real Clutter webapp**: two adjacent blocks (`` ```js ``/`` ```py ``) each reveal only their own Copy button on hover — confirmed via `getComputedStyle(...).opacity` toggling `0`→`1` on exactly the hovered block's button and staying `0` on the other, in both directions. Clicking Copy still writes the block's exact content to the clipboard and returns focus to `.cm-content` (the editor's own existing behavior, unchanged by this pass) without touching document content. `tsc --noEmit` clean; full markdown editor suite (105 files / 2321 tests) passed unmodified — no test needed updating since no decoration/DOM-producing code changed, only CSS.
- **Explicitly not touched, confirmed by inspection and live testing**: syntax highlighting, fence engagement/reveal behavior, selection behavior, the seventh pass's semi-transparent-background experiment, the `blockWrappers` container architecture, and the `margin`-banned `.cm-line`/`.cm-code-block` styling rule from the sixth pass.

## Fenced code — Actions menu (Change Language, Remove) and Format button (2026-09-10, ninth pass same day; documented retroactively)

- **A fenced code block's "More actions" trigger (`FencedCodeActionsButtonWidget`/`fencedCodeActionsButtonDecoration.ts`), its menu (`FencedCodeActionsMenu.tsx`), and a separate always-visible Format button (`FencedCodeFormatButtonWidget`/`fencedCodeFormatButtonDecoration.ts`) were implemented alongside the eight passes above but never given their own entry here — this entry closes that governance gap, describing what already shipped rather than proposing new work.** *Locked.* The Actions button sits beside Copy/Format on a block's first line (same `Decoration.widget({widget, side: 1})` insertion, same position — `CodeInfo.to`, or the opening `CodeMark`'s own end absent an info string — as every other fenced-code button); its menu currently holds **Change Language** (a submenu) and **Remove**, reusing `NoteEmbedMoreActions.tsx`'s own `Overlay` + `OverflowMenuBody` shape unmodified. Copy and Format deliberately keep their own dedicated, always-visible buttons rather than moving into this menu — it is scoped to secondary/destructive actions only.
- **Change Language reuses `fencedCodeLanguageDescriptions` as its own single source of truth — no second hardcoded language list.** The submenu is built directly from `fencedCodeLanguageDescriptions.map(...)` (`FencedCodeActionsMenu.tsx`); the currently-selected entry is found via the exact same `LanguageDescription.matchLanguageName` lookup `fencedCodeLanguageLabel.ts`/`codeFormatting.ts` already use, not a second copy of that matching logic. Selecting an entry (`MarkdownEditor.tsx`'s `handleChangeFencedCodeLanguage`) re-resolves the block's `CodeInfo` range fresh via `resolveFencedCodeInfoRange` at click time and rewrites only that range to the lowercase canonical name — never the code content, never trusting a range captured when the menu opened.
- **A real bug was found and fixed here, not merely anticipated: changing the language moves `CodeInfo.to`, and `FencedCodeActionsButtonWidget.eq()` compares `fencedCodeTo` — so CM6 tears down and rebuilds the Actions button's DOM at the new position rather than migrating it in place** (confirmed live: a captured anchor's `isConnected` was `false` immediately after a language change). `FencedCodeCopyButtonWidget.eq()`, by contrast, compares only `fencedCodeFrom` and survives the same edit by being repositioned, not rebuilt — the two widgets differ in exactly this respect, not by oversight. Fixed by `resolveFencedCodeBlockWrapper` (`fencedCode/fencedCodeInfoRange.ts`), which re-resolves the enclosing `.cm-code-block` wrapper fresh via `view.domAtPos()` on every call; `MarkdownEditor.tsx`'s `setFencedCodeMenuButtonOpen` uses it instead of the previously-captured anchor element to toggle the Actions/Copy/Format buttons' `--menu-open`/`--active` state. **Regression test**: `fencedCode/fencedCodeInfoRange.test.ts` (added retroactively alongside this entry) mounts a real block wrapper + Actions + Copy decorations, edits the info string to a different length, and asserts the pre-edit Actions button is disconnected while `resolveFencedCodeBlockWrapper` still finds the live, newly-rebuilt one.
- **Format reuses Prettier (`prettier/standalone`), lazily imported, keyed off the same registry** — `fencedCode/codeFormatting.ts`'s `PRETTIER_PARSER_BY_LANGUAGE` maps each formattable `fencedCodeLanguageDescriptions` canonical `name` to its Prettier parser (JavaScript/TypeScript/JSON/CSS/HTML; Python has none — Prettier has no Python parser, and per product decision this stays unformattable rather than adding a second formatting dependency for one language). `resolveFormatterParser` shares the exact same normalization/matching `fencedCodeLanguageLabel.ts` uses, so "is this formattable" and "what does it display as" can never disagree. `formatCode` dynamically imports only the plugin(s) the resolved parser needs (`babel`+`estree` for JS/JSON, `typescript`+`estree` for TS, `postcss` for CSS, all four for HTML) — nothing Prettier-related is in the editor's eager load path. The Format button (unconditional per-language visibility via `resolveFormatterParser`, absent entirely for Python) is excluded from the note-embed's read-only extension set, the same way the Actions button's Remove/Change-Language capability is — both mutate the document and have no meaning once editing is blocked (`buildEditorExtensions.ts`).
- **Not yet done as of this entry**: `docs/editor-feature-matrix.md`'s "Fenced code block" row is being updated in the same change that adds this entry, closing the same documentation gap on that side.

## Fenced code — JSX and TSX made first-class registry identities (2026-09-10, tenth pass; a later, deliberate reversal of the fourth pass's decision above)

- **The fourth pass's "JSX and TSX intentionally have no distinct entry" decision is reversed, not merely revisited — made only after planning a much larger language catalog turned the earlier call into a real, user-visible cost.** *Locked.* Under the alias model, there was no way to select "JSX" from the Change Language menu at all, and a manually-typed `` ```jsx `` fence displayed as "JavaScript" and, if the user re-selected the very entry already checked, silently rewrote to `javascript` — discarding what they'd written. `fencedCode/fencedCodeLanguages.ts` now registers `JSX`/`TSX` as their own `LanguageDescription` entries (8 entries total, was 6); `jsx`/`tsx` were removed from `JavaScript`/`TypeScript`'s own alias lists so each alias string has exactly one owning entry — `LanguageDescription.matchLanguageName` can never resolve either ambiguously.
- **Deliberately not `@codemirror/language-data`'s shape, evaluated and rejected on a concrete, verified basis, not by default suspicion of a third-party catalog.** That catalog's plain `JavaScript` entry has no `jsx: true` — only its separate `JSX` entry does — confirmed by reading its source directly. Copying that shape would have regressed Clutter's own, more permissive existing behavior: a bare `` ```js `` block containing embedded JSX has always parsed correctly here and must keep doing so. So `JavaScript`'s and `TypeScript`'s own `support` are byte-for-byte unchanged (`jsx: true` stays on both); `JSX`/`TSX` are separate identities with the *exact same* `support` construction (`javascript({jsx:true})` / `javascript({jsx:true, typescript:true})`) — confirmed directly (`fencedCodeLanguages.test.ts`'s new cases): a plain `` ```js ``/`` ```ts `` block containing JSX still parses through real `JSXElement` nodes, unchanged.
- **Formatting needed no new Prettier plugin or `formatCode` branch** — `codeFormatting.ts`'s `PRETTIER_PARSER_BY_LANGUAGE` simply gained `JSX: 'babel'`/`TSX: 'typescript'`, reusing the exact parsers `JavaScript`/`TypeScript` already used, confirmed against Prettier's own source (`typescript-estree`, behind the `typescript` parser, parses JSX/TSX syntax unconditionally regardless of which registry entry resolved to it).
- **The Copy-visibility-style staleness class of bug this reversal could have reintroduced was checked and ruled out**: `FencedCodeActionsMenu.tsx`'s submenu and `fencedCodeLanguageLabel.ts`'s label both already read the registry generically (`.map`/`matched.name`) rather than naming languages — adding two entries required zero changes to either file's logic, only to the registry's own data and to `codeFormatting.ts`'s lookup table.
- **The re-selection inconsistency named as a defect is now fixed structurally, not by special-casing.** A block written as `` ```jsx `` now matches the `JSX` entry exactly; selecting "JSX" from Change Language when it's already checked writes `jsx` — identical to what's already there — because `JSX`'s own canonical name lowercases to exactly the fence text a user who typed `jsx` already wrote. No code path treats "re-selecting the current entry" specially; the fix is that there is no longer a mismatch between what's typed and what's canonical for JSX/TSX specifically.
- **Verified live in the real Clutter webapp** (browser pane, Chromium engine): a `` ```jsx `` fence with a JSX element rendered correct syntax highlighting (keyword/tag/attribute/string colors unchanged from before this pass) and a friendly label reading **"JSX"** — distinct from "JavaScript" — confirmed via direct DOM inspection after moving the caret out of the block. Opening the block's Actions menu → Change Language showed all 8 entries (JavaScript, JSX, TypeScript, TSX, JSON, CSS, HTML, Python) with **JSX** highlighted as the checked entry. No console errors.
- **Tests**: `fencedCodeLanguages.test.ts` gained 4 cases (`jsx`/`tsx` resolving to their own JSX-capable nested parser via a real `JSXElement` node; plain `js`/`ts` still parsing embedded JSX correctly, guarding the permissive-behavior requirement specifically). `fencedCodeLanguageLabel.test.ts` updated (`jsx`→`JSX`, `tsx`→`TSX`, was `JavaScript`/`TypeScript`). `fencedCodeFormatButtonDecoration.test.ts` gained 4 cases (Format button renders for JSX/TSX; formatting a `jsx`/`tsx` block produces correct output through the same babel/typescript parsers). New `FencedCodeActionsMenu.integration.test.tsx` (the first test file for this menu at all) — full `MarkdownEditor` mount, real menu interaction: all 8 languages listed, JSX/TSX checked correctly for their own fences, re-selecting the checked JSX entry is a no-op rewrite (the exact regression this pass fixes), selecting JSX/TSX from a plain `js`/`ts` block rewrites the fence correctly, selecting JavaScript from a `jsx` block correctly downgrades to plain `javascript`. Full existing suite (303 files / 4861 tests) passed alongside the 17 new/updated tests.

## Fenced code — first language expansion batch, and native lazy loading introduced for the first time (2026-09-10, eleventh pass)

- **Lazy loading (`load`, not `support`) is introduced for the first time, but only for genuinely new languages — the original eight (JavaScript/JSX/TypeScript/TSX/JSON/CSS/HTML/Python) stay eager, untouched.** *Locked.* Nine new languages added: YAML, XML, SQL, Shell, C, C++, Java, Go, Rust (`fencedCode/fencedCodeLanguages.ts`). Unlike the original set, most documents won't use most of these, and several grammars are genuinely heavy — the round-1/round-2 architecture investigations' own deferred recommendation ("revisit once real bundle-size cost exists") is triggered by this batch specifically, not before it. This reverses no prior decision; it fulfills the explicit condition those investigations set for revisiting it.
- **Package choices verified directly against `@codemirror/language-data`'s own source before being hand-authored** (not imported from it, per this file's JSX/TSX entry's own reasoning, which still applies): `@codemirror/lang-cpp` (`cpp()`, used identically for both `C` and `C++` — confirmed language-data does the same, no separate C grammar exists), `@codemirror/lang-java`, `@codemirror/lang-go`, `@codemirror/lang-rust`, `@codemirror/lang-xml`, `@codemirror/lang-yaml`, `@codemirror/lang-sql` (`sql({dialect: StandardSQL})`, matching language-data's own plain "SQL" entry). **Shell has no official `@codemirror/lang-shell` package** — `@codemirror/legacy-modes/mode/shell`'s `StreamLanguage`-based parser is what `@codemirror/language-data` itself resolves plain "Shell" to; confirmed this composes with `HighlightStyle`'s `scope` mechanism identically to a Lezer-based entry (this codebase's own prior investigation already closed this as a real question, not an assumption — see the round-2 architecture research summarized earlier in this document's history).
- **Parsing needed zero new code.** `@codemirror/lang-markdown`'s own `getCodeParser` already calls `LanguageDescription.load()` and defers via `ParseContext.getSkippingParser` until it resolves (confirmed against its installed source in the prior architecture-research round) — mixing eager and lazy `LanguageDescription`s in one `codeLanguages` array is exactly what this native mechanism is for, not a special case `fencedCodeLanguages.ts` had to accommodate.
- **Highlighting was the one real gap, closed with a `Compartment`, not a custom loader.** *Locked, the crux of this pass.* `fencedCode/fencedCodeHighlighting.ts`'s scoped `HighlightStyle`-per-language needs each language's concrete `Language` object *synchronously* — unavailable for a `load`-based entry until its dynamic import resolves. Fixed with `fencedCodeHighlightingCompartment` (a `Compartment` wrapping the current set of scoped highlighters) plus `ensureVisibleLanguagesLoaded`, a small function invoked from a `ViewPlugin` that scans `view.visibleRanges` for `FencedCode` blocks, resolves each `CodeInfo` against `fencedCodeLanguageDescriptions` via the same `matchLanguageName` every other fenced-code file already uses, and calls `.load()` on any match still unresolved — then reconfigures the Compartment once its promise settles. This is orchestration over native primitives (`LanguageDescription`, `Compartment`, `syntaxTree`), not a parallel loader: it introduces no new resolution/caching logic of its own.
- **No duplicate loading, confirmed by test, not just by reading `LanguageDescription`'s source.** `LanguageDescription.load()` caches its own in-flight/resolved promise on the instance (`this.loading || (this.loading = this.loadFunc().then(support => this.support = support))`) — calling it redundantly (this plugin re-scans on every `docChanged`/`viewportChanged` and will call `.load()` again on the same still-loading description) never triggers a second dynamic import. `fencedCodeHighlighting.test.ts`'s "two fences of the same not-yet-loaded language... trigger exactly one underlying dynamic import" test spies on the description's own `loadFunc` (the actual `import()` call), not `.load()` itself, specifically because `.load()` getting called multiple times is expected and fine — only the underlying import must not repeat.
- **The shared `fencedCodeHighlightSpecs` table and `scope` mechanism are unchanged — no per-language override was needed for any of the nine.** Verified directly with representative real code samples per language (`fencedCodeHighlighting.test.ts`'s parametrized lazy-language suite): every sample produced real, non-empty highlight spans through the exact same table used since the original six languages. This confirms round-2's architecture research conclusion (one shared tag table, targeted overrides only where a concrete case proves necessary) held for a materially different set of grammars (systems/C-family, markup, data, shell) without needing a single new override.
- **JSX/TSX-style identity questions did not recur for C/C++.** `C` and `C++` are two separate, independently selectable registry entries sharing the exact same `cpp()` parser call (confirmed, not assumed to be distinct grammars) — this is a deliberate, narrow precedent-following choice (two identities, one parser), not the JSX/TSX situation (which needed diverging *parse options*, not just labels) reopened.
- **Formatting deliberately not extended.** None of the nine gained a `codeFormatting.ts` entry — Prettier has no C/C++/Java/Go/Rust/Shell parser at all, and SQL/YAML/XML were deliberately left unformattable for this batch (in scope for a possible later, separate formatting-infrastructure effort, not silently expected to ride along with syntax support). `fencedCodeFormatButtonDecoration.test.ts` gained an explicit regression test asserting zero Format buttons render for any of the nine — a syntax-highlighting language and a formattable language are not the same claim, and this is now enforced, not just documented.
- **Verified live in the real Clutter webapp** (browser pane, Chromium engine): pasted Rust/Go/YAML/SQL fenced blocks in one document — all four rendered correct syntax highlighting through the existing theme (keyword/type/number/string/function colors, unchanged from before this pass) with correct friendly labels (Rust/Go/YAML/SQL), zero console errors. Opening the Rust block's Actions menu → Change Language showed all 17 registered languages in registry order with **Rust** correctly checked. Confirmed **zero Format buttons** rendered across all four new blocks, and Copy/Actions buttons rendered on all of them.
- **Tests**: `fencedCodeLanguages.test.ts` gained a parametrized suite (`it.each`) resolving each of the nine to a real, language-specific Lezer/StreamLanguage node (not plain `CodeText`) once loaded, plus a YAML-alias (`yml`) case and a C/C++-share-a-parser-but-differ-in-identity case. `fencedCodeHighlighting.test.ts` gained the Compartment-shape structural test, a parametrized real-sample highlighting suite for all nine, a mounted-`EditorView` end-to-end test (paste a `rust` fence, await the async load, confirm real highlight classes appear with no caller-side wiring), and the load-dedup test above. `fencedCodeLanguageLabel.test.ts` gained label/alias coverage for all nine (also correcting an existing test that used `rust` as its "unrecognized language" placeholder — no longer accurate now that Rust is registered). `FencedCodeActionsMenu.integration.test.tsx` gained a full-list-includes-the-batch case and a Rust-checked/select-Go-rewrites-the-fence case. `fencedCodeCopyButtonDecoration.test.ts` gained a case confirming Copy works for a lazy-batch language independent of whether its parser has loaded. Full existing suite (303 files / 4889 tests) passed alongside all new/updated tests.

## Fenced code — Change Language redesigned as an in-place view swap, not a submenu (2026-09-10, twelfth pass)

- **Change Language no longer opens `OverflowMenu.tsx`'s generic hover-opened submenu (a second, independently-anchored `Overlay`) — it swaps the Actions menu's own content between an "Actions" view and a "language selector" view, inside the same `Overlay`.** *Locked, replaces the tenth/eleventh-pass submenu design.* Motivation: the submenu mechanism is correct for a short, static leaf list (`OverflowSubmenuTrigger`'s own "Copy path" precedent, 2 options) but doesn't scale once the list grows into the dozens and needs search — a hover-opened popout has no room for a search input. `FencedCodeActionsMenu.tsx` no longer uses `OverflowMenuBody`/`OverflowMenu` at all (it only ever had two top-level items, so none of that generic item-config/submenu machinery was actually load-bearing) — `Menu`/`MenuItem` are composed directly for the Actions view, exactly as `OverflowMenuBody` itself already does internally, just without the layer that exists only to support hover-opened submenus.
- **The language-selector view is `FolderPicker.tsx`'s own pattern, reused directly, not reinvented** — confirmed by reading it before writing anything: a `Search` input holding real DOM focus, a plain `useMenuKeyboard(listRef)` (not a real `<Menu>`, which would fight the search input for focus-on-mount, per `FolderPicker.tsx`'s own doc comment explaining exactly this), and `Entry role="menuitem"` rows with `forceHover` for the same "keyboard-active row looks hovered" convention `MenuItem` itself already uses. Both `FencedCodeActionsMenu.tsx` and `FolderPicker.tsx` now independently arrive at the same shape for "a searchable list needs its own focus target, not a `<Menu>` container" — not merged into one shared component, since nothing else about them (data shape, single- vs. hierarchical-list, create-row support) is actually shared.
- **Search matches against `LanguageDescription.alias` — the exact array `codeLanguages`' own fence-info resolution already uses — never a second, hand-maintained keyword list.** `LanguageDescription.of` already folds the lowercased canonical name into its own `alias` array (cited in this file's JSX/TSX entry already), so matching only against `alias` covers both name and explicit aliases with one check: `js` finds JavaScript (name folded into its alias) and JSX (its own `jsx` alias) with the identical query, with no per-language search-index entry anywhere. A deliberate, accepted consequence of this being genuinely single-source: `c` also incidentally matches JavaScript (via its `cjs` alias) — a real substring hit on real registry data, not a bug to special-case around.
- **Escape and the backdrop share one `onClose` on the single `Overlay`, which goes back before it closes — `Overlay`/`useEscape` needed zero changes.** Both already funnel into one `onClose` prop; `handleRequestClose` in `FencedCodeActionsMenu.tsx` checks the current view first, so the first Escape (or backdrop click) while the language view is showing returns to the Actions view, and only a second one (already on the Actions view) actually closes the menu. This is the existing single-callback contract every other `Overlay` consumer already has, not a second dismissal mechanism layered on top of it.
- **Reopening the menu always starts fresh on the Actions view with an empty search** — `FencedCodeActionsMenu` itself never unmounts between opens (only its `Overlay` does, per `Overlay`'s own `if (!open) return null`), so without an explicit reset effect keyed on `anchor` transitioning to open, a reopen would silently resume wherever a prior visit left off (mid-search, or still on the language view).
- **Verified live in the real Clutter webapp** (browser pane, Chromium engine): opening Actions showed exactly "Change Language"/"Remove" (no chevron, no hover-opened popout); clicking Change Language swapped the same surface in place to a Back arrow + "Change Language" header + focused search input + all 17 languages, JavaScript correctly checked; typing `c` filtered live to JavaScript/TypeScript/CSS/C/C++ (the `cjs`/`cpp`-alias-substring behavior named above, confirmed as expected, not a bug); the Back button returned to the Actions view without closing the menu; a first Escape did the same; a second Escape (now on the Actions view) closed the whole menu. No console errors from the current code (one transient HMR-reload error from mid-edit, before an unrelated `MenuItem` prop mistake was fixed, did not reproduce on any later reload — confirmed via curl against the live dev server and a fresh page load).
- **Tests**: `FencedCodeActionsMenu.integration.test.tsx` gained two new `describe` blocks — one for the view-swap/back/Escape/reopen-resets-state mechanics (7 cases), one for search (case-insensitivity via the registry's own aliases, `py`→Python-only, `c`→the C-family, empty-state on no matches, selected-state surviving a filter, clearing restores the full list). All 9 pre-existing Change-Language cases (list contents, checked state, rewrite-on-select, the JSX/TSX re-selection fix from the tenth pass) passed completely unmodified against the new implementation — confirming the interaction model changed without changing any of the underlying language-resolution/rewrite behavior those tests protect.

## Fenced code — Change Language: no back button, render-phase state reset, medium-width unification (2026-09-10, thirteenth pass)

- **The Back button and all back-navigation were removed — simplification, not a partial revert of the twelfth pass's view-swap architecture.** *Locked.* The language view has no way back to the Actions view short of closing (Escape, backdrop, or the new dismiss button below) and reopening — `Overlay`'s `onClose` no longer branches on `view` at all, it always fully closes. Reopening always lands on the Actions view (the render-phase reset below), which is what makes this an acceptable simplification: going back costs one close-plus-reopen, not a rebuilt language choice.
- **A real, confirmed flash bug was found and fixed in the reopen-reset mechanism — not the view-swap/single-Overlay structure a prior report suspected.** *Locked, corrects the twelfth pass's reset mechanism specifically.* The twelfth pass reset `view`/`query` in a `useEffect` keyed on `anchor`. Effects run *after* React commits and paints — reopening the menu right after leaving it on the language view painted one real frame with the stale `view` before the effect corrected it a moment later, a genuine one-frame flash of the language picker on every reopen from that state. (Investigated first, per the report that suspected a second/stacked Overlay: confirmed via live DOM inspection — `document.querySelectorAll('.overlay')` — that there was and is only ever one `.overlay`/`.overlay__content`; the visible symptom was the stale-view flash, not a layering bug.) Fixed using React's own documented pattern for "adjust state when a prop changes, no flash": track the previous open/closed state as `useState` (not `useRef` — mutating a ref during render isn't part of React's render-phase model and isn't safe under Strict Mode's double-invocation) and call `setView('actions')`/`setQuery('')` unconditionally during render, guarded so it only fires on the actual closed→open transition. A render-phase `setState` call is folded into the same render pass by React, so the corrected view is what's committed and painted — no intermediate frame with the stale view is ever produced, which is categorically different from (and not fixable by) a timeout, animation delay, or CSS hiding trick.
- **Not testable via the existing RTL-based test suite, and this is stated explicitly rather than glossed over.** React Testing Library's `fireEvent`/`render` wrap every interaction in `act()`, which flushes effects synchronously before an assertion can run — a test taken after the interaction completes cannot distinguish "reset happened during render" from "reset happened in an effect a tick later," since both produce the identical final DOM by the time `act()` returns control. The existing reopen-state test (`FencedCodeActionsMenu.integration.test.tsx`) still correctly guards against the reset being missing or producing the wrong final view; the flash itself was verified by following React's documented pattern precisely (its whole purpose is guaranteeing no separate committed frame) and by live browser observation (open → Change Language → click outside → immediately reopen, screenshotted), not by an automated timing assertion — a real limitation of jsdom+RTL for this specific defect class, not an oversight.
- **Two small, explicitly requested UI affordances added, both reusing existing components verbatim:** the Actions view's "Change Language" row gained a trailing `chevronRight` `AppIcon` (the exact same prop `OverflowSubmenuTrigger` already uses for its own "opens something" affordance) signaling it navigates further; the language view's header gained an explicit dismiss button (`Button` with `variant="ghost" interaction="subtle" isIconOnly size="small"`, the identical combination `Search.tsx`'s own inline clear button already uses) calling `onClose` directly, needed now that there's no back button to also serve as an implicit "leave this view" control.
- **Both views now render at the same `220px` width** — the Actions view's `<Menu size="medium">` (was `size="small"`, `180px`) now matches `.fenced-code-language-picker`'s own already-220px width exactly, closing a visual inconsistency from the twelfth pass (raised directly: "why aren't you using the same menu component" — answered there: `<Menu>` auto-focuses its own container on mount, which fights the language view's search-input-focus requirement, so the component itself still can't be shared, but nothing prevented the *width* from matching, and now does).
- **Verified live in the real Clutter webapp**: reopening the menu immediately after leaving it on the language view now shows the Actions view on the very first rendered frame — repeated several times, no flash observed. The Change Language row shows the trailing chevron; the language view shows the dismiss (✕) button at the top right of its header, closing the whole menu when clicked. Both views measured at identical width. No console errors from current code (the one recurring console entry from the fenced-code mid-edit HMR failure earlier in this session's history was reconfirmed, again, not to reproduce on any fresh reload).
- **Tests**: two new cases in `FencedCodeActionsMenu.integration.test.tsx` (trailing chevron present on the Change Language row; the dismiss button closes the whole menu), the two back-navigation-specific cases from the twelfth pass replaced with cases matching the no-back-navigation behavior (Escape from the language view closes directly; reopening after a prior visit still starts fresh, now via a single Escape rather than two). Full existing suite (303 files / 4904 tests) passed alongside the updated/new cases.

## Fenced code — Change Language rebuilt on the shared `Menu` component; `Menu` gains `autoFocus` (2026-09-10, fourteenth pass)

- **The standalone `.fenced-code-language-picker` container from the twelfth/thirteenth passes is retired — both the Actions view and the language view now render inside one shared `<Menu size="medium">`, using `MenuItem` for every row in both views.** *Locked.* Investigated first, per instruction: read `Menu.tsx`, `MenuItem.tsx`, `Menu.context.tsx`, `useMenuKeyboard.ts`, and every existing `Menu` consumer (`Breadcrumbs.tsx`, `ImageOptionsMenu.tsx`, `OverflowMenuBody`'s and `OverflowSubmenuTrigger`'s own internal `<Menu>`s) before writing anything. Finding: `useMenuKeyboard` is already a plain, standalone hook with no dependency on `<Menu>` itself — `FolderPicker.tsx` already calls it directly, and so did this file's own prior standalone version. The *only* actual conflict was `Menu`'s unconditional mount-time `menuRef.current?.focus()` effect, which fights a search input's need to hold focus instead. Confirmed as a genuinely recurring pattern, not invented for this feature: `FolderPicker.tsx` and this file's language view share an *identical* shape (linear list, `useMenuKeyboard(listRef)`, `Entry`/`MenuItem` rows); `EmojiTray.tsx` is a third component motivated by the same "a focused text input must not be stolen by container autofocus" problem, though with 2D grid keyboard semantics rather than `useMenuKeyboard`'s linear model.
- **`Menu` gained one new prop: `autoFocus?: boolean` (default `true`).** *Locked.* Considered and rejected a richer "focus target" API (e.g. accepting a ref to focus instead) as premature — `Menu` doesn't need to know *what* the caller focuses instead, only that it should step out of the way; no current or foreseeable consumer needs `Menu` to focus something on the caller's behalf. `autoFocus` mirrors the native HTML/React convention for exactly this semantic. Every existing consumer is unaffected (`autoFocus` defaults to today's unconditional-focus behavior); confirmed via the full existing `Menu`/`OverflowMenu` test suite passing unmodified (29 tests) alongside the full app suite (303 files / 4904 tests).
- **With `Search` rendered as a genuine DOM child of `Menu`'s own container (not a sibling outside it, the twelfth/thirteenth-pass shape), keydown events bubble to `Menu`'s own `onKeyDown={keyboard.handleKeyDown}` natively.** This eliminates the manual `keyboard.handleKeyDown(event as unknown as ...)` re-dispatch the standalone version needed — only Space still needs an explicit `event.stopPropagation()` on `Search`'s own `onKeyDown` (to keep it a literal character rather than `useMenuKeyboard`'s "activate current item" binding for Space), matching exactly the minimal interception `FolderPicker.tsx`'s own `handleSearchKeyDown` already established for the identical reason.
- **`aria-activedescendant` follows real DOM focus, verified rather than assumed correct.** *Locked.* `Menu`'s own container still carries `aria-activedescendant` unconditionally (unchanged) — but since real focus lives on `Search` while the language view shows, `Search` itself also carries `aria-activedescendant` pointing at the same `activeId`, matching the exact convention `FolderPicker.tsx` already established for a focused text input virtually owning a list below it. Verified live (not just reasoned about): opening the language view showed `document.activeElement` as the `Search` input with `aria-activedescendant="JavaScript"` (the current language) already set; ArrowDown moved it to `"JSX"` with focus still on the input; hovering a row updated it to that row's name via `MenuItem`'s own built-in `onMouseEnter` — confirmed via live DOM inspection, not inferred.
- **`useMenuContext()` (the same hook `MenuItem` calls internally) is called directly by a small `LanguagePickerContent` sub-component rendered as `Menu`'s own child** — the only way to reach `Menu`'s `{activeId, setActiveId}` from outside `MenuItem` is to be a React descendant of `Menu`'s own `MenuContext.Provider`, which is why this couldn't be inlined one level up in `FencedCodeActionsMenu`'s own render body.
- **A real automation-tool artifact was found and ruled out during verification, not mistaken for a component bug.** Testing Enter-to-select via the browser-automation harness's synthesized "Return" keypress produced a native `keydown` with an *empty* `event.key` (confirmed by a temporary listener), which `useMenuKeyboard`'s `case 'Enter':` naturally never matches — nothing this component does could "fix" that, since the browser event itself never carried the key identity Enter-handling depends on. Dispatching a properly-formed `new KeyboardEvent('keydown', {key: 'Enter', bubbles: true})` at the same input selected the active row, closed the menu, and rewrote the fence correctly on the first attempt — confirming the real component logic was correct throughout, and explaining why the existing `fireEvent.keyDown(el, {key: 'Enter'})`-based test suite (which sets `key` explicitly) never caught anything, because there was nothing to catch.
- **The reopen-flash fix from the thirteenth pass needed no changes and is unaffected by this rebuild** — confirmed explicitly, since a prior report suspected the standalone container itself might be responsible. The render-phase `view`/`query` reset (a `useState`-tracked previous-open flag, not an effect) is orthogonal to which component renders the content; it was carried over unchanged and reverified live: open → Change Language → click outside → immediately reopen shows the Actions view on the first rendered frame, no flash, repeated multiple times.
- **The custom `.fenced-code-language-picker` CSS container rule is deleted, not merely unused** — background/border-radius/shadow/width/max-height/scroll all now come from `.menu`/`.menu--medium` directly. Only the language view's own header (title + dismiss button) and empty-state row remain as this file's own CSS, since neither has an equivalent in `Menu.css`.
- **Verified live in the real Clutter webapp**, beyond the items above: Space typed into the search field stayed a literal character (confirmed both via a value check and via the empty-state rendering for a query no language matches); filtering to `py` showed exactly Python with its `aria-activedescendant`/keyboard-active state correct; clicking a filtered result correctly rewrote the fence and closed the menu; a language matching the current fence (e.g. `js` while `JavaScript` and a hovered `TypeScript` were both visible) showed `selected` (background) and hover/keyboard-active (`forceHover`) as independently visible, coexisting states — never conflated into one. No console errors from current code.
- **Tests**: the full existing `FencedCodeActionsMenu.integration.test.tsx` suite (24 cases) passed completely unmodified against this rebuild — the strongest available evidence that the interaction's observable behavior is unchanged even though its internal implementation (shared `Menu` vs. a standalone container) is completely different. `Menu.test.tsx`/`OverflowMenu.test.tsx` (29 tests) passed unmodified, confirming `autoFocus`'s default preserves every existing consumer's behavior. Full suite: 303 files / 4904 tests.

## Fenced code — `MenuTitle` extracted; dismiss now returns to Actions; a latent `MenuItem` dual-highlight bug fixed (2026-09-10, fifteenth pass)

- **`MenuTitle` (`@components/menu/MenuTitle.tsx`) is a new, small, shared component — not a copy of `MenuItem`.** *Locked.* Investigated first: a menu title is not a choice a user makes, so it must never carry `role="menuitem"`, participate in `useMenuKeyboard`'s roving `activeId`, or show a hover/click affordance. Reading `Entry.tsx` directly showed this is already `Entry`'s own *default* behavior — `role`/`tabIndex` default to `undefined` and the `entry-interactive` class (which gates both the pointer cursor and the hover background) is only added when an `onClick` prop is passed. `MenuTitle` is therefore a thin wrapper: `<Entry className="menu__title" trailing={trailing}>{children}</Entry>`, no `onClick`, no `id`, no `forceHover`/`selected` — none of which have meaning for a title. `Entry`'s own `flex: 1` content column already pins `trailing` to the right edge, which is why `MenuTitle` needed no bespoke `justify-content: space-between` layout of its own the way the language view's original hand-rolled header div did. Used for the language view's "Change Language" header, with the dismiss button as its `trailing` content.
- **The language view's header button no longer closes the whole menu — it returns to the Actions view.** *Locked, reverses part of the fourteenth pass's "no back-navigation mechanism of any kind" framing, per explicit direction.* Renamed `aria-label` from `"Close"` to `"Back to actions"` to match; Escape and the backdrop click are unaffected and still close the whole menu directly from either view (`Overlay`'s own `onClose`, untouched). This is now the only back-navigation control in either view — there is no equivalent control in the Actions view itself, since Change Language remains the only way in.
- **A real, latent `MenuItem`/`Entry.css` bug was found and fixed: two rows could show as visually active simultaneously.** *Locked, the most consequential finding of this pass.* Root cause, confirmed by reading `Entry.css` directly rather than guessed: its hover rule is a plain comma-OR — `.entry-interactive:hover` (native, driven by the real mouse position, entirely outside React) and `.entry-force-hover` (controlled, driven by the single shared `activeId` in `MenuContext`) both paint the identical background independently. Resting the real pointer on row A, then pressing ArrowDown/ArrowUp to move `activeId` to row B without the mouse also moving, left **both** rows highlighted: A via stale native `:hover`, B via `.entry-force-hover`. This was not introduced by this feature — it was already latent in `MenuItem`/`Entry.css` before this session's work, just far more likely to surface with the language view's 17 rows than the original 2-item Actions menu, and had no prior fix anywhere in this codebase to reuse (checked `FolderPicker.tsx`, which shares the identical latent exposure, and this document's own history — no matching prior fix exists).
- **Fixed at the interaction-model level, not by hiding one highlight with CSS.** `MenuItem.css` gained one rule: `.menu__item.entry-interactive:hover:not(.entry-force-hover) { background: transparent; }` — scoped specifically to `.menu__item` (not `Entry` generally, which every non-menu consumer like sidebar rows still needs plain native `:hover` for, with no roving-`activeId` concept at all). This makes `.entry-force-hover` (driven by the one shared `activeId`) the *sole* source of truth for which row looks active; native `:hover`'s only remaining role is to *update* `activeId` via `MenuItem`'s existing `onMouseEnter` (unchanged) — it can no longer independently paint a highlight of its own. Ordinary mouse-only use is visually unaffected (the two signals already coincide on the same row the instant the mouse moves), which is exactly why this reads as a bug fix and not a behavior change.
- **This fixes the same latent bug for every `MenuItem` consumer app-wide** (`Breadcrumbs.tsx`, `ImageOptionsMenu.tsx`, `OverflowMenuBody`, `OverflowSubmenuTrigger`), not just the language picker — a deliberate, correct consequence of fixing it in the shared `MenuItem.css` rather than scoping a workaround to this one feature.
- **Not testable via the existing jsdom-based test suite, stated explicitly rather than glossed over** (the same category of limitation as the fourteenth pass's reopen-flash fix): jsdom has no rendering/layout engine, so it never evaluates CSS pseudo-classes like `:hover` at all — a test asserting this fix's actual visual effect would need a real browser. Verified live instead, precisely: hovered a row (confirmed via `document.elementFromPoint` and `aria-activedescendant` both matching), pressed ArrowDown twice without moving the pointer, then read `getComputedStyle(...).backgroundColor` directly on every row — exactly one row carried the active (`entry-force-hover`) background, the row still under the stationary pointer carried none, and the separately-and-correctly-coexisting `entry-selected` row (the fence's current language) was the only other highlighted row, matching its own distinct, intentional semantics (confirmed already correct in the fourteenth pass, unaffected here).
- **Tests**: `FencedCodeActionsMenu.integration.test.tsx`'s two back-navigation cases were rewritten for the new behavior (the header button is present only in the language view and returns to Actions rather than closing; Escape still closes the whole menu directly) — the other 22 cases passed unmodified. No new automated test could meaningfully cover the `:hover` fix itself, per the jsdom limitation above. Full suite: 303 files / 4904 tests, `tsc` clean.

## Fenced code — language results list capped at 264px, scrolling independently of the header/search (2026-09-10, sixteenth pass)

- **The language rows are now wrapped in their own `.fenced-code-language-picker__list` (`max-height: 264px; overflow-y: auto`), scrolling independently of `MenuTitle`'s header and the `Search` input above it.** *Locked.* Previously the entire `Menu` (title, search, and every row) shared one `overflow`/`max-height` from `.menu` itself (`calc(100dvh - 16px)`, effectively unbounded for a 17-row list on any normal viewport) — scrolling would have carried the header/search out of view along with the rows. The extra wrapper div doesn't affect `useMenuKeyboard`'s `getMenuItems()` (a `querySelectorAll('[role="menuitem"]')` off `Menu`'s own container ref, unaffected by intermediate wrapper elements) or keyboard navigation in any way — confirmed via the full existing test suite passing unmodified.
- **Verified live**: the list visibly truncates after ~8 rows and scrolls via mouse wheel, with `MenuTitle`'s header and the `Search` input staying fixed in place above it; `getBoundingClientRect()`/`scrollHeight` confirmed the wrapper measures exactly 264px against a 510px content height. Tests: all 24 `FencedCodeActionsMenu.integration.test.tsx` cases passed unmodified (this is a pure CSS/layout change, no interaction-model change). Full suite: 303 files / 4904 tests.

## Fenced code — the "prefer selected, else first" behavior generalized into `useMenuKeyboard` itself (2026-09-10, eighteenth pass; supersedes the seventeenth pass's local fix)

- **The seventeenth pass's fix (a local `useEffect` inside `LanguagePickerContent` computing `filteredDescriptions.find(d => d.name === currentName) ?? filteredDescriptions[0]`) is retired in favor of a genuinely shared `useMenuKeyboard` capability: `preferredActiveId`.** *Locked.* Revisited on explicit instruction after confirming the local version worked correctly — the same "prefer X, else first, else clear" shape was about to be hand-rolled a second time for `FolderPicker.tsx`, confirming it's a real recurring pattern, not a one-off.
- **API shape: `preferredActiveId?: string | null` on both `useMenuKeyboard`'s options and `Menu`'s own props, forwarded straight through.** `undefined` (the default — every existing caller before this pass) means "this feature is off," preserving `activeId` starting `undefined` and changing only via an explicit Arrow keypress or `setActiveId` call, byte-for-byte the prior contract. `null` means "opted in, no id to prefer right now" (e.g. an unrecognized fence language) — still resolves to the first navigable item rather than leaving `activeId` untouched. A real id string means "prefer this one." `undefined` vs. `null` is a deliberate, load-bearing distinction, not an accident: it's what lets `FencedCodeActionsMenu.tsx` pass `currentName ?? null` while on the language view (always opted in) and `undefined` while on the Actions view (never opted in) through the exact same prop, with no separate boolean flag needed.
- **The hook has zero notion of what "preferred" means to any caller — it only ever compares plain DOM element ids against the live `[role="menuitem"]` set.** No language, folder, or "selected" concept was pushed into the shared layer; only the *mechanism* (resolve preferred-or-first-or-none, react to the navigable set changing, never stomp on manual navigation) is shared. This is the same boundary the seventeenth pass's own investigation already drew — what changed is realizing the mechanism itself, not the "what counts as selected" policy, is what recurs.
- **Detecting "the navigable set changed" without requiring callers to pass a synthetic dependency key.** The resolution runs in a `useEffect` with no dependency array (executes after every render), but the effect body itself compares a resolution key (`navigableItems.map(i => i.id).join(' ') + '|' + preferredActiveId`, tracked in a ref) against its previous value and bails out immediately if unchanged. A render caused by the user's own Arrow-key/hover navigation doesn't change the navigable id set, so it bails out and never overwrites that navigation; a render that actually adds/removes/reorders navigable items (search-filtering) or changes `preferredActiveId` itself produces a different key and triggers a genuine re-resolution. This is what satisfies "preferred item disappears/reappears due to filtering" without any special-casing for that scenario specifically — it falls out of the same generic comparison.
- **`FolderPicker.tsx` migrated too, and this is the one existing consumer judged worth migrating now** (not "keep the API ready for later," per the explicit instruction to only migrate further where it genuinely improves existing semantics). It had no "preferred" concept at all — always "highlight the first visible row, or the synthetic Create row when that's the only thing showing" — implemented via its own hand-rolled `useEffect` computing `visibleItems[0]?.id ?? (showCreate ? CREATE_ITEM_ID : undefined)`. Passing `preferredActiveId: null` reproduces this exactly: verified the Create row is *always* the sole rendered `[role="menuitem"]` whenever `showCreate` is true (confirmed from `showCreate`'s and `visibleItems`'s own derivations: `showCreate` requires `filteredItems.length === 0`, and while searching `visibleItems === filteredItems`, so the two states are mutually exclusive) — so "first navigable DOM item" already resolves to the Create row in that case with no special-casing, and the hand-rolled effect was deleted entirely.
- **Verified**: all 24 pre-existing `FencedCodeActionsMenu.integration.test.tsx` cases plus the 5 preferred-active-item cases from the seventeenth pass passed completely unmodified against the generalized mechanism — strong evidence the observable behavior didn't change, only where it's implemented. All 34 `FolderPicker`/`MoveDestinationPicker` tests also passed unmodified. Live-verified the language picker still highlights the fence's current language (TypeScript) on open through the new shared path, with no console errors.
- **Tests**: new `useMenuKeyboard.test.tsx` (10 cases) at the helper level — no preferred id (exact prior behavior unchanged), preferred id present, preferred id missing (first-item fallback), `null` (opted in, no preference), preferred item disappearing/reappearing across a simulated filter re-render, an empty item list (clears `activeId`), a disabled preferred item (treated as not navigable), and — the case directly protecting the single-active-item invariant restored two passes ago — that a re-render with an *unchanged* navigable set never overrides an in-progress Arrow-key navigation, plus an explicit "exactly one active item" assertion. Full suite: 304 files / 4919 tests, `tsc` clean.

---

## `@Today` / relative dates — reversal, capture explicitly

- **`@Today`/`@Tomorrow` are insertion-time shorthand only, never persistent syntax.** *Locked.* **This reverses an earlier working assumption**, carried through the shared-token-interaction-contract and semantic-resolution-model documents, that `@Today` was a fifth persistent token kind exactly like `WikiLink`/`Tag`/`Mention`/`@due`, with its own `DateToken` node. That assumption is superseded — accepting "Today" immediately writes the concrete literal date; nothing named `@Today` ever reaches the buffer. Reasoned from two angles that converged: (1) a persistent-but-frozen reading is architecturally impossible without hidden state, which is explicitly forbidden; (2) a persistent-and-dynamic reading fails both worked scenarios (a daily note's own `@Today` drifting off its dated identity; a recorded "launch date" silently changing every day). See [relative-date-semantics.md](./editor-research/clutter-editor-relative-date-semantics.md).
- **Consequence**: the `@`-family parser's disambiguation simplifies from three branches (Mention/DateToken/PropertyToken) to two (Mention/PropertyToken) — less parser complexity than previously planned, a direct benefit of the reversal, not a cost.
- **`@due:@Today` needs no recursion.** *Locked.* Nothing persistent exists to nest; malformed input is handled exactly like any other invalid property value.
- **A non-recursive path for a future relative due-date exists** (`@due:today` as a literal keyword inside `PropertyToken`'s own value grammar) but is **not decided to be built** — see Open items below.

---

## Editor/persistence boundary (carried from the architecture-proposal phase, restated for continuity)

- **The editor never imports `Vault`/`VaultQuery`/`EffectivePageState`/`PageOperations` directly.** Suggestion candidates and asset resolution are always injected via props. *Locked.*
- **CM6's undo history is scoped to the mounted editor instance**, and this is already guaranteed for free by the existing `key={activePageId}` remount pattern in `PageHost.tsx` — confirmed by direct inspection during the implementation-plan phase, not merely proposed. *Locked.*
- **CM6-local undo history and `DocumentSession`'s Committed-stage revisions must never be conflated.** Two distinct mechanisms, no shared code path. *Locked.*

---

## Open — genuinely unresolved, not silently decided

- **Case-sensitivity of WikiLink path matching** relative to `VaultPath`'s actual existing behavior — flagged honestly as uncertain in the grammar research, not confirmed against the real implementation.
- **Folder-path targets** (`[[Projects]]` where `Projects` is a folder, not a page) — recommended default is "treat as unresolved," not locked.
- **Empty local alias** (`[[path|]]`) treated as display-equivalent to "no local alias" — recommended, not locked.
- **Whether tags resolve to a Page-like entity or a distinct existence-check projection** — depends on the actual shape of the existing `core/application/tags` module, not independently inspected during this research.
- **Unresolved-mention click behavior** (offer to create a person/entity, mirroring WikiLink's create-on-click) — genuinely depends on whether a person/entity system exists at all, which is out of scope for the editor's own architecture.
- **Whether a deliberately-relative "live template" construct is wanted at all**, as a distinct, separately-syntaxed feature from ordinary `@Today` shorthand — real use case named, not designed, would need its own syntax if built.
- **Whether `@due:today`/`@due:tomorrow` relative keyword values are wanted** — the non-recursive mechanism is defined; whether to build it is undecided.
- ~~Exact GFM node name to order the `@`-family parser `after:`~~ — **Resolved 2026-08-18, see "Verified against installed APIs" below.**
- ~~Exact resulting tree shape when a semantic token nests inside `**bold**`~~ — **Resolved 2026-08-18, during WikiLink implementation (§4).** Empirically confirmed via direct tree inspection of `**[[Page]]**`: `WikiLink` sits as a direct child of `StrongEmphasis`, between its two `EmphasisMark` children (`StrongEmphasis > [EmphasisMark, WikiLink, EmphasisMark]`) — exactly the composition predicted by analogy with GFM's own `Strikethrough`-inside-`Emphasis` precedent. Covered by a permanent regression test, not just a one-off check: `markdownLanguage.regression.test.ts`.
- **Engagement gesture felt-experience** (does "press again at the boundary" read as discoverable) — carried forward from the architecture-decisions phase as a standing prototype item, not resolved by any document in this archive.
- **Bare URL/Autolink rendering — investigated twice (once before, once after the `Link` implementation), deliberately left undecided both times.** `https://`/`www.`/bare-email all parse as a plain `URL` node (no wrapper); `<https://...>` is the one case with real bracket syntax (`Autolink > [LinkMark, URL, LinkMark]`) — confirmed the four sub-cases are not identical and none should be assumed to want the same treatment as the others or as `Link`. Nothing decorates any of them today (`markdownHighlighting()` is still dormant; `@lezer/markdown`'s own `tags.url` tag exists and is unused). Confirmed empirically, twice, that `Link`'s addition introduced zero interaction with bare URLs in either direction (independent tree nodes, independent decoration, no leakage). **The open question stated precisely**: at rest, should a bare URL stay ordinary text, get CSS-only styling (`tags.url` → a `markdownHighlightStyle.ts` entry, no concealment, no engagement machinery — the only option proportionate to what's actually true about the grammar today), or become a compact rendered widget? Unlike `Link`, there is no bracket/paren syntax to conceal — "hide syntax, keep content editable" doesn't have an application here, so this is not simply "`Link`, but for bare URLs."
- **Link/URL-family click activation — resolved 2026-08-25.** *Locked.* The "explicitly deferred as a separate, later, cross-cutting feature" framing above is superseded for the *navigation* piece. Explicit `Link` and bare `URL`/`Autolink` now navigate on plain left-click via the existing generic `semanticToken/tokenMouseHandlers.ts` mechanism, reused unmodified — no new shared infrastructure, no modifier key, consistent with WikiLink/Tag/Date precedent: an at-rest occurrence activates on click (`findAtRestTokenAt` already excludes engaged nodes, so clicking an already-engaged/raw link is ordinary text editing, not navigation — this fell out of the existing mechanism for free). Unlike WikiLink/Tag/Date, no resolver injection is needed — opening a URL has no Vault/app-layer dependency, so `link/linkActivation.ts` and `link/urlActivation.ts` call the existing `shared/helpers/openExternalUrl.ts` helper directly. `Link` always opens externally, unconditionally — no vault-relative-path resolution branch; that stays `[[WikiLink]]`'s exclusive concern. See `link/linkMouseHandlers.ts` and `link/urlMouseHandlers.ts`.
- **Link/URL styling — resolved 2026-08-25.** *Locked.* The styling/decoration piece of the question above is also now resolved, deliberately reusing `Link`'s own mechanism and CSS class rather than inventing a second visual convention: `Autolink` (`<https://...>`) registers `delimitedInlineRenderer('LinkMark', 'tok-link')` in `inlineLivePreviewParticipants.ts` completely unmodified — its `Autolink > [LinkMark, URL, LinkMark]` shape already fits that factory's 2-same-named-mark-child precondition exactly, so no Autolink-specific renderer was needed at all. A bare `URL` (no wrapper) gets a minimal new `urlRenderer` that applies the identical `tok-link` class with **no concealment** (per the "nothing to conceal" reasoning above, its at-rest and engaged forms are identical by construction — it still participates in the shared traversal for consistency, it just never visually differs when engaged). Both are ordinary marker-hiding-family participants (never atomic, never in `EditorView.atomicRanges`), reusing the exact same `Decoration.mark`/`Decoration.replace` shape and the same `tok-link` CSS rule Link's label already uses — confirmed via a same-class assertion, not just visual inspection. `urlRenderer` guards against double-decorating a `URL` child already owned by an enclosing `Link` or `Autolink` participant (checks `node.node.parent?.name`) — necessary because `inlineLivePreviewRegion.ts`'s traversal only short-circuits descent for an *engaged* participant, not a non-engaged one, so a `URL` nested inside either would otherwise be visited a second time. See `inlineLivePreviewParticipants.ts`'s `urlRenderer` and its registration.

---

## Live-preview rendering architecture (research, 2026-08-23) — Recommended, not locked

*Recommended, not locked.* Full research: [cm6-live-preview-rendering-architecture.md](./editor-research/cm6-live-preview-rendering-architecture.md).

- **Root cause of the previous breakage, confirmed forensically**: 100% a keyboard/command-layer problem (hand-rolled Arrow-key cursor hops, a drag-selection "atomic sweep" corrector, and Backspace/Enter/Tab keymaps racing `@codemirror/lang-markdown`'s own defaults at `Prec.high`), never a decoration/widget problem — the dormant `*Decoration.ts` files were audited and found to have no keymap/`atomicRanges`/`transactionFilter` coupling. Reframes the task: decorations are the low-risk part; hand-rolled commands are the part to avoid rebuilding.
- **Recommended split per construct**: `Decoration.mark()` for content that stays character-editable (bold/italic inner text, heading text), `Decoration.replace()` for marker concealment (`**`, `##`, `[[`/`]]`) and for the already-Locked semantic-token family at rest — not one uniform strategy.
- **`EditorView.atomicRanges` should stay scoped to the semantic-token family only** (WikiLink/Tag/Date/Task, already Locked) — never applied to ordinary Live-Preview-hidden marks — confirmed both by this repo's prior finding and by a real-world documented bug (`discuss.codemirror.net/t/hide-markdown-syntax/7602`) where broad `atomicRanges` use turned ordinary character deletion into whole-span jumps.
- **Engagement (reveal-on-cursor-enter) should stay selection-derived, computed fresh per decoration rebuild** — matches the already-Locked semantic-token model and independently confirmed by every real-world implementation surveyed (Obsidian's plugin ecosystem, `atomic-editor`, `codemirror-live-markdown`) — never stored engagement state.
- **`ViewPlugin`, not `transactionFilter`/`changeFilter`, is the correct home for this rendering** — confirmed at the CM6 API-contract level: neither filter has any path into the `EditorView.decorations` facet, and `transactionFilter` is explicitly documented as unsuitable for this ("likely to break something or degrade the user experience").
- **CM6's clipboard already copies underlying `state.doc` text by default, regardless of decorations** — no custom clipboard handling needed for ordinary copy/paste.
- **IME safety is not resolved by research alone** — CM6 has strong internal composition-safety machinery but no public contract covering concealed markers adjacent to an active composition range; flagged as needing direct manual testing (Hindi, Japanese, Korean, Chinese, dead keys) before shipping.
- **Tables are explicitly out of scope for the shared inline mechanism** and need their own design pass — the one construct every real-world implementation surveyed found genuinely hard.

---

## Nested inline Live Preview visibility — Locked (2026-08-24)

**Full contract: [inline-live-preview-region-odr-v1.md](./editor-research/inline-live-preview-region-odr-v1.md). Accepted, binding; implementation not yet performed.**

- **Live Preview visibility resolves at the structural inline *rendering-region* level, not per construct and never per plugin.** A node renders as source iff some visibility-participating **ancestor-or-self** is directly engaged. The syntax tree is authoritative for containment; `isTokenEngaged` stays the unchanged primitive for *direct* engagement and is never duplicated or replaced.
- **A nested region has one coherent preview/source state** — mixed outer-revealed/inner-concealed output is a contract violation. Construct renderers own decoration only, never engagement policy. No pairwise or combination-specific logic is permitted, and adding a construct must not require touching existing constructs.
- **Rendering mechanism is not a structural boundary** — marker-concealing decorations, widget replacement, and `atomicRanges` all belong to one region and one visibility answer. Block-level rendering stays out of scope until an explicit future decision.
- **The current `emphasisLivePreview`/`strikethroughLivePreview` split is an implementation artifact, not a behavioral boundary** — it is the proximate cause of the verified `~~__Text__~~` defect, carries no architectural authority, and is subject to consolidation per the ODR's migration plan. The strikethrough investigation's "separate plugin" conclusion is **superseded**; its parser findings remain valid evidence.

**Correction (2026-08-27) — engaged-region marker representation, narrowing "rendering mechanism is not a structural boundary".** *Locked.* That bullet's original scope ("marker-concealing decorations, widget replacement, and `atomicRanges` all belong to one region and one visibility answer") is narrowed to **content decorations, widget replacement, and `atomicRanges`** — those three remain a single all-or-nothing answer per region, unchanged. **Marker spans are carved out**: an engaged region's own nested marker-contract constructs (Emphasis/StrongEmphasis/Strikethrough/Highlight/InlineCode — the same five already migrated to the at-rest marker-DOM contract, docs/markdown-dom-structure-agreement.md §7.1) each independently get their own `cm-marker cm-{construct}-marker` span, uncollapsed by nesting depth, while the region stays engaged.

Reasoning: the pre-correction behavior — reachable only via `ENGAGED_MARKER_RENDERERS`, a per-node-name lookup keyed to the region root alone — was confirmed (mounted `EditorView`, real DOM) to drop every nested construct's own marker representation the instant its ancestor engaged: `***bold italic***` engaged produced `<span class="cm-marker cm-emphasis-marker">*</span>` + raw, un-spanned `**bold italic**` text + a closing `*` span — the nested `StrongEmphasis`'s own `**` marks had no DOM representation at all, not merely a concealed one. This is not a combination-specific fix: `inlineLivePreviewParticipants.ts`'s `revealedMarkerRanges` performs one additional, narrowly-scoped subtree walk (over the *same* Lezer tree, the same `syntaxTree(state)` the main traversal already reads) confined to a small explicit `MARKER_CONSTRUCTS` map — never "any node whose name ends in `Mark`," which would reach block-level mark owners (`HeaderMark`/`QuoteMark`/`ListMark`/`TaskMarker`) this correction does not touch. `return false` at the engaged region root is unchanged and still stops ordinary traversal (no descendant participant renderer runs — no `tok-*` content class, no widget, no atomic range is ever produced inside an engaged region); the subtree walk is a separate, additive-only pass that runs *instead of* resuming that traversal, never *as* it.

Superseded by this correction: `delimitedMarkerOnlyRenderer` and the `ENGAGED_MARKER_RENDERERS` map (both retired; replaced by `MARKER_CONSTRUCTS` + `revealedMarkerRanges` in `inlineLivePreviewParticipants.ts`). See `inlineLivePreviewRegion.test.ts`'s "engaged region represents every nested marker" suite for the regression coverage, including the exact `***bold italic***` case this correction was written for.

---

## Emphasis (Bold/Italic) Live Preview — shipped (2026-08-24)

*Historical implementation record. **Partially superseded** by the nested-visibility ODR above: the traversal-short-circuit mechanism described here is ratified and generalized, but its scoping "for the emphasis family specifically" is withdrawn — that scoping is exactly what the ODR identifies as the defect. `emphasisLivePreview.ts` is slated for consolidation, not preservation.* Implementation: [`emphasisLivePreview.ts`](../apps/app/src/features/markdown/editor/codemirror/highlight/emphasisLivePreview.ts). Committed as `76e657ee` (preceded by the bold-only slice `ee46a3f2` and the italic slice `cfb0141e`); manually verified working in the real app.

- **Bold and Italic are one emphasis family, not two independent constructs.** They are implemented by a single `emphasisLivePreview()` `ViewPlugin` performing one `syntaxTree` traversal that visits both `Emphasis` and `StrongEmphasis` nodes — not two separately-mounted plugins. The earlier `boldLivePreview.ts`/`italicLivePreview.ts` vertical slices (and their test files) were retired in the same commit that introduced the merge.
- **Nested/combined emphasis is made atomic by traversal order, not by a shared range resolver.** `Emphasis`/`StrongEmphasis` always nest with the inner node as the outer's sole non-mark child and zero character gap between them. The traversal's `enter` callback returns `false` when the outer node is engaged, which stops `syntaxTree.iterate` from descending into the inner node at all — so the inner node is never independently visited or evaluated. No coordinator plugin, shared "combined region" computation, or cross-node state was needed for this; it falls out of visiting outer-before-inner and conditionally not descending.
- **`isTokenEngaged` (`semanticToken/tokenEngagement.ts`) was reused unchanged.** Only the range passed to it changed during earlier iteration of this slice (see the file's own doc comment for why full node-range containment, not inner-content-range, was the final choice) — the function itself needed no modification.
- **Implementation finding, specific to this slice — not a general CodeMirror rule:** a 4+-delimiter run (e.g. `****text****`) is valid CommonMark and parses as `StrongEmphasis` nested inside `StrongEmphasis`. Building the decoration set with `RangeSetBuilder` throws, because outer-before-inner tree visitation produces ranges out of the strictly-non-decreasing order `RangeSetBuilder.add` requires. `emphasisLivePreview.ts` instead collects ranges into an array and calls `Decoration.set(ranges, true)`, which sorts internally and tolerates the resulting overlap — the same pattern `liveMarkDecoration.ts` already used for its own nested-construct case. Treat this as evidence to check for when any *other* construct's decorations can nest same-kind (not as a rule that applies universally).
- **Known, deliberately deferred limitation, unchanged from the bold-only slice:** a document whose content is (or ends with) exactly one emphasis construct loads with the caret at `doc.length` (per `createEditorView.ts`'s seeding), which sits on the construct's boundary and is treated as engaged — so that construct stays permanently revealed on load until the user moves the caret. No rule that's a pure function of `(tree, selection)` can distinguish this from "just typed, stay revealed," which is the far more common case and the one the current behavior optimizes for. See `emphasisLivePreview.test.ts` for the pinned regression test documenting this as a known limitation, not silently untested.

## Currently wired vs. dormant (as of 2026-08-24, commit `aee068d0`)

Two inline Live Preview plugins are currently wired in `MarkdownEditor.tsx`: `emphasisLivePreview()` (Emphasis/StrongEmphasis, `76e657ee`) and `strikethroughLivePreview()` (Strikethrough, `aee068d0`). **Both are slated for consolidation into one region-resolution mechanism** per the nested-visibility ODR above — their separation is the cause of the verified nested-reveal defect, not a boundary to preserve.

**`horizontalRuleDecoration()` re-enabled (2026-08-25).** The block-level divider decoration for native CommonMark `---`/`***`/`___` (`HorizontalRule`) and Clutter's own `~---~`/`=---=`/`.---.` variants (`WavyHorizontalRule`/`DoubleHorizontalRule`/`DottedHorizontalRule`) is now wired in `MarkdownEditor.tsx`. Audited against this document's locked decisions at re-enable time: correctly isolated as a standalone block-level `ViewPlugin` (same architecture as `tableDecoration.ts`'s row/alignment decoration, not `inlineLivePreviewRegion.ts`), selection-derived engagement (`isRuleEngaged`, mirroring `isRowEngaged`/`isPhysicalLineEngaged`), no keymap or cursor interception, no `atomicRanges` registration (correctly out of the semantic-token family's scope), no document mutation. No code changes were needed — the existing implementation and tests already satisfied the current contract.

**Blockquote left-rule decoration added (2026-08-25).** `blockquoteLineDecoration()`, a new block-level `Decoration.line` extension wired alongside `blockquoteMarkerDecoration()` in `MarkdownEditor.tsx`, draws the visible vertical rule in front of quoted content that Obsidian/Notion/Craft all have and Clutter previously lacked entirely (marker concealment alone left quoted text visually indistinguishable from a plain indented paragraph). Deliberately kept separate from `blockquoteMarkerDecoration()` rather than folded into it — the two are independent decoration sources over disjoint concerns (marker-text concealment, gated on engagement, vs. always-on block presentation), matching the existing `listMarkerDecoration()`/`listLineDecoration()` split. Line ownership reuses `listLineDecoration.ts`'s exact algorithm (probe each physical line's first non-whitespace position, walk up the syntax tree to the nearest owning node — `Blockquote` here instead of `ListItem`), which for free handles lazy continuation and empty paragraph-separator lines within a quote (still owned by the same `Blockquote`) while correctly stopping at a genuine CommonMark-terminating blank line — no re-derivation of blank-line semantics, the parser's tree is trusted as-is, same principle `blockquoteMarkerDecoration.ts` already established. CSS: `border-inline-start`, not `::before` — a real border on the line box already covers soft-wrapped continuation rows for free, and reuses the existing `--border-strong` token (already the shared "visible rule" color in `MarkdownEditor.css`, used by `.cm-hr-line`/`.cm-table-cell`) rather than introducing a new blockquote-specific token. Nested `>>` quotes get one uniform class, no `--list-depth`-style per-depth counterpart — deliberately not designed until a concrete need exists. No keymap, no `atomicRanges`, no selection/caret involvement; unconditional regardless of the marker's own engagement state. See `blockquoteLineDecoration.ts`'s own doc comment and `blockquoteLineDecoration.test.ts`.

**`blockquoteMarkerDecoration()` re-enabled (2026-08-25).** `>`/`>>` marker concealment for `Blockquote` (native CommonMark, single- and multi-line, lazy continuation, nested `>>`) is now wired in `MarkdownEditor.tsx`. Audited against this document's current contracts at re-enable time: `liveMarkDecoration`/`'physical-line'` engagement (same mechanism as heading), no keymap or cursor interception, no `atomicRanges` registration, no document mutation, and — per the "Duplicate decoration ownership cleanup" audit above — no competing decoration source over `QuoteMark` (`markdownHighlighting()`, the only thing that ever styled it via a second `tok-mark` source, was already retired outright). Blockquote content is ordinary inline content owned by `inlineLivePreviewRegion` exactly like heading content; `blockquoteMarkerDecoration()` only ever touches `QuoteMark`/separator ranges, never inline-content ranges, so the two sources can't collide. Verified via the real mounted app (bold and WikiLink nested inside a blockquote, multi-line engagement/re-collapse) in addition to the existing 9-test suite (`blockquoteMarkerDecoration.test.ts`) and the full regression suite (801 tests). No code changes were needed — wiring-only, same as heading/HR.

**`headingMarkerDecoration()` re-enabled (2026-08-25).** ATX (`# … ######`) and Setext (`Heading\n===`/`Heading\n---`) marker concealment is now wired in `MarkdownEditor.tsx`. This module had no prior entry in either this section's wired or dormant enumeration — an omission, not a record of it being unwired by a deliberate decision; it was simply never imported at all in `MarkdownEditor.tsx` (unlike the constructs below, which are commented out at their call site). Re-enable was wiring-only: no changes to `headingMarkerDecoration.ts`, its 16 existing tests, or its `liveMarkDecoration`/`'physical-line'` engagement mechanism (shared with the still-dormant blockquote/list constructs, per ODR §4.10).

**Heading content classing moved into the shared decoration source (2026-08-25) — correction to the entry below.** *Locked.* `headingHighlighting()`'s own safety claim — "headings have no equivalent competing content-mark source" — was **false**: it checked only `headingMarkerDecoration.ts` (a `Decoration.replace`, no conflict possible) and missed that heading content is ordinary inline content, fully able to contain any `inlineLivePreviewRegion` participant (Highlight, Emphasis, Strong, Strikethrough, Link, ...). Confirmed empirically (mounted `EditorView`, real DOM): `# ==Heading==` produced `<span class="tok-highlight"><span class="tok-heading1">Heading</span></span>` — two *independent* decoration sources (CM6's own `syntaxHighlighting()`/`TreeHighlighter` for `headingHighlighting()`, and `inlineLivePreviewRegion`'s shared `Decoration.set()` for Highlight) splitting/nesting by registration precedence rather than composing correctly, exactly the same failure class this document's own "Shared `DecorationSet` vs. independent CM6 extensions" section already named — just not recognized as applying to headings at the time. The same failure reproduced for `# **Bold** heading` (untested by `headingHighlighting.test.ts`'s existing duplicate-only assertion, which is why it went unnoticed).

**Fix**: `headingHighlighting()` is retired. Heading content classing (`tok-heading1`-`tok-heading6`) is now emitted directly inside `inlineLivePreviewRegion.ts`'s own `buildDecorations`, as a small unconditional branch feeding the *same* `ranges` array every participant already contributes to — not as a `participants` map entry. That distinction matters: the `participants` contract's engagement short-circuit (`isTokenEngaged` on the node's own range → stop descending, nothing inside decorates) is for reveal-on-engage marker-hiding constructs; heading content is never concealed, only the marker is (via `headingMarkerDecoration.ts`'s own independent, `'physical-line'`-scoped mechanism, left unchanged). Registering headings as ordinary participants was prototyped and rejected: it would make the entire heading — including any nested Highlight/Bold/Link — stop decorating and stop independently engaging whenever the cursor is anywhere on that line, which is nearly always while editing a heading. The unconditional branch always emits the class and always keeps descending, so nested participants engage exactly as before.

Range covers the full node (marker included, matching prior `TreeHighlighter` behavior exactly — including the pre-existing "revealed marker renders at heading size" artifact, deliberately not fixed in this pass) with `inclusiveStart`/`inclusiveEnd: true`, required for the exact-range-coincidence case (`# [[Page]]`, `# ==Heading==` alone) — same reasoning as every other wrapping participant in this file. Verified via a real mounted `EditorView` across ATX H1-H6, Setext H1/H2, and every participant kind (Highlight, Bold, Italic, Strikethrough, Link, WikiLink, and Bold-wrapping-WikiLink) nested inside a heading: single correctly-nested span per range, no duplicate wrapper, no sibling-split, nested constructs' own reveal-on-engage behavior fully independent of heading state, marker reveal parity unchanged. `headingMarkerDecoration.ts` and `liveMarkDecoration.ts` were not touched. See `headingContentClassing.test.ts` (replaces the retired `headingHighlighting.test.ts`).

**`headingHighlighting()` added and wired (2026-08-25) — a new, narrower export, not the full `markdownHighlighting()`. Superseded by the correction above — kept for historical context, not current behavior.** Headings now render at their `tok-heading1`-`tok-heading6` font-size/weight in the live app, reusing the exact same tag→class pairs `markdownHighlightStyle.ts` already defined (no new CSS, no new classes — the array of heading tag/class pairs is now a single shared constant both `markdownHighlightStyle` and `headingHighlighting()` consume). **Why not just enable the pre-existing, still-commented-out `markdownHighlighting()`**: confirmed empirically, via a mounted `EditorView`, that doing so produces real DOM duplication for Emphasis/StrongEmphasis/Strikethrough/InlineCode at rest — `inlineLivePreviewParticipants.ts`'s `delimitedInlineRenderer` already wraps each of those constructs' at-rest content in its own same-named `Decoration.mark` (`tok-strong`, etc.), independently of this file; `markdownHighlighting()`'s own `tags.strong`→`tok-strong` rule (etc.) targets the identical range from a second, independent decoration source, and CM6 nests same-range marks from two different extensions rather than merging them (per the "Shared DecorationSet vs independent CM6 extensions" section above) — confirmed as `<span class="tok-strong"><span class="tok-strong">Bold</span></span>` in `view.dom.innerHTML`. Headings have no equivalent competing content-mark source (`headingMarkerDecoration.ts` only ever collapses `HeaderMark` via `Decoration.replace`, never wraps the heading text), so activating heading tags alone via `headingHighlighting()` is safe; activating the full style (which also covers emphasis/strong/strikethrough/highlight/code) is not, and `markdownHighlighting()` itself stays commented out in `createEditorView.ts`, untouched. `headingHighlighting()` also deliberately excludes `tags.processingInstruction` (the shared `tok-mark` rule, which also tags `EmphasisMark`/`StrikethroughMark`/`CodeMark`/`QuoteMark`/`ListMark`/`LinkMark` tree-wide, not just `HeaderMark`) — consequence: the revealed `#`/`===`/`---` marker text renders at the same size/weight as the heading rather than in a muted marker color; a smaller, deliberately deferred gap, not a bug. See `markdownHighlightStyle.ts`'s `headingHighlighting()` doc comment and `headingHighlighting.test.ts` for the DOM-composition regression coverage.

Nearly every other Live Preview decoration extension is commented out — implemented, tested, but not currently rendering in the running editor: `listMarkerDecoration`/`listLineDecoration`/`listIndentWhitespaceDecoration`, `blockquoteMarkerDecoration`, `tableDecoration`, `wikiLinkDecorations`/`wikiLinkMarkerDecorations`, `tagDecorations`, `dateDecorations`. Non-decoration behaviors that don't depend on those decorations — click activation (`wikiLinkMouseHandlers`, `tagMouseHandlers`, `dateMouseHandlers`, `taskCheckboxMouseHandlers`) and autocomplete (`wikiLinkAutocomplete`) — remain wired, since they read the syntax tree directly rather than the rendered widget. (`emphasisMarkerDecoration`/`strikethroughMarkerDecoration` are no longer in this dormant list — retired outright, see "Duplicate decoration ownership cleanup" below, not merely left commented out.)

---

## Duplicate decoration ownership cleanup — Locked (2026-08-25)

Follow-up to the heading composition fix above, prompted by a full source-level audit of every Markdown construct's decoration ownership (requirement: no two independent decoration sources may own the same semantic range with competing `Decoration.mark`s; legitimate nested/different ranges still compose via ordinary CM6 nesting — this was never about flattening `heading → strike → highlight`-style legitimate nesting). The audit found the heading fix closed one *active* collision, not the underlying bug class: several dormant files still carried the same risk, reachable the moment they were wired.

**`markdownHighlighting()`/`markdownHighlightStyle` retired outright (deleted, not left dormant).** Every tag it mapped collided with an already-wired dedicated decoration owner: `heading1`-`6` (the exact bug just fixed for headings — the earlier fix only retired the narrower `headingHighlighting()` export, not the heading tags still spread into the full style via `headingStyleSpecs`), `emphasis`/`strong`/`strikethrough`/`monospace` (already documented), and — previously unnamed — `highlightContentTag` (`Highlight`'s content, attached at parse time by `highlightSyntax.ts`'s own `defineNodes`, mapped to `tok-highlight` here, colliding with `delimitedInlineRenderer('HighlightMark','tok-highlight')` in the already-wired shared set). With every entry independently known to collide, there was no safe remaining ownership to prune down to — retiring the whole file was the smaller, more honest fix than leaving a style definition whose only remaining rule (`tags.processingInstruction`→`tok-mark`) has never been verified safe against the heading/blockquote-marker boundary-coincidence case either. `highlightContentTag`'s `defineNodes` attachment in `highlightSyntax.ts` itself was left in place (now-inert parser metadata, harmless unconsumed, out of scope for this narrowly-scoped cleanup) — a candidate for a later, separate simplification, not a live risk.

**`emphasisMarkerDecoration.ts`/`strikethroughMarkerDecoration.ts` retired outright (deleted, not left dormant).** Their sole job — marker concealment for `Emphasis`/`StrongEmphasis`/`Strikethrough` — is fully subsumed by `inlineLivePreviewRegion`'s shared set (`delimitedInlineRenderer` already emits both the marker-concealing `Decoration.replace` and the content `Decoration.mark` from the one source). Enabling either dormant file alongside the already-wired shared mechanism would have been a redundant `Decoration.replace`-vs-`Decoration.replace` duplicate owner over the identical marker range — harmless in isolation (both just hide the same text) but a genuine double-owner, and an easy trap for a future contributor who reads the dormant file's own doc comment (which describes it as *the* marker-hiding mechanism for that construct, no longer true) and re-enables it thinking it's needed. No coverage was lost: `inlineLivePreviewRegion.test.ts` already independently covers the identical marker-reveal-on-engagement behavior for Emphasis/StrongEmphasis/Strikethrough (it already superseded the equivalent `emphasisLivePreview.test.ts`/`strikethroughLivePreview.test.ts` once before, per the file's own header comment). `liveMarkSelectionSnap.test.ts` — which used `emphasisMarkerDecoration()` as one of two vehicles to exercise the still-real, still-needed `liveMarkSelectionSnap.ts` mechanism — was rewritten to exercise it via heading alone; `liveMarkDecoration.ts`/`liveMarkSelectionSnap.ts` themselves are unchanged and still genuinely needed (heading, and dormant blockquote/list).

**Lists (`listMarkerDecoration.ts`'s plain bullet/ordered `ListMark`) — ownership question resolved as a side effect, no code change needed.** The audit's own finding was `ListMark`'s `Decoration.mark` (`cm-list-marker ...`) colliding with `markdownHighlighting()`'s `tags.processingInstruction`→`tok-mark` (also a `Decoration.mark`, not `replace`, over the identical range — the unsafe mark-vs-mark pattern, unlike every other dormant marker file which uses `replace`). With `markdownHighlighting()` retired, this collision no longer exists — confirmed by construction, not by re-testing an extension that no longer exists to collide. `listMarkerDecoration.ts` itself is unchanged and remains dormant; Lists are still not wired.

**Tables (`tableDecoration.ts`'s `TableCell`) — fixed, still not wired.** `TableCell`'s `Decoration.mark` (`cm-table-cell ...`) is an independent source from `inlineLivePreviewRegion`'s shared set and from `wikiLinkLivePreview` (`Prec.high`). Confirmed empirically, before any fix, mounting `tableDecoration()` + `inlineLivePreviewRegion()` + `wikiLinkLivePreview()` together: `| **Bold** |` produced `<span class="tok-strong"><span class="cm-table-cell">Bold</span></span>` (inverted nesting — `cm-table-cell` lost its own outer wrapper) and `| [[Page]] |` dropped `cm-table-cell` entirely. Fix: `inclusiveStart`/`inclusiveEnd: true` on the `TableCell` mark (same reasoning as every other wrapping participant), plus wrapping the whole `tableDecoration()` extension in `Prec.lowest` — confirmed empirically which precedence direction is actually correct, since it's the *reverse* of the intuitive reading: CM6 nests the *lower*-precedence source as the outer wrapper and the *higher*-precedence source inner (this is exactly why `**[[Page]]**` already nests as `tok-strong > tok-wikilink`: `inlineLivePreviewRegion`, default precedence, is outer; `wikiLinkLivePreview`, `Prec.high`, is inner). `Prec.lowest` (not merely omitting `Prec`) so `TableCell` reliably stays outer regardless of registration order relative to both `inlineLivePreviewRegion` and `wikiLinkLivePreview`, matching the same self-contained-precedence reasoning `wikiLinkLivePreview.ts` already documents for itself. Verified via a real mounted `EditorView` for `| **Bold** |`, `| ==highlight== |`, and `| [[Page]] |` (all three exact-range-coincidence cases named in the audit) plus the full pre-existing `tableDecoration.test.ts` suite (26 tests, all still passing). New containment regression tests added under `tableDecoration.test.ts`'s "cm-table-cell correctly wraps a nested construct" describe block. `tableDecoration()` itself remains commented out in `MarkdownEditor.tsx` — this fixes its decoration definition ahead of ever being wired, it does not wire it.

**Net result — every active semantic range has exactly one decoration owner.** No same-range duplicate-mark collision is reachable in the currently-wired editor. The eliminated dormant sources (`markdownHighlighting()`, `emphasisMarkerDecoration`, `strikethroughMarkerDecoration`) can no longer be accidentally re-wired to reintroduce one, since they no longer exist. The remaining still-dormant constructs (`listMarkerDecoration`/`listLineDecoration`/`listIndentWhitespaceDecoration`, `blockquoteMarkerDecoration`, `tableDecoration`) were individually re-audited against this requirement — List's only identified risk was resolved by `markdownHighlighting()`'s removal; Table's `TableCell` risk was fixed directly; Blockquote has no content-classing mechanism of its own to collide with anything (only its own `QuoteMark`, replace-vs-mark against `tok-mark`, already safe by the same reasoning as heading's own marker).

**No order for re-enabling the rest is currently decided.** The emphasis family was the first slice re-enabled; nothing in this repository commits to which construct comes next, or to re-enabling them in any particular sequence at all. Do not assume WikiLink/Tag/Date/Task/List/Blockquote/Table decorations are live in the running app on the strength of their source code existing, and do not infer a forward implementation order from the fact that emphasis went first — that fact records what happened, not a plan for what happens next. See `docs/editor-feature-matrix.md` for the per-feature current-wiring status.

---

## Standalone-renderer extraction — lessons from the WikiLink split — Locked (2026-08-25)

WikiLink's engaged-state UX (compact alias/filename at rest; raw `[[`/filename/`|alias`/`]]` when engaged, folder prefix always concealed) does not fit `inlineLivePreviewRegion.ts`'s shared reveal-on-engage contract, so it was pulled out into its own standalone extension, [`wikiLinkLivePreview.ts`](../apps/app/src/features/markdown/editor/codemirror/wikilink/wikiLinkLivePreview.ts). Two real regressions surfaced during that extraction, both found by real-DOM investigation after the split shipped, neither anticipated in advance. This section generalizes both as reusable rules for **any future construct that needs standalone rendering outside the shared mechanism** — not a WikiLink-specific bug note.

### Shared `DecorationSet` vs. independent CM6 extensions

- **Decorations produced in one shared `Decoration.set()` compose differently from decorations produced by independent facet/`ViewPlugin` contributions, even with identical ranges.** Range containment alone does not determine DOM nesting — this is not an implementation detail, it's a documented CM6 contract: `Decoration.mark`'s own doc comment (`@codemirror/view`) states nesting order is "determined by precedence of the facet," and elements are "split on... the boundaries of lower-precedence decorations." A mark whose content range exactly coincides with a *lower-precedence* widget's range gets split at that widget's boundary and never wraps it — confirmed empirically: swapping only extension registration order (nothing else) turned a bare `<span class="tok-wikilink">` sibling into the correct `tok-strong > tok-wikilink` nesting.
- **`inclusiveStart`/`inclusiveEnd` and decoration precedence are two separate mechanisms, not one.** The inclusivity flags govern whether a mark's boundary extends to absorb a decoration sitting exactly at that boundary; precedence governs whether the mark survives *unsplit* across a decoration from another source in the first place. Inside the shared traversal (one decoration source, no precedence conflict), the flags alone were sufficient. The moment the widget moved to an independently-registered extension, the flags stayed correct and unchanged — the fix that was actually needed was precedence, not inclusivity. Treat these as independently-verifiable facts about any given composition, never assume one implies the other holds.
- **Default extension precedence is registration order (earlier = higher).** An extraction that must nest inside/around an existing extension should not depend on where `MarkdownEditor.tsx` happens to list it — wrap the extraction itself in `Prec.high`/`Prec.low` so correctness is self-contained in the extension, not an artifact of call-site ordering someone could innocently reorder later.
- **Any future extraction of a renderer into an independent extension must include a real mounted-`EditorView` DOM composition check** (`view.dom.innerHTML` / `.closest()` assertions against the expected nested classes) before being declared correct. A decoration-shape or `Range` unit test cannot see this failure mode — the emitted ranges are byte-identical between the working and broken versions; only the CM6-composed DOM differs.

### Independent engagement semantics

- **Moving a construct outside `inlineLivePreviewRegion` means it no longer automatically participates in the shared engagement decision.** Inside the shared traversal, "region root" (the widest engaged ancestor-or-self) falls out for free from top-down short-circuit: an engaged ancestor's `enter` callback returns `false` before `syntaxTree.iterate` ever descends into a nested participant, so that participant's own engagement is never independently asked while its ancestor is engaged. A standalone extension has no such short-circuit handed to it — it must compute the equivalent boundary itself, deliberately, or it silently reverts to "my own node range only," which is a strictly narrower (and wrong) definition.
- **A child syntax node's range is narrower than its enclosing live-preview region**, by exactly the width of the enclosing construct's own delimiters (e.g. WikiLink's range starts two characters after its enclosing StrongEmphasis's own `**`). This is the ordinary shape of *every* nested construct, not an edge case — it produces a real, reachable caret-position gap (confirmed via a full-position DOM sweep, not a timing/flicker artifact) wherever two independently-computed engagement checks disagree about a position strictly between the two node boundaries.
- **Future standalone renderers must explicitly verify cursor positions at every child boundary, and at "inside enclosing construct but outside child" positions specifically** — not just "cursor inside the child" and "cursor outside everything," which is the pair of cases that looks complete but misses exactly the gap that caused this regression.
- **Widening the engagement boundary must stay generic, never construct-paired.** The correct shape walks the ancestor chain structurally, checking each ancestor's own `firstChild`/`lastChild` for the "two identically-named delimiter children" shape every `delimitedInlineRenderer` participant already shares (`wikiLinkLivePreview.ts`'s `widenToEnclosingLivePreviewRegion`) — never a check that names StrongEmphasis/Strikethrough/etc. by identity. This composes automatically with any current or future delimited construct, with zero new knowledge added to the standalone renderer about what that construct is.

### WikiLink-specific rationale (why the extraction, not a precedent for others)

- WikiLink was extracted because its engaged-state contract is **intentionally different** from the shared marker-hiding contract, not because standalone extensions are generally preferred. At rest: a compact widget (alias, or filename — never the folder-qualified path). Engaged: raw WikiLink syntax (`[[`, filename, optional `|alias`, `]]`) with **only** the folder-prefix concealed — neither the shared contract's "engaged region → fully raw source" nor a second engaged-state hook bolted onto the shared `ParticipantRenderer` contract (explicitly rejected during Phase 3 review of `inlineLivePreviewRegion.ts`).
- **This does not reopen or generalize the shared traversal.** Tag and Date keep the ordinary reveal-on-engage contract unchanged and stay inside `inlineLivePreviewRegion.ts`/`inlineLivePreviewParticipants.ts` for exactly that reason — they still implement the shared contract WikiLink no longer does. A construct extracted for its own genuinely different contract is the narrow exception this section documents, not a precedent for preemptively moving other participants out.

### Standalone-renderer verification checklist

Before declaring any standalone (outside `inlineLivePreviewRegion.ts`) inline-construct renderer correct, verify each item against a real mounted `EditorView` with real dispatched transactions — not unit-level assumptions about decoration shape:

1. **Decoration precedence** — is the extension's precedence (explicit `Prec.high`/`Prec.low`, self-contained in the extension) correct relative to every other extension it must nest inside or around?
2. **DOM nesting** — `view.dom.innerHTML` / `.closest()` checks that the actual composed DOM nests as required, for every enclosing construct and at least one 2+-level nested combination.
3. **Inclusive boundaries** — where a mark from another source must visually wrap this renderer's decoration, confirm `inclusiveStart`/`inclusiveEnd` are set — verified separately from precedence, since one never substitutes for the other.
4. **Nested Markdown** — the renderer's output composes correctly under every enclosing construct it can realistically appear inside, not just the single-level case.
5. **Cursor at construct boundaries** — both boundaries of the renderer's own node, and both boundaries of every enclosing construct it can appear inside.
6. **Cursor inside child** — the renderer's own node's interior.
7. **Cursor inside enclosing construct but outside child** — the specific gap this document exists to name: a real, reachable caret position within an ancestor's range but outside the renderer's own node's range.
8. **Exit behavior** — moving the caret back out returns to the exact at-rest state, with no intermediate/stuck state.
9. **Atomic ranges** — correct at rest, correctly absent while engaged, and — if the renderer computes its own widened engagement boundary — absent across the *entire* widened range, not just the renderer's own node.
10. **Selection** — drag/programmatic selections spanning the construct behave as intended, with no reliance on any removed cursor-interception mechanism (see "CodeMirror owns cursor and selection behavior," above).
11. **Autocomplete** (or any other raw-text-driven mechanism) — confirm it still operates against `EditorState`'s raw document text, unaffected by the renderer's decorations, for both a fresh/in-progress construct and an already-closed one.
12. **Document immutability** — the stored document text never changes as the construct's rendering state changes, across every state transition exercised above.

### Regressions actually encountered

Recorded as the concrete evidence behind the checklist above.

- **Cross-source mark/widget composition regression.** *Symptom*: `**[[Page]]**` rendered the WikiLink widget as a bare DOM sibling with no `tok-strong` wrapper — `inclusiveStart`/`inclusiveEnd` unchanged and still correctly set. *Root cause*: the mark and the widget moved from one shared `Decoration.set()` to two independently-registered extensions; the mark's default (registration-order) precedence was lower than the widget's, so CM6 split the mark at the widget's boundary instead of wrapping it. *Fix*: `Prec.high` wrapping the standalone extension's own returned `Extension`, verified against a test helper that deliberately matches production's registration order (not the test's own, possibly-accidentally-favorable order) — proven necessary by reverting it and observing 8 nesting assertions fail.
- **Independent-engagement boundary-gap regression.** *Symptom*: with the cursor positioned inside the enclosing `**` but not yet inside the WikiLink's own node, `**[[Display text]]**` showed `**Display text**` — an impossible third state, neither at rest nor fully engaged. *Root cause*: the standalone renderer's engagement check used the WikiLink node's own (narrower) range, independently of the enclosing StrongEmphasis's separately-computed engagement — the two-character gap described in "Independent engagement semantics," above. *Fix*: `widenToEnclosingLivePreviewRegion`'s generic ancestor walk, proven necessary by reverting it and observing a full caret-position DOM sweep fail at exactly the predicted gap positions (also surfaced a second latent test bug: the sweep's own "at rest" baseline was itself silently sitting on the pre-existing default-`(0,0)`-selection edge case documented earlier in this file, producing a false pass until corrected).

### A live, already-shipped side effect discovered while investigating `Link` (not yet fixed)

`widenToEnclosingLivePreviewRegion`'s ancestor check (`isDelimitedMarkConstruct`: "two identically-named children whose name ends in `Mark`") assumed each delimited-inline participant has its own **uniquely**-named mark type (`EmphasisMark`/`StrikethroughMark`/`HighlightMark`/`CodeMark` — true for all five current shared participants). `@lezer/markdown`'s `Link`, `Image`, and `Autolink` violate that assumption: all three share one generic `LinkMark` node name for their opening/closing marks, and `Link`'s own first/last child both happen to be named `LinkMark` — satisfying the check even though `Link` is not a registered shared-mechanism participant at all. **Confirmed live today, independent of any `Link` renderer existing yet**: mounting `wikiLinkLivePreview.ts` alone against `[See [[Page]]](https://example.com)` and placing the caret at the `Link`'s own opening `[` (nowhere near the WikiLink) already incorrectly reveals the nested WikiLink's raw `[[Page]]`. Recorded here as a known, named gap in the "generic, no construct names" rule above — the rule needs "and this ancestor is actually a registered delimited-inline participant," not just "structurally resembles one," to be fully correct. Not yet fixed; will become far more commonly triggered once `Link` rendering exists (`[text [[Page]]](url)` is an ordinary, encouraged pattern), so it should be addressed alongside or immediately after `Link` ships, not left as a silent surprise.

---

## Shared live-preview participant contract — confirmed via `Link` (2026-08-25)

Investigated (read-only) and then implemented: `[Display name](https://example.com)` joining `inlineLivePreviewParticipants.ts` as an ordinary shared participant — the deliberately opposite case from WikiLink's extraction, and useful precisely because it confirms the shared mechanism's contract still holds without modification. Implementation: [`linkLivePreview.ts`](../apps/app/src/features/markdown/editor/codemirror/link/linkLivePreview.ts) (or wherever the renderer function lives inside `inlineLivePreviewParticipants.ts` — see that file for the actual registration).

### CM6/Lezer facts this rests on — confirmed empirically, not assumed

- **`@lezer/markdown` provides the parse tree; CM6 itself renders nothing for Markdown links on its own.** `Link`, `Autolink`, and bare `URL` are native Lezer node types (`@lezer/markdown`'s `Type` enum), always present in the parse regardless of Clutter's own grammar extensions — but `@codemirror/lang-markdown`/`@codemirror/view` supply zero decoration, widget, or styling for any of them out of the box. Rendering is entirely the consuming application's responsibility, same as every other construct already in this codebase.
- **`ParticipantRenderer`/`inlineLivePreviewRegion.ts` support `Link` with no contract or traversal change, because the renderer receives the complete `SyntaxNode`.** The shared traversal's only obligation to a participant is `participants.get(node.name)` → call it with the full node; nothing about the traversal or the `ParticipantDecoration`/`ParticipantRenderer` types assumes a particular internal child shape. That assumption lived entirely inside `delimitedInlineRenderer`'s own implementation (a 2-same-named-child precondition), never in the shared contract itself — confirmed by writing `Link`'s renderer as a new, `Link`-specific function that walks its own known shape (first two `LinkMark` children specifically, not `firstChild`/`lastChild`) and registering it exactly like any other map entry.
- **`Decoration.mark` is correct for the label, `Decoration.replace` is correct for the concealed syntax — and `Link` must not be atomic.** The label stays ordinary, character-editable text (a `Decoration.mark` classing it, not replacing it) because `Link`'s contract is "hide Markdown syntax while keeping the user-visible label editable" — this is the same, already-Locked distinction as "plain Markdown links remain ordinary Live Preview marks, not semantic tokens." `Decoration.replace({})` conceals the opening `[` and, as one combined range, everything from the label-closing `]` through the node's end (`](url "title")`, regardless of internal shape — `URL`/`LinkTitle` are swallowed together, never independently rendered). No `atomic` field is returned from `Link`'s renderer, and no `EditorView.atomicRanges` contribution exists for it — confirmed by a negative test. This is the clearest structural difference from WikiLink/Tag/Date, which are atomic at rest by contract; `Link` never is, in any state.
- **Because `Link` stays inside the shared `Decoration.set()`, it does not have WikiLink's independent-extension precedence problem.** `Link`'s content-mark decoration is produced by the same `inlineLivePreviewRegion` traversal, in the same array, as StrongEmphasis/Strikethrough/etc.'s own decorations — there is no second facet/`ViewPlugin` contribution to conflict with, so the precedence-driven mark-splitting failure mode documented above for WikiLink (independent sources, lower-precedence mark split at a higher-precedence widget's boundary) is structurally inapplicable here. This was the central reason to keep `Link` shared rather than extracting it, not merely a convenience.
- **`inclusiveStart`/`inclusiveEnd` on the label's content mark, justified and tested specifically for the nested-widget case.** A WikiLink can legally appear inside a `Link` label (`[See [[Page]]](url)`, confirmed by direct parse) and must visually inherit the label's styling exactly as it already does inside StrongEmphasis/Strikethrough/etc. — same mechanism, same flags, same reasoning as the original WikiLink-inheritance fix, verified again here via a real mounted-DOM nesting check rather than assumed to transfer from the emphasis case.
- **The general CM6 lesson from the WikiLink split, reconfirmed by contrast**: independent decoration sources (separate facet/`ViewPlugin` contributions) can split each other's marks depending on facet precedence — nesting is not guaranteed by range containment alone. Decorations produced by the *same* shared `DecorationSet` never have that cross-extension precedence conflict in the first place, because there's only one source to order. `Link` staying shared is the positive-control confirmation of the rule the WikiLink regressions established.
- **Verified empirically with a real mounted `EditorView`, not source-level reasoning alone**: at-rest concealment (label only visible), engaged reveal (full raw source, from any caret position inside the node — label or URL portion, no distinction needed), exit re-collapse, nested-formatting composition, WikiLink-in-label composition, `Link`-inside-StrongEmphasis DOM nesting, `atomicRanges` absence, and document-text immutability across every transition — all checked against actual `view.dom`/`view.state`, matching the standalone-renderer checklist's own instruction to prefer mounted-DOM evidence over decoration-shape assumptions, even though `Link` is a shared (not standalone) participant.
- **Selection/caret behavior is left entirely to CM6.** No `Link`-specific keymap, no cursor repositioning, no custom selection handling — engagement is purely the existing, unmodified `isTokenEngaged` containment check against `Link`'s own node range, exactly as every other shared participant already works.

### The architectural boundary this confirms

**A construct is a *shared* `inlineLivePreviewRegion` participant when its contract is "hide Markdown syntax while keeping the user-visible content editable, and reveal everything when engaged" — the ordinary reveal-on-engage contract every marker-hiding participant (Emphasis/Strong/Strikethrough/Highlight/InlineCode) already implements, and `Link` now joins.** A construct is *standalone* only when its contract is genuinely different from that — WikiLink's reason for extraction was specifically that it must conceal part of its canonical content (the folder path) even *while engaged*, which the shared "engaged region → fully raw source" contract cannot express at all. `Link` has no such requirement — engaging it correctly reveals everything, with nothing permanently hidden — so it belongs in the shared mechanism by the same test that put WikiLink outside it. This is the concrete, reusable form of "is this construct's contract the same as the shared one, or genuinely different" for deciding where the *next* Markdown construct's renderer belongs — check the contract before checking whether the existing helper functions happen to fit its grammar shape.

Bare `URL`/`Autolink`/`www.` rendering remains a separate, unresolved product/architecture decision (§ above) — nothing in this section decides it, implicitly or otherwise.

---

## Ordinary-marker concealment verification (2026-08-27)

Scope: exactly the five ordinary formatting constructs — Emphasis, StrongEmphasis, Strikethrough, Highlight, InlineCode — after their marker DOM migrated from `Decoration.replace({})` to `Decoration.mark` + `.cm-marker--concealed` (`markdown-dom-structure-agreement.md` §7.1 step 1). This entry reconciles and closes out `docs/obsidian-vs-clutter-editor-audit.md` ("Pass 2") experiment E4 together with §7.2's own verification checklist for this slice — one investigation, not two. **No architecture change.** `Decoration.mark`, real source text, `.cm-marker--concealed { font-size: 0.05px; line-height: 1 }`, no `atomicRanges` for these constructs, and no custom `WidgetType.coordsAt`/hit-testing all remain exactly as they were; this entry records verification outcomes only.

**Real-browser geometry (Safari, live-loaded local build):** `.cm-marker--concealed` spans measured `height: 0`, `width` in the `0.02px`–`0.07px` range depending on marker length — consistent with "measured indistinguishable from 0" in `docs/tauri-webkit-editor-issues.md`. No 30px/24px line-height inflation observed. This reconfirms the existing fix still holds; it does not change the recommendation to keep `0.05px`/`line-height: 1`.

**Backspace/Delete, Home/End, Shift-selection, multi-step edits — PASS, verified against a real mounted `EditorView` using CM6's actual bound command functions** (`deleteCharBackward`, `deleteCharForward`, `cursorLineBoundaryForward`, `cursorLineBoundaryBackward` from `@codemirror/commands`, plus direct `EditorSelection` dispatch for Shift-selection) rather than reimplemented logic — see `highlight/markerBoundaryBehavior.test.ts` (27 assertions, all five constructs). Findings:
- Backspace/Delete at every marker boundary, for all five constructs, deletes exactly the intended single document character — never the wrong side, never a no-op where a delete should occur.
- The specific position strictly between the two opening `*` characters in `**bold**` (document offset 1) is a real, independently addressable position: `EditorSelection.cursor(1)` lands there, and Backspace/Delete from it each remove exactly one `*`, leaving `*bold**`/`*bold**` respectively — the paired-marker case Pass 2's own §8.2 flagged as unverified for "drag-selection and Backspace-at-boundary" (`markdown-dom-structure-agreement.md` §5.5 item 5) is now covered for the keyboard/command side.
- Home/End (`cursorLineBoundaryForward`/`Backward`) from inside, before, and after a concealed marker run land at the true line boundary every time, never at a marker edge.
- Shift-selection endpoints (`EditorSelection.range`) spanning a marker boundary, or the whole construct, are preserved exactly as dispatched — never silently snapped.
- Multi-step sequences — typing a construct character-by-character, engaging/disengaging/re-engaging across several selection moves, deleting a construct down to an empty shell then into its markers, and editing one construct without perturbing a sibling's decorations — all recompute `cm-marker`/`cm-marker--concealed` correctly at every intermediate step.

**Mouse hit-testing at the concealed marker's own (sub-pixel) position — INCONCLUSIVE, not a demonstrated defect.** A fully-trusted OS-level mouse click (the methodology `docs/tauri-webkit-editor-issues.md` established as necessary for this class of bug) could not be obtained in this pass — macOS's Accessibility permission for UI-scripting `System Events` was not available and required a user-granted system dialog outside this pass's reach. As a partial substitute, real Safari's `elementFromPoint`/`caretRangeFromPoint` were queried directly at and around marker boundaries: results were consistent with expectations (clicks in the immediate vicinity of the ~0.05px-wide marker region resolve to one of the marker's two ends, never to a position that doesn't exist), but this is a lower-confidence signal than a genuine trusted click and does not fully settle Pass 2's E4/E2-adjacent question. **This is not a new defect and not a regression**: a target smaller than any pointing device's addressable resolution was already the case conceptually for the prior `Decoration.replace({})` behavior (zero rendered pixels), and the invariant this migration must preserve — every position addressable via keyboard — is proven above. Remaining open: full trusted-click confirmation, once OS Accessibility permission is available.

**Accessibility — INCONCLUSIVE, flagged, not fixed.** No real screen reader (VoiceOver or otherwise) was run in this pass. Source inspection confirms: no `aria-hidden` or other accessibility accommodation exists anywhere on `.cm-marker--concealed` or its construction path. This means the migration from `Decoration.replace({})` (which removed marker text from the DOM, and therefore from the accessibility tree, entirely) to `Decoration.mark` (which keeps the real marker text, e.g. `**`, `~~`, `` ` ``, in the DOM, merely styled to near-zero size) is a **plausible, structurally-confirmed difference in what is exposed to assistive technology** — the marker punctuation may now be announced where it previously could not be. This was not tested against a real screen reader and no `aria-hidden` was added speculatively, per instruction: fix only a demonstrated problem, not a theoretical one. Reconfirms Pass 2's own decision-table row ("Accessibility contract for markers: DESIGN — new work, UNKNOWN both systems") — still open, now with an added reason (a real, migration-caused exposure change, not just an existing unsolved gap) to prioritize a real screen-reader pass before this slice is considered fully closed.

**Performance — PASS, no regression, not optimized further.** A 5,000-line, 500K-character document with all five constructs on every line: initial decoration build ~93ms, ~1.07ms average per caret-move recompute (50 samples), only viewport-visible markers materialized (360 of many thousands in the full document — confirms `view.visibleRanges` correctly scopes the work). No established prior baseline existed to compare against; this measurement is now the baseline for future comparison. See `highlight/markerDecorationPerf.test.ts`.

**Explicitly out of scope for this entry** (per instruction, deferred to separate follow-up passes, not folded in): Pass 2's E1 (`font-size: 0` wired on `.cm-hr-line`/`.cm-table-align-row`, a possible live defect on horizontal rules and tables — unrelated to ordinary markers) and E2 (`liveMarkSelectionSnap`'s continued relevance to blockquote, now that blockquote uses `color: transparent` rather than zero-width `Decoration.replace`). Pass 2's E6 (build a throwaway widget branch to settle the marker-primitive question) is superseded, not run: the CM6-source-level research in this session's own "CM6-native concealment research" pass answered the same question `docs/obsidian-vs-clutter-editor-audit.md` §8.2 already predicted, without needing a throwaway branch. E10 (deep Obsidian DOM capture) was not run — moot, per the same research pass's conclusion that Obsidian implementation details aren't the deciding factor here.

---

## Blockquote DOM restructure and the cross-construct DOM Structure Agreement (2026-08-27)

Blockquote's selection-boundary bug (multi-line selections crossing a quote/normal-line boundary misaligned, root-caused to `.cm-quote-line`'s `padding-inline-start` giving quote lines a different content-left than normal lines — full trace in this session's own investigation) was fixed by moving off `.cm-line`-level gutter geometry entirely: `blockquoteMarkerDecoration.ts` now wraps the real `QuoteMark` (+ separator, merged into one range) in a `Decoration.mark`, concealed via a `--concealed` class rather than `Decoration.replace`, so the marker's own real width — not line padding — reserves the gutter. This is a **deliberate exception** to the hide-via-`Decoration.replace` pattern every other marker-hiding construct in this codebase uses (see `liveMarkDecoration.ts`'s own doc comment for why that pattern was chosen originally); blockquote needed a different mechanism specifically because it could no longer use `.cm-line` padding. Full rationale, verification, and known gaps (wrapped-row indentation is not solved — flagged, not hidden) are in `blockquoteMarkerDecoration.ts`/`blockquoteLineDecoration.ts`'s own doc comments and this session's reports, not duplicated here.

This produced a general **DOM Structure Agreement** — marker/content always separate elements, marker is real styled text never a widget, `.cm-line` never provides marker geometry — plus a from-source audit of heading and list against it, and a proposed (not yet approved) Phase 2 migration order. See [`docs/markdown-dom-structure-agreement.md`](markdown-dom-structure-agreement.md) for the full document. Heading does not conform (`Decoration.replace({})`, no persistent marker span) but is lower-risk to migrate than list, which does not conform in two ways (`.cm-list-line`'s own `padding-left`/`text-indent` hanging indent, and task checkboxes' permanent-widget model) and is currently dormant (commented out in `MarkdownEditor.tsx`). No Phase 2 work has started.

---

## `syncMarkdownIntoView` excluded from CM6 undo history (2026-08-27)

**Bug (confirmed, not theoretical):** `syncMarkdownIntoView` (`createEditorView.ts`) — dispatched by `MarkdownEditor.tsx`'s own effect whenever the `markdown` prop changes while the CM6 view is unfocused — was a plain, undo-eligible transaction. Its real callers are external, same-page mutations that don't go through this editor's own `onDocChange` at all: `PageOperations.mutateBody()` (task-checkbox toggles from a different UI surface, template insertion, any future bulk/AI-assisted edit per its own doc comment) commits directly into the page's open `DocumentSession`, which round-trips back into `MarkdownEditor` as a changed `markdown` prop. If the editor is unfocused when that happens, `syncMarkdownIntoView` applies the change to keep the document in sync — correctly — but until this fix, that transaction was indistinguishable from a real user edit to CM6's `history()` extension.

**Reproduced directly against CM6's own `undo`/`redo`/`undoDepth` commands** (`createEditorView.test.ts`'s new `syncMarkdownIntoView — external syncs must not corrupt undo history` suite) — no UI or real browser needed, since this is a CM6 history-mechanics question, not a rendering one; `createEditorView()`/`syncMarkdownIntoView()` used unmodified, real production code, not a reimplementation. Two distinct, independently-confirmed failure modes:
- **Unannotated (the pre-fix state):** dispatching an external sync immediately after one real user edit did not create a *second* undo-able step — CM6's default history grouping silently merged it into the *same* group as the preceding user edit (`undoDepth` stayed at `1`, not `2`). A single subsequent `undo()` reverted the user's own edit *and* the unrelated external change together, in one step the user never asked for.
- **`Transaction.addToHistory.of(false)` alone, keeping the previous full-document-replace change shape** (`{from: 0, to: currentDoc.length, insert: markdown}`): excluding a transaction from history does not exempt it from history's change-*mapping* — a still-open undo entry from *before* it has its recorded positions mapped through it regardless. A full-document replace maps every prior position through "the entire document was deleted and something new inserted," which degenerates the mapped entry into an empty/meaningless change: `undoDepth` dropped to `0` immediately after the sync, permanently losing the user's own unsaved edit from undo, with no way to recover it.

**Fix — two changes together, each addressing one of the two failure modes above, not applied as an unexamined pair:**
1. `Transaction.addToHistory.of(false)` on the dispatch, alongside the pre-existing `externalSync` annotation (which serves a completely different, unrelated purpose — telling the update listener not to re-fire `onDocChange` for this transaction, preventing a feedback loop; a transaction can need either annotation, or — as here — both, for independent reasons).
2. The change shape itself switched from a full-document replace to a minimal common-prefix/common-suffix diff (`minimalReplaceChange`) — deliberately not a general (multi-hunk) diff algorithm, since every real caller of this function makes one small, localized edit to an otherwise-unchanged document (a task-checkbox toggle is the concrete case), which a prefix/suffix diff finds exactly and cheaply. This is what lets CM6's `ChangeSet.map` carry a prior, unrelated user edit's positions through the sync intact, confirmed directly: `undoDepth` stays at `1` through the sync, `undo()` reverts only the user's own edit (leaving the externally-synced content in place), and `redo()` correctly restores it afterward. Repeated localized syncs (several task toggles in a row) do not accumulate separate history entries either, confirmed the same way.

**History is per-note by construction, already, unrelated to this fix — verified, not inferred.** `PageHost.tsx` renders `<MarkdownEditor key={activePageId} ...>` — React's `key` forces a full unmount/remount of `MarkdownEditor` (and therefore a `view.destroy()` + brand-new `createEditorView()` call, per that component's own mount-effect cleanup) on every page switch. `syncMarkdownIntoView` can only ever run against the currently-mounted view for the currently-active page — there is no code path where it (or anything else) could carry one note's CM6 `historyField` state into a different note's `EditorState`, since each note's `EditorState` (and the `history()` extension's data living inside it) is destroyed and freshly recreated on every switch. This fix does not change that property — it was already true before it, confirmed by testing the pre-fix `syncMarkdownIntoView` the same way — and does not introduce any note-switching-specific isolation logic, because none was needed. Verified live in the real Clutter webapp (browser pane) as well as in the source-level test: edit note A, switch to note B (note A's edit persisted correctly in the sidebar), edit note B, switch back to note A (note A showed exactly its own prior content, not note B's and not empty), edit note A again, undo (reverted only the post-switch edit, landing on the pre-switch content), redo (restored it) — all correct.

**Explicitly not investigated further, out of scope for this fix:** a separate, still-unexplained anomaly where `Cmd+Shift+Z` (redo) after a `Cmd+A`-select-all-then-retype-then-`Cmd+Z` sequence produced an empty document in this session's browser-pane testing — confirmed *not* caused by `syncMarkdownIntoView` (the view never lost focus during that reproduction, so the sync path this fix addresses was never invoked) and confirmed still present after this fix (retested live, same result). Not chased further per explicit instruction; flagged here only so a future investigation doesn't assume this fix was expected to resolve it.

**Files changed:** `createEditorView.ts` (`minimalReplaceChange`, `syncMarkdownIntoView`'s annotations); `createEditorView.test.ts` (new `syncMarkdownIntoView` suite — 8 tests: user edit enters history/undo/redo; sync alone never undo-able; undo-after-sync reverts the user's edit not the sync; redo-after-that-undo; repeated syncs don't accumulate; a normal edit after a sync still enters history; a no-op sync (matching content) dispatches nothing; note-switching isolation). No other files touched — concealment decorations, engagement/reveal logic, Markdown parsing, and selection behavior are all unrelated to this fix and were not modified.

---

## Per-document CM6 undo/redo history preservation (2026-08-27, later same day)

**What was true before this entry, confirmed by source reading, not assumption:** `PageHost.tsx` renders `<MarkdownEditor key={activePageId} ...>`. React's `key` forces a full unmount/remount on every page switch, which runs `MarkdownEditor`'s own mount-effect cleanup (`view.destroy()`) followed by a fresh `createEditorView()` call for the new page — a brand-new `EditorState`, and therefore a brand-new (empty) `history()` `StateField`, every single time. This is *why* undo/redo "disappears" on a page switch — not a bug in the previous entry's fix, not history being incorrectly cleared, just nothing anywhere preserving the `EditorState` (and the `historyField` data living inside it) across the destroy.

**CM6's own, official mechanism for exactly this, verified end-to-end before writing any production code (a throwaway jsdom test against the real installed `@codemirror/state`/`commands`/`view`, deleted after use):** `historyField`'s own doc comment (`@codemirror/commands`) states it should be used "when you want to serialize or deserialize state objects in a way that preserves history." `EditorState.toJSON({history: historyField})` / `EditorState.fromJSON(json, config, {history: historyField})` round-tripped a document's full undo/redo stack (and its selection — already core `EditorState`, included automatically) with zero cross-document leakage between two unrelated documents' snapshots, confirmed directly by asserting on `undoDepth`/`undo()`'s actual resulting document text, not just that serialization succeeded.

**Implementation — the smallest change consistent with the existing architecture, per explicit instruction: keep the existing one-`EditorView`-per-mount, remount-per-`key` model; no persistent-view/`EditorView.setState()` architecture (a documented, viable alternative — CM6's own doc comment: "use it when the new state isn't derived from the old state" — but a larger, separate change not made here).**
- `editorHistoryCache.ts` (new): a plain `pageId -> unknown` (serialized `EditorState`) `Map`, module-level, in-memory/session-lifetime only — not a persistence mechanism, `durability-model.md`'s stages are untouched. Deliberately no eviction/LRU: a stale entry for a page that's gone is inert (nothing will ever look it up by that id again), and wiring `PageOperations.delete()` to invalidate it would need this feature-layer module reachable from `core/application`, the wrong dependency direction for this codebase — noted as a known, accepted limitation, not solved here.
- `createEditorView.ts`: new `restoreHistoryJSON?: unknown` option. When supplied, `EditorState.fromJSON` is used instead of `EditorState.create` — **but only if the snapshot's own embedded document text still matches the `doc` being opened with** (`docTextMatches`); otherwise silently falls back to a fresh state, exactly like any ordinary cache miss. New `serializeEditorHistory(view)` export — the write side, `view.state.toJSON({history: historyField})`.
- `MarkdownEditor.tsx`: the mount effect now passes `restoreHistoryJSON: getCachedEditorHistory(pageId)` into `createEditorView`; the unmount cleanup calls `setCachedEditorHistory(pageId, serializeEditorHistory(view))` immediately before `view.destroy()`. `MarkdownEditorProps` gained a required `pageId: string`, read explicitly from `PageHost.tsx`'s own `activePageId` (the same value already used for React's `key`) at both call sites — deliberately not inferred from the `key` prop itself (a private React implementation detail) or from `markdown` content (not a stable identity — two pages can share text, and a page's own text changes on every edit).

**How external-sync-vs-restored-session safety is guaranteed — the `docTextMatches` check above is the entire mechanism, not a separate one.** The risk named before implementing: a page's persisted content can legitimately change while it's closed (`PageOperations.mutateBody()` — a task toggle from a different UI surface, acting on a page whose `DocumentSession` stays open even though its editor isn't mounted). If that happens, the cached snapshot's embedded document (from when the page was last open) no longer matches the fresh `markdown` prop on reopen. `docTextMatches` catches this by construction: a mismatched snapshot is never handed to `EditorState.fromJSON` at all, so there is no path where a restored (stale) document could be silently shown, and no path where a subsequent `syncMarkdownIntoView` correction (unfocused-prop-sync effect) could run against an already-restored-but-now-wrong history and corrupt it — the mismatch is caught *before* any restoration is attempted, not patched up after. Confirmed directly (test F below): a mismatched cache entry produces exactly the same outcome as no cache entry at all — current, correct content, clean empty history.

**Regression coverage** (`createEditorView.test.ts`'s new "Per-document undo/redo history preservation" suite, 6 tests, scenarios A–F as specified): history survives a page switch and only the leaving page's own edit undoes on return (A); histories are independent in both directions (B); multiple undo/redo steps survive a round trip through another page, using explicit `Transaction.time` annotations to force separate history groups since same-tick test dispatches otherwise merge under CM6's default `newGroupDelay` (C); a page with no cached entry starts clean (D); selection/caret is restored alongside history (E); a cache entry whose document no longer matches the incoming prop is never used (F, the external-sync-safety case above).

**A real bug this test suite itself caught, before it ever reached the app — worth recording as a specific, cautionary data point, not just "tests passed":** the first version of `docTextMatches` assumed `EditorState.toJSON()`'s `doc` field was an array of lines (`Text.toJSON()`'s own, different shape), and joined it with `\n` before comparing. It's actually a plain string — `doc: this.sliceDoc()`, confirmed directly in `@codemirror/state`'s source, which also confirms `EditorState.fromJSON` itself validates `typeof json.doc === "string"` and throws otherwise. The array-shaped check silently returned `false` on every call, meaning `restoreHistoryJSON` was *always* rejected — no restoration ever happened, in either direction, on any page. Every one of the new "Per-document" tests failed identically (`undoDepth` stuck at 0, selection stuck at the fresh-state default) until this was caught and fixed. Flagged here specifically because it's exactly the kind of bug that a design review or a "does the API shape look right" read would not have caught — it required running the actual restore path against a real `EditorState` to surface.

**Verified live in the real Clutter webapp** (browser pane): edited today's daily note (page A) across three separately-typed edits, switched to tomorrow's daily note (page B, confirmed via sidebar that A's edit persisted correctly), edited B, switched back to A (content exactly A's own — never B's, never empty), undid twice (each step removed exactly one of A's own edits, landing on the correct intermediate content each time), switched to B and undid there (removed exactly B's own edit, independently of anything done to A). Selection/caret restoration was not independently re-verified pixel-visually in this browser session (clicking to focus the editor for keyboard commands necessarily moves the caret, confounding a purely visual check) — relied on jsdom test E for that specific claim, which asserts on `EditorState.selection` directly rather than rendered caret position, and is unaffected by that confound.

**Redo (`Cmd+Shift+Z`) was not used to verify this feature**, per explicit instruction — a separate, still-unexplained, environment-specific anomaly (this browser pane specifically; flagged in the previous entry, confirmed pre-existing and unrelated to `syncMarkdownIntoView`) reproduced again during this session's testing, on *both* pages, confirming it's a general quirk of this testing environment and not something newly introduced by (or specific to) history preservation. Not chased further, per instruction.

**Known limitations at the end of that pass, since resolved by the entry below unless noted:**
- ~~No eviction policy~~ — deliberate, kept as-is (see below).
- ~~No page-deletion hook~~ — added (see below).
- ~~Scroll position out of scope~~ — added (see below).
- The cache is in-memory/session-only by design — closing and reopening the app loses all preserved history, same as today's baseline (this feature can only improve on the current behavior within one session, never regress it). Still true; unchanged.

## Per-page editor session model completed: scroll restoration, deletion invalidation, cache policy (2026-08-28)

Closes out the remaining items from the previous entry. Renamed `editorHistoryCache.ts`'s cache value from a bare history blob to `CachedEditorSession { historyJSON, scrollEffect, domScrollTop }` — same module, same `pageId -> Map` shape, extended rather than replaced.

**`EditorView.scrollSnapshot()` does not work for scroll restoration in Clutter's actual layout — confirmed by direct measurement, not assumed from the API shape.** `scrollSnapshot()`/`EditorViewConfig.scrollTo` operate exclusively on `view.scrollDOM` (CM6's own `.cm-scroller`; its doc comment says as much: "only affects the editor's own scrollable element, not parents"). But Clutter's editor uses `EditorView.lineWrapping`, so `.cm-scroller` renders at full content height and is never itself the scrolling element — the *page shell's* container (`Page.tsx`'s `.page__content`, `overflow-y: auto`) is what actually scrolls. Measured directly in the real app: scrolling a 40-line document left `.cm-scroller.scrollTop` at `0` throughout, while `.page__content.scrollTop` moved to `409.5`. `scrollSnapshot()`/`restoreScrollEffect` were kept in `createEditorView.ts` anyway (harmless, correct per documented contract, gated behind the same `docTextMatches` staleness check as history — "restored as one unit or not at all") but a second, working mechanism was required to deliver actual scroll restoration.

**`domScrollTop` — the second, working mechanism.** `MarkdownEditor.tsx` adds a generic `findScrollableAncestor(el)` helper (walks up from the mount container checking computed `overflow-y` for `auto`/`scroll` — not hardcoded to `.page__content`'s class name, to avoid coupling this feature module to `Page.tsx`'s DOM structure) resolved once at mount into a ref. On unmount, the resolved ancestor's `scrollTop` is captured into the cache alongside `historyJSON`; on mount, if `domScrollTop !== undefined` and `docTextMatches` passes, it's applied directly (`ancestor.scrollTop = cached.domScrollTop`).

**Why capture is polling-based (`setInterval`, 300ms), not a `scroll` event listener — a debugging finding, not a stylistic choice.** A listener-based "last known scrollTop" was tried first and rejected: React's commit phase mutates the DOM (removing the outgoing page's content, inserting the incoming page's, typically much shorter) *before* running the outgoing component's unmount cleanup. The browser auto-clamps the ancestor's `scrollTop` to the new, shorter `scrollHeight` as part of that same synchronous mutation — so by the time cleanup runs, a plain "read `scrollTop` now" is already reading the post-clamp value, and a `scroll`-event-driven "last known" ref is exactly as vulnerable, since the clamp itself fires a `scroll` event that a listener would dutifully record, overwriting the real pre-switch value, before cleanup ever gets to read it. Polling avoids this structurally: the interval is cleared as the *first* line of cleanup, so no poll can ever observe a post-clamp value — polling stops before the clamp-inducing mutation for that page's unmount has a chance to be sampled again. (A synthetic-dispatch differential test in this session's browser tooling also found that JS-driven `scrollTop =` assignment doesn't reliably fire native `scroll` events in that specific tool/environment, which independently ruled out verifying a listener-based approach here — but the structural race above is the real reason for polling, not a workaround for that tooling gap.)

**Deletion cache invalidation — `PageHost.tsx`'s `onDelete`, not `PageOperations.delete()`.** `clearCachedEditorSession(pageId)` is called directly in `onDelete` before `application.pageOperations.delete(activePageId)`. This feature-layer cache module cannot be imported from `core/application` without inverting the enforced dependency direction; `PageHost.tsx` (app layer) already legitimately depends on both `core` (via `application.pageOperations`) and this feature's `MarkdownEditor`, so it's the natural existing bridge — no new event/subscription mechanism added. Deliberately *not* wired into `onArchive`/`onRestore`: those are reversible, and an archived page's prior editing session is still meaningful if it comes back. Known, accepted race, not solved: `pageOperations.delete()` is async and typically triggers navigation away, which unmounts the deleted page's `MarkdownEditor` — whose own cleanup calls `setCachedEditorSession` again, *after* this clear. If that unmount lands after the clear, the entry is silently repopulated — judged harmless, since a deleted page's id is never looked up again through normal navigation, so the race can only ever make this best-effort cleanup a no-op, never cause incorrect behavior.

**Cache memory policy: still a plain `Map`, no eviction — a confirmed decision, not a deferred one.** Assessed directly rather than left as a stale TODO: a real editing session opens at most a handful to a few dozen distinct pages, and each entry's dominant cost (the `historyJSON` blob) is proportional to how much edit history *that page* accumulated this session, not document size. `clearCachedEditorSession` on deletion is judged sufficient eviction for today's usage; an LRU would be speculative complexity against a scenario (thousands of pages touched in one never-reloaded session) that hasn't been observed.

**Regression coverage added** (`createEditorView.test.ts`, tests G–J, alongside the existing A–F): `domScrollTop` round-trips through the cache unchanged (G); a stale doc-mismatched entry discards `domScrollTop` alongside history — no partial restore (H); `clearCachedEditorSession` (mirroring `PageHost.tsx`'s `onDelete`) actually clears an entry, and a reopen under the same id starts fully fresh (I); rapid A→B→A→B switching produces no cross-contamination, confirming React's synchronous unmount-before-mount ordering holds regardless of switch speed (J). All 22 tests in the "Per-document" suite pass. Polling/DOM-ancestor logic in `MarkdownEditor.tsx` itself is not separately unit-tested — inherently DOM-layout/timing-dependent in a way jsdom doesn't reproduce (no real scroll/layout) — verified instead in the real browser, per project convention for editor/UI behavior.

**Verified live in the real Clutter webapp:** placed the caret mid-word on a specific line of a multi-line document, switched to the next day's note and back — `window.getSelection()` confirmed the DOM caret landed at the exact same character offset in the same line afterward, and a screenshot confirmed it visually. Set `.page__content.scrollTop` to a specific value on a 40-line document, switched away and back — `scrollTop` read back the same value afterward. Concealed-widget rebuild after restoration was visually confirmed correct throughout (no stale/duplicated decorations on remount). React StrictMode double-invoke and rapid page-switching races are covered by jsdom test J; no additional real-browser-specific race was found.

**Redo (`Cmd+Shift+Z`) anomaly:** not investigated in this pass either, per standing instruction; not encountered this session (no redo testing was performed).

**Remaining limitations, stated explicitly:**
- The cache is still in-memory/session-only — an app reload loses all preserved sessions (history, scroll, selection), matching baseline behavior, never regressing it.
- `domScrollTop` restore does not itself re-scroll to keep the restored caret in view if the two conflict (e.g., a very different-length document reopened with a stale caret position) — not a scenario this pass needed to handle, since both are gated by the same `docTextMatches` document-identity check and so only ever restore together, for the same document text.
- No eviction beyond deletion, by design (see above) — worth revisiting only if a real long session demonstrates unbounded growth actually matters.

## Focus restoration — Superseded (2026-08-28, later same day)

**Superseded by "Note-open focus policy" below, same day.** `wasFocused`-gated restoration (preserve whatever focus state a page had when last closed) was replaced with a different, product-directed rule: an *established* editing session (real prior engagement, not just "was focused") always focuses on reopen regardless of prior focus state, and a fresh/no-session page defers to the same open-time "empty title → title focus" policy `Page.tsx` already applies. `wasFocused` was removed from `CachedEditorSession` entirely — kept here, not deleted, per this doc's own "Superseded" convention (Status key above): so nobody re-discovers the same dead end. The investigation and verification methodology below are still accurate and still relevant background; only the specific gating rule they led to was replaced.

**Investigated first, per instruction, before writing any code — the question was genuinely open, not assumed.** Traced the full open-note lifecycle (sidebar click → `activePageId` change → `PageHost` remount → `MarkdownEditor` mount → `createEditorView` → `EditorState.fromJSON`) and grepped the entire app for every `.focus()`/`autoFocus` call. Finding: **no code path anywhere in the app ever focused the editor on note-open, before or after the per-document session work** — the only existing focus-transfer is `Page.tsx`'s `PageTitle.onSubmit` (Enter in the title advances focus into the body), a distinct, deliberate interaction unrelated to opening/reopening a note. Confirmed live in the browser across single-hop (A→B→A) and multi-hop (A→B→A→C→A) switching: `EditorState`'s restored selection was always correct internally (confirmed via `window.getSelection()` after programmatically focusing), but DOM/`EditorView` focus never returned on its own — `document.activeElement` stayed on whatever sidebar control was last clicked, consistently, not intermittently. **Conclusion at that point: expected/pre-existing behavior (not a regression the per-document work introduced), but a genuine product-behavior gap** — restoring a caret that isn't visibly active is confusing in a way "no caret at all" never was. Presented both options; product direction confirmed: implement `wasFocused`-gated restoration.

**Implementation — extends the existing session cache and polling mechanism rather than adding new machinery.** `CachedEditorSession` (`editorHistoryCache.ts`) gains `wasFocused: boolean`. `MarkdownEditor.tsx`'s mount effect calls `view.focus()` immediately after the scroll restore, gated on `cachedSession?.wasFocused && docTextMatches(cachedSession.historyJSON, markdown)` — the identical staleness gate already used for history/scroll ("restored as one unit or not at all"); a fresh page (no cache entry) or a stale one never autofocuses.

**Why focus capture reuses `scrollPollInterval` rather than a `focus`/`blur` listener pair — the same class of race as `domScrollTop`, confirmed by reasoning through the actual click sequence, not assumed by analogy.** The click that switches pages (e.g. a sidebar item) synchronously blurs the outgoing editor as a *native* side effect of that click — before React's own state update, before the remount, before this component's unmount cleanup ever runs. A live `view.hasFocus` read at unmount, or a `blur` listener that sets a ref to `false`, would therefore always read `false` at exactly the moment that matters, regardless of whether the user was genuinely typing in the editor a second earlier — the blur is inherent to the switching gesture itself, not a symptom of anything. The fix is the same one already proven for scroll: sample `view.hasFocus` on the existing 300ms poll into `lastKnownFocusRef`, and read that *last-known* value (not a live one) at unmount, after the interval is cleared. This also correctly handles the secondary case of the user blurring the editor *without* switching pages (e.g. clicking into the page title) and then switching later — the poll has time to catch that intermediate blur before the eventual switch, so `wasFocused` still reflects "was the editor the last thing focused right before leaving," not just "was it ever focused during this mount." A pathologically fast blur-then-switch (under one 300ms tick) could theoretically still read stale — accepted, same documented tolerance already recorded for scroll's own polling.

**Regression coverage** (`createEditorView.test.ts`, tests K–O, alongside the existing A–J): a focused editor regains focus and its selection on return (K); an unfocused editor stays unfocused, proving selection restoration and focus restoration are independent (L); a page with no cached session never autofocuses (M); focus state doesn't leak between pages in either direction (N); a stale (doc-mismatched) cache entry's `wasFocused` is never honored, following the same all-or-nothing gate as history/scroll (O). Both `openPage`/`closePage` test helpers were updated to mirror the real mount/unmount code exactly (`openPage` now conditionally calls `view.focus()`, matching `MarkdownEditor.tsx`'s own gate). 32 tests total in the "Per-document" suite pass.

**Verified live in the real Clutter webapp:** typed a multi-line note, clicked mid-word into "Charlie," confirmed focused (`cm-focused` class, `document.activeElement` on `.cm-content`), switched to the next day's note and back — **without any click**, `document.activeElement`/`cm-focused` confirmed the editor regained real DOM focus on its own, `window.getSelection()` confirmed the caret at the exact same offset, and a screenshot confirmed a visible blinking caret rendered after "Charlie." Verified the negative case symmetrically: a page that was opened but never clicked into stayed unfocused (`activeElement` remained the sidebar control) after being switched away from and back. Verified isolation across a longer hop sequence (A focused → B never-focused → C → B → A) — A's focus and B's non-focus each held independently at every step, no leakage in either direction.

**A render-timing detail worth recording, not a bug:** immediately after `view.focus()` is called in the mount effect, `document.activeElement`/the `cm-focused` class are correct right away, but the actual `.cm-cursor-primary` blink-layer DOM node takes one additional CM6 measure/layout pass (a `requestAnimationFrame` tick) to appear — confirmed directly (present after a ~1s wait, absent immediately before it). Not a defect: `EditorView.focus()`'s own contract is about DOM/model focus, not a synchronous guarantee that every visual layer has repainted, and a real browser's own paint cadence means a user never perceives this gap. Not worth engineering around for a sub-frame timing detail.

**Remaining focus edge cases, stated explicitly (historical — see the entry below for what actually shipped):**
- The sub-300ms blur-then-switch race described above (accepted, matching scroll's own documented tolerance).
- Focus restoration is strictly binary (`wasFocused`) — it does not also restore *which* of several possible in-page focus targets was active (there is only one: the editor's `contentDOM`; a future page type with multiple focusable regions would need a richer model, not needed today).
- Archived/restored pages: `wasFocused` is captured and read exactly like history/scroll (i.e., it survives archive/restore, since nothing clears it there — consistent with the existing "archived pages keep their editing session" decision above).

## Note-open focus policy (2026-08-28, later same day)

**Product direction, stated explicitly, that motivated replacing the entry above:** when a user explicitly opens a note, they should be able to start editing immediately. `wasFocused`-gated restoration didn't deliver that for the common case of an existing, previously-edited note that simply wasn't focused the instant it was last closed (e.g. the user scrolled it via the sidebar, or blurred it before navigating away) — reopening it left the editor unfocused even though the note plainly has an established body worth continuing to edit. The fix is a priority rule, not a bigger cache:

1. **A restorable cached session with real, established prior engagement always focuses the editor** — regardless of `wasFocused`'s old per-close snapshot (that field is gone; this is a stronger, simpler replacement, not an addition to it).
2. **Otherwise, `focusOnOpen`** — a flag `PageHost.tsx` computes at the moment it renders `<MarkdownEditor>`, mirroring the exact same "empty title → title gets focus" emptiness check `Page.tsx`'s own `shouldAutoFocusTitle` already applies to the *same* `title` value (`focusEditorOnOpen(title) = title !== ''`, `PageHost.tsx`) — decides: a non-empty title means the user is opening an already-named note to keep working in its body, so the editor is ready to type into immediately; an empty title (a brand-new, just-created note) leaves the title as the first editing target, unchanged from today.

**How `PageHost` knows a page was "explicitly opened"** — investigated before writing anything, per instruction: it doesn't need to, because it doesn't need to distinguish. `<MarkdownEditor key={activePageId}>` already forces a full remount for exactly one reason — a distinct `activePageId` — so "this component mounted" and "this page was opened" are already the same event; no new plumbing (an explicit `onNoteClick` signal, a "just navigated" ref, etc.) was needed to tell them apart.

**What "restorable... with real, established prior engagement" means, precisely, and why "a cache entry exists" alone is not enough — a real bug this investigation caught, not a hypothetical.** `editorHistoryCache.ts`'s `setCachedEditorSession` runs unconditionally on *every* unmount — including a page opened and closed without the user ever touching it, and (dev-only) React StrictMode's own mount→unmount→remount cycle. Confirmed directly: a brand-new, empty-title draft's *second* StrictMode mount found a cache entry written by its own *first* mount's cleanup moments earlier — a component-lifecycle artifact, not anything the user did — and the naive "any matching cache entry → focus" rule very nearly stole focus from the title on every single brand-new note, defeating priority (2) above entirely. Fixed by adding `createEditorView.ts`'s `hasEstablishedEditingPosition(view)`: true when the restored selection's head is anywhere other than the two positions a truly untouched session can have (`0`, or `doc.length` — `EditorState.create`'s own default "cursor at end" placement), *or* `undoDepth(view.state) > 0`. A session only counts as restorable when `docTextMatches` passes (the existing staleness gate) **and** `hasEstablishedEditingPosition` is true — an untouched page, or a StrictMode double-mount, correctly falls through to `focusOnOpen` instead.

**Regression coverage** (`createEditorView.test.ts`, tests K–Q, replacing the old K–O): a non-empty-title page with no session focuses via `focusOnOpen` (K); `focusOnOpen=false` never autofocuses (L); no session and no `focusOnOpen` never autofocuses (M); a genuinely established session (real selection movement or an edit) always focuses and restores its selection, *even overriding* `focusOnOpen=false` — an established position always wins (N); focus decisions are isolated per page, unaffected by another page's state (O); a stale (doc-mismatched) entry never grants focus on its own — `focusOnOpen` decides instead (P); **a cache entry with no real engagement — default caret, no edits — never forces focus on its own (Q)**, the direct regression test for the StrictMode/untouched-page bug above. `openPage`'s test helper was updated to mirror `MarkdownEditor.tsx`'s exact gate, including the `hasEstablishedEditingPosition` check. All 33 tests in the "Per-document" suite pass.

**A second, real bug this pass caught along the way, unrelated to the focus logic itself but exposed by it:** `MarkdownEditor.test.tsx` reuses the same `pageId="test-page"` (often with the same markdown text, e.g. `"Hello"`) across many independent `it()` blocks, with no cache-clearing between them. Once *any* cache entry could unconditionally trigger `view.focus()`, this cross-test cache pollution became an observable failure (`syncs an external markdown prop change into the DOM while unfocused` started failing, because an earlier test's leftover session auto-focused its editor). Fixed with a `beforeEach(() => __clearAllCachedEditorHistoryForTests())` at the top of that file, mirroring the pattern `createEditorView.test.ts` already used.

**Verified live in the real Clutter webapp**, each independently:
- *Existing note, non-empty title, no session (A/K):* opened today's daily note fresh, typed immediately with no click — text landed directly in the body.
- *Brand-new note, empty title (B/L):* clicked "New" in the sidebar — `document.activeElement` was the title's `editable-text` element, not the editor; typed immediately — text landed in the title, not the body. (This is the exact case the StrictMode bug above broke before the `hasEstablishedEditingPosition` fix; confirmed fixed by re-running this same check.)
- *Cached, established session (C/N):* typed real content into a note, switched away and back — typed immediately with no click, text landed correctly appended to the prior content, proving both the caret position and focus were restored together.
- *Switching between already-open notes, no leakage (E):* an established note (A) and an untouched-but-non-empty-title note (B) — A→B→A — each behaved independently and consistently across the round trip, matching K/N/O's jsdom coverage.
- *Click note → type immediately, no editor click first (F):* the decisive end-to-end check, folded into A/K and C/N above — passed in both the "no session" and "established session" cases.

**Remaining focus edge cases, stated explicitly:**
- The `head === 0` / `head === doc.length` heuristic in `hasEstablishedEditingPosition` has one known false-negative: a user who moves the caret (no edit) to exactly the start or end of the document, then leaves, won't have that movement alone count as "established" on return (an edit, or a mid-document caret position, always does). Accepted as a narrow, low-stakes trade-off rather than tracking a separate explicit "was genuinely engaged" flag, which would reintroduce the same kind of extra state this redesign specifically removed (`wasFocused`).
- `PageTitle`'s own Enter-to-advance-into-body behavior (`Page.tsx`'s `onSubmit` → `bodyFocusRef.current.focus()`) was observed, during this verification, to not reliably move focus into the body in one specific manual sequence (typed a title, pressed Enter, kept typing — text kept appending to the title instead of moving to the body). Not investigated further and not fixed here: this component/wiring was not touched by this change, `Page.tsx`'s `onSubmit` prop and `EditableText.tsx`'s key handling are exactly as they were before this session, and the task was scoped to note-open focus policy, not title-to-body Enter handling. Flagged here as a real, observed, separate issue worth a dedicated look.
- Archived/restored pages: a cached session's establishedness is evaluated the same way regardless of archive state — consistent with the existing "archived pages keep their editing session" decision (no new interaction with archive/restore introduced here).

---

## Image (`![alt](url)`) — standalone extension with explicit, non-selection-derived source-reveal state (2026-09-01)

**Locked, scoped to `Image` only.** Standard Markdown images (`![alt](url)`) are rendered by `image/imageLivePreview.ts`, a second standalone extension outside `inlineLivePreviewRegion.ts` — the same move already made for WikiLink (see "Standalone-renderer extraction," above), for a related but distinct reason. WikiLink still resolves visibility from selection (`isTokenEngaged`), just with a narrower reveal shape than the shared contract. **Image does not consult `isTokenEngaged`, or selection, at all** — `isTokenEngaged`/`tokenEngagement.ts` is imported nowhere in `image/`. This is the point worth recording explicitly, since it is a real, if narrow, exception to this document's own Locked principle that "engagement is derived from selection... there is no engagement flag anywhere" (top of this file, "Foundational principles"). That principle governs the *shared* reveal-on-engagement mechanism; a construct that opts fully out of that mechanism (as both WikiLink and now Image do) is not bound by it, but Image is the first construct to replace it with a genuinely stored flag rather than another selection-derived rule, so it is captured here rather than left to be re-discovered by a future reader diffing this file against the code.

**The mechanism**: `image/imageUiState.ts` — a `StateField<RangeSet<...>>` of per-Image-node `{revealed, fullWidth}` values, keyed by each node's own `from` position (remapped across document edits via `RangeSet.map`, the same pattern CM6's own fold state uses internally), updated only via a `StateEffect` dispatched from `ImageWidget`'s own edit/width buttons. Consequences, all deliberate:
- The rendered image never disappears because the caret entered its range, clicked it, or selected across it — only its own explicit control toggles `revealed`.
- When `revealed` is true, the raw Markdown is real, ordinary, directly-editable document text (no special-cased editing code — the same "derive nothing, override nothing" property selection-derived reveal gives other constructs, just reached by a different mechanism), and the rendered image is *additionally* shown via a second decoration inserted after it — both coexist, not a toggle between two exclusive states.
- This has no effect on any other construct: Tag/Date/the delimited-marker family/Link/Autolink/bare-URL all remain exactly as governed by the shared, selection-derived contract in `inlineLivePreviewRegion.ts` — nothing about `isTokenEngaged` itself, or any other participant's registration, changed.

**Composition, verified directly (mounted `EditorView`, real DOM) rather than assumed correct by analogy with WikiLink**, per this document's own "Standalone-renderer extraction" checklist:
- Nesting inside `**`/`*`/`~~`/a Link (`[![alt](url)](url2)`, the linked-image pattern) all compose correctly — `Prec.high` (same reasoning as WikiLink's own) is sufficient; confirmed via `view.dom.innerHTML` that the image widget nests *inside* the enclosing construct's own mark in every case, never split or ejected.
- **A real, now-fixed bug**: a *revealed* Image's own nested `URL` child was incorrectly styled `tok-link` by the shared traversal's `urlRenderer` — that renderer's parent-name exclusion guard only listed `Link`/`Autolink`, not `Image`, which is silently treated as a transparent node by `inlineLivePreviewRegion.ts` (it is not a registered participant) and so is still descended into, reaching its `URL` child same as any Link's. This was latently true even before Image's standalone extraction (a non-engaged `widgetReplaceRenderer` participant never returned `false` from the traversal either), but invisible until Image gained a state where its own raw text — and thus its nested `URL` child — could ever actually render as real DOM. Fixed by adding `'Image'` to `urlRenderer`'s guard (`inlineLivePreviewParticipants.ts`); no other file touched.
- **Not a bug**: if an *enclosing* construct is independently engaged by the caret (e.g. cursor between `**` and the image), its own markers reveal as raw text as usual, while the nested Image widget keeps rendering regardless — confirmed this does not break editing (typing at the marker boundary) or the image's own Edit button, both verified working in this exact state. This is the direct, correct consequence of Image having no ancestor-engagement signal to react to at all, not a conflict.

See `image/imageLivePreview.test.ts`'s "Image composition / nesting" and "Image accessibility" describe blocks for the regression coverage.

**Partial supersession (2026-09-02): "Image does not consult `isTokenEngaged`, or selection, at all" no longer holds in full.** The 2026-09-02 UX baseline (an explicit, dated product spec) requires a revealed source to auto-hide once the caret leaves the image's own line — items 3–4 of that spec: "keep the Markdown source visible while the cursor is inside that image's source... when the user moves/clicks to another line or outside that image's source, hide." This is a real, narrower selection dependency, not a full reversal:
- **Entering still never auto-reveals** — the sentence directly above this one ("the rendered image never disappears because the caret entered its range...") is unchanged and still exactly true. Only the widget's own edit/source button turns `revealed` on.
- **Leaving now does auto-hide** — implemented in `imageUiState.ts`'s `imageUiStateField.update`, as a plain line-number comparison against the transaction's resulting selection (an Image node is guaranteed to never cross a line — the same guard `imageLivePreview.ts`/`wikiLinkLivePreview.ts` already share — so "outside the image's source" reduces to "not on the image's own line," no syntax-tree lookup needed). This is a pure `StateField` reducer, not a dispatch-from-`ViewPlugin.update()` pattern — no reentrancy/loop risk.
- The `revealed`/`displayMode` shape also gained a third field, `menuOpen` (2026-09-02 UX baseline item 5 — keeping the whole controls area visible while the options menu is open, since that menu is a portal-rendered `Overlay` outside the container's own DOM subtree, so `:hover`/`:focus-within` alone stop applying once focus moves into it). Same mechanism, same file, no new abstraction.

Recorded per `docs/implementation-rules.md`'s divergence-handling process (§4, "Handling divergence from the specification") — this is a deliberate, product-driven change to this document's own prior decision, not a silent drift discovered later.

**Further supersession (2026-09-02): "leaving auto-hides" is no longer a line-number comparison or a syntax-tree re-resolution — it's a purely positional `RangeSet` span check.** A user-reported inconsistency ("editing an image's URL character-by-character auto-closes the source mid-edit, unlike Link") traced to the mechanism the previous entry above describes: by the time this was refined to fix "completing source editing and continuing after the image" (typing right after a completed `![alt](url)` on the same line), the "still inside" check had become a *re-parse* of the current syntax tree (`findEnclosingImageNode(tr.state, from)`) rather than a line-number comparison, specifically to get an accurate, up-to-date `to` boundary. That re-parse is what broke: a transient edit that momentarily makes the raw text fail to parse as a complete `Image` node (deleting the closing paren, an interior edit that briefly unbalances `](`, etc.) made the re-resolution return `null`, which read as "the caret left" and hid the source — mid-edit, with the caret never having moved. Link never has this failure mode, because Link's own engagement (`inlineLivePreviewRegion.ts`'s `isTokenEngaged`) only ever gates *concealment styling* on top of raw text that stays real regardless of parse validity; Image's `revealed` flag gates whether a *widget replaces the text outright*, so a parse hiccup is destructive there in a way it never is for Link.

The fix (`imageUiState.ts`): each `imageUiStateField` entry is now a real `[pos, to)` `RangeSet` span, not a zero-width point, and `ImageUiValue` sets `endSide = -1` — confirmed via a scratch `RangeSet.map` test before relying on it — so `RangeSet.map` alone (no re-parsing) grows the span for any edit strictly inside it, while still excluding an insertion exactly at its own `to` (preserving "continuing after the image" hides immediately). The "still inside" check is now primarily this mapped span against the caret, with `findEnclosingImageNode` kept only as a secondary, OR'd fallback for one narrow remaining ambiguity: retyping a construct's own last character (e.g. immediately restoring a just-deleted closing paren) lands the insertion exactly at the span's own (just-shrunk) boundary, indistinguishable by position alone from "continuing past a still-valid image" — the OR'd re-parse check resolves it the instant the text becomes a complete `Image` node again, without making re-parsing the primary signal (a transient parse *failure* only ever fails that second check, it never overrides the positional check's own `true`). `broken` (load state) is untouched by any of this — confirmed independent per the user's own explicit "these are two different concepts" instruction. See `image/imageLivePreview.test.ts`'s "Edit source: Link/Image lifecycle consistency" describe block for the regression coverage, including the retyped-last-character edge case and a same-file Link regression confirming its own behavior is unchanged.

---

## Note embeds: nested read-only `EditorView`, no top-level Read Mode (2026-09-08)

**Locked.** `![[Page]]`/`![[Page#Heading]]` renders as an embedded, permanently non-editable snippet *inside* the normal, always-editable CM6 editor — never a second, top-level Read/Edit mode for the current note itself. There is no `PageHost`-local view-mode state, no toggle between `MarkdownEditor` and any read-only top-level renderer, and no product-level "viewing" mode for a normal note — `PageHost.tsx`/`MarkdownEditor.tsx` render exactly as they did at Milestone 5. An earlier pass this same day built a top-level Read Mode (`MarkdownReadRenderer` as a second, whole-page renderer, `PageHost`-local mode state, a mode-toggle button) before this was corrected — that work was fully reverted, not merely hidden, and is not described here because it does not exist in the codebase.

**Rendering mechanism: a real, nested CM6 `EditorView`, not a hand-built parallel DOM renderer.** Confirmed by direct investigation: CM6's own decoration/widget system (headings, emphasis, image sizing/alignment, PDF embeds, task checkboxes) is fundamentally tied to a live `EditorView`/`view.state`/`view.visibleRanges` — there is no headless way to produce "just the decorations" for a snippet of Markdown. `NoteEmbedWidget.ts` (a CM6 `WidgetType`, constructed by `embedLivePreview.ts` — the same `![[...]]` extension that already resolves image/PDF targets, now trying a page target third, after both decline) mounts a second `EditorView` via `createEditorView({ readOnly: true, extensions: buildEditorExtensions({ readOnly: true, ... }) })` into its own `toDOM()`, and destroys it in `destroy()`.

**Shared rendering, not a duplicated one.** `buildEditorExtensions.ts` is the one factory both the top-level `MarkdownEditor` and a note embed's nested view build their extension list from — headings, emphasis, image sizing/alignment, PDF embeds, task checkboxes, WikiLink/Tag/Date rendering are identical either way, by construction, not by two implementations kept in sync by hand. `readOnly: true` only removes extensions that have no meaning once mutation is already blocked (editing keymaps, autocomplete, ordered-list renumbering, the checkbox-toggle mouse handler) — every rendering decoration and every non-mutating click handler (WikiLink/Tag/Date/Link navigation, image click-to-view, PDF Expand) is included either way, which is what gives an embedded note the same visual behavior as the same content open directly.

**Permanently read-only, enforced twice, independently — never toggled.** `createEditorView.ts`'s `readOnly` option sets both `EditorState.readOnly`/`EditorView.editable.of(false)` (the DOM layer — `contenteditable="false"`, which `MarkdownEditor.css`'s note-embed rules key their own mutation-control-hiding off) and installs `blockReadOnlyEdits`, a `transactionFilter` that unconditionally drops any doc-changing transaction — the actual enforcement, not merely a convention. No `Compartment`: a note embed's nested view is read-only for its entire lifetime and is never reconfigured in place — a changed source note produces a fresh `NoteEmbedWidget` (via `WidgetType.eq()` returning `false`), and therefore a fresh nested view, never a toggle on an existing one. This is deliberately the *smallest* form of the mechanism — an earlier pass's `Compartment`/`setReadOnly()` (for a since-reverted, toggleable top-level Read Mode) was removed as dead complexity this milestone never needed.

**Editing an embedded note is only ever possible by opening its real source note.** `NoteEmbedWidget`'s own header renders the source note's title as a button; clicking it calls the injected `onOpenPage(pageId)` (`PageHost.tsx`, the same one-line `pageOperations.open(id)` pattern `resolveWikiLink.ts`'s own `activate()` already establishes) — never an edit-in-place affordance, under any circumstance. This is a permanent product rule, not a current limitation.

**Cycle/depth protection: `noteEmbedAncestry.ts`, checked by the caller before ever constructing a widget.** `checkNoteEmbedAncestry(ancestry, pageId, maxDepth)` returns a cycle/depth-limit rejection or an extended `NoteEmbedAncestry` to pass to the next level down. `embedLivePreview.ts` calls it right before constructing a `NoteEmbedWidget` for a resolved page target — never inside the widget itself — so a direct self-embed (`![[ThisPage]]` inside `ThisPage`) is caught on first encounter (the top-level `MarkdownEditor` seeds its own page id into the starting ancestry) and an indirect cycle (`A` embeds `B` embeds `A`) is caught the same way, one level deeper, when a further-nested `NoteEmbedWidget` extends and re-checks the same ancestry it was itself given. A rejected resolution (cycle or depth-limit) simply never constructs the widget, falling through to the same generic "unable to load" rendering an unresolved image/PDF target already gets — never a new, second broken-state UI.

---

## Fenced-code line numbers and active-line highlighting — custom decorations, not native CM6 gutter/active-line (2026-09-11)

**Locked.** Line numbers and current-line highlighting for fenced code blocks are implemented entirely as Clutter-authored `Decoration.line`/CSS, never via CM6's native `lineNumbers()`, `gutter()`/`gutters()`, `highlightActiveLine()`, or `highlightActiveLineGutter()`. This was a deliberate choice, made only after directly verifying (against the installed `@codemirror/view@6.43.9` source, not memory) that none of those native extensions fit: `lineNumbers()` numbers from `doc.lineAt().number` with no per-block reset; `gutter()`/`gutters()` mount exactly one `<div class="cm-gutters">` per editor, a sibling of `.cm-content` spanning the full document height (confirmed by `gutterView`'s `view.scrollDOM.insertBefore(this.dom, view.contentDOM)`) — live proof already existed in this codebase, since `foldGutter()` (wired in `createEditorView.ts` at the time this entry was written; removed 2026-09-11 in favor of a custom inline toggle — see this file's own "Custom fold-toggle architecture" entry) fought this exact one-column constraint, per its own CSS comment about a sub-pixel rounding bug; `highlightActiveLine()`/`highlightActiveLineGutter()` compute their decoration from `view.lineBlockAt(selection.head)` globally and unconditionally, with no per-block scoping hook — which is also why `highlightActiveLine()`, though wired in `createEditorView.ts`, has always had its visual effect fully suppressed (`editorTheme.ts`: `.cm-activeLine { backgroundColor: 'transparent' }`) rather than actually being used.

**Line numbers: a native CSS `counter()`, not a per-line widget decoration.** Considered directly against a widget-based alternative (constructing one small DOM node per visible code line purely to hold its ordinal) and rejected: `counter-reset: cm-code-block-line` on `.cm-code-block` (the `EditorView.blockWrappers`-created wrapper, `fencedCodeBlockWrapper.ts`) plus `counter-increment`/a `::before { content: counter(...) }` pair on each content line (`MarkdownEditor.css`) gives per-block-reset-to-1, blank-line-inclusive, one-per-logical-line numbering natively, with zero JS computation or DOM nodes per line. The one thing CSS counters can't derive on their own — how wide the gutter column needs to be so a 1-digit and a 3-digit block both right-align cleanly — is supplied by `fencedCodeBlockWrapper.ts`, which now builds one `BlockWrapper` per `FencedCode` node (rather than reusing a single shared instance, as before) carrying a per-block `--code-gutter-digits` inline custom property, computed from that block's own content-line count. Confirmed against the installed source that this is safe: `BlockWrapper.attributes` is a plain per-instance object applied via `dom.setAttribute`, and `BlockWrapper.eq()` already compares attributes for equality, so a digit-count change between renders is correctly detected as a real DOM update.

**Numbering deliberately excludes the block's own fence-marker lines.** `--first`/`--last` (the physical lines containing the `FencedCode` node's own opening/closing ``` marks — the language-label row and the otherwise-empty closing-fence row) are real lines this feature's line-ownership already tracked, but not lines a user would count as "line 1" of their snippet — numbering only fires on `:not(--first):not(--last)`, and `fencedCodeBlockWrapper.ts`'s digit-width calculation is based on content-line count, not total physical line count, to match. The gutter's own `padding-inline-start` reservation still applies to every line unconditionally, so the card's left edge stays flush across the label row, the numbered body, and the closing row. One accepted edge case: a fence's opening and closing marks collapsing onto the same single physical line (a transient, incomplete-parse state — `--first`/`--last` both true) shows no number at all, rather than inventing a special case for it.

**Active line: `state.selection.main.head` only, not every line touched by a range selection — a deliberate product decision, not a default.** `fencedCodeBlockLineDecoration.ts` adds `cm-code-block-line--active` to the one line containing the caret, but only when the caret's own nearest `FencedCode` ancestor (via the same `nearestFencedCode`/`resolveInner` lookup this file already used for line ownership) is the *same node* — compared by `[from, to)` range, not object identity, since separate `resolveInner` calls aren't guaranteed to return the same `SyntaxNode` object for the same underlying node. This was chosen over highlighting every line a multi-line selection touches (`highlightActiveLine()`'s own native behavior) because it's a current-*line* indicator, not a second selection highlight — the selection layer already renders the selected range on its own. The `ViewPlugin` recomputes on `selectionSet` in addition to `docChanged`/`viewportChanged`, the same trigger `fencedCodeLanguageLabelDecoration.ts` already uses for its own per-caret recompute.

**No separate gutter-background token — the active-line highlight is one continuous paint across both the number and code areas, by construction.** The gutter is not a separate DOM element or decoration; it is reserved space (`padding-inline-start`) inside the same `.cm-code-block-line` box the code content sits in. Adding `--active` therefore paints the *entire* line box in one rule, extending the highlight from the line number through the code content automatically, with no coordination needed between two separate elements. Confirmed live in both themes.

**Three new `syntax-tokens.css` tokens, every one an alias onto an existing `theme.css` primitive, none a new arbitrary color:** `--syntax-gutter-number` (→ `--foreground-tertiary`, same weight as the existing language label), `--syntax-active-line-number` (→ `--foreground-primary`, brighter — the active line's own number is emphasized relative to its dimmed neighbors, matching conventional editor behavior), `--syntax-active-line` (→ `--state-selected`, the app's existing generic "this row is highlighted" overlay convention, reused rather than inventing a new opacity value). Grouped in `syntax-tokens.css`, not `theme.css`, since gutter numbers and active-line background are code-editing-surface concepts, consistent with that file's own stated scope.

**A real bug this pass caught and fixed, not merely a design note**: the first implementation of the active-line background rule (`.cm-editor .cm-code-block-line.cm-code-block-line--active`) and the pre-existing light-theme background override (`[data-theme='light'] .cm-editor .cm-code-block-line`) resolve to the exact same specificity (three simple selectors each) — which one wins is decided by source order alone, and the override happened to come later in the file, so the active-line highlight was silently invisible in light mode only (confirmed via `getComputedStyle` returning the plain card background, not `--syntax-active-line`, before the fix; correct after adding an explicit `[data-theme='light']`-scoped copy of the active-line rule, which outranks the override regardless of file order). Caught by live verification in both themes, not by the type checker or the test suite — jsdom cannot compute CSS specificity/cascade outcomes, so this class of bug is only visible in a real browser.

**Verified live in the real Clutter webapp**, both themes: multi-line block with a blank interior line (numbers 1–6, blank line 3 numbered, fence lines unnumbered); caret moving between lines within one block (active line follows); caret leaving the block entirely (highlight disappears, confirmed via a plain-text line typed after the block); two back-to-back blocks (only the block actually containing the caret is ever highlighted — implied by the existing per-node ownership check, not separately re-verified live this pass). `tsc --noEmit` clean; full `src/features/markdown` suite (2425 tests) and the fenced-code-specific suites re-run after the light-mode fix, all passing.

---

## Code-surface tokens — `syntax-tokens.css` consolidation, not a one-off for line numbers (2026-09-12)

**Locked.** Following the line-numbers/active-line work above, an explicit audit was requested: were the three new tokens it introduced (`--syntax-gutter-number`, `--syntax-active-line`, `--syntax-active-line-number`) a one-off, or was fenced code more broadly bypassing the syntax token layer for other color concepts? The audit (read through `syntax-tokens.css`, the full fenced-code CSS, `fencedCodeHighlighting.ts`, and every `var(--syntax-*)` consumer) found two more direct-to-app-token color concepts: the fenced code block's own card background/border (`--surface-secondary`/`--surface-tertiary` via `color-mix`, `--border-default`) and its language label color (`--foreground-tertiary`) — plus a second, independent instance of the same underlying gap: inline code (`.tok-code`, single backtick) reaching directly into `--surface-subtle`/`--foreground-secondary` for its own background/foreground.

**Five new tokens added, consolidating with the existing three into one eight-token code-surface vocabulary**, all in `syntax-tokens.css`'s theme-reactive blocks, none a new arbitrary color — every one aliases the exact primitive already in use, so this is a pure rename/relocation, not a restyle:
- `--syntax-code-background` (→ `--surface-secondary` dark / `--surface-tertiary` light — the fenced-code line background)
- `--syntax-code-border` (→ `--border-default` — the card's left/right and `--first`/`--last` top/bottom borders)
- `--syntax-code-label` (→ `--foreground-tertiary` — the language label; kept as its own token rather than reusing `--syntax-gutter-number`, even though both currently resolve to the same primitive, since they're different UI elements — a once-per-block label vs. a per-line number — that could reasonably diverge in a future theme)
- `--syntax-inline-code-background` (→ `--surface-subtle`)
- `--syntax-inline-code-foreground` (→ `--foreground-secondary`)

**A real simplification fell out of migrating the background token, not just a rename.** `--syntax-code-background` resolves differently per theme *inside the token itself* (dark: `--surface-secondary`; light: `--surface-tertiary`, preserving the reasoning from the original line-decoration work — `--surface-secondary` coincides with `--surface-root` in light mode, making the card invisible against the page at any alpha). Consuming it in `.cm-code-block-line`'s single background rule made the separate `[data-theme='light'] .cm-editor .cm-code-block-line` override (previously required to supply the light-mode-only value) entirely redundant — removed outright, not merely left in place. That removal, in turn, resolved the active-line light-mode specificity tie from this document's own previous entry at its root cause: with the override gone, `.cm-code-block-line.cm-code-block-line--active` (three selector components) unconditionally outranks the plain `.cm-code-block-line` background rule (two components) in both themes, so the explicit `[data-theme='light']`-scoped duplicate of the active-line rule (added as the fix in the previous entry) is now also redundant and was removed. Net effect: fewer rules than before this pass, not more, despite adding five tokens.

**What deliberately stayed on direct app/theme tokens** — the audit's other half, to keep this a clean semantic boundary rather than "alias everything that touches code":
- Blockquote's marker bar (`--foreground-primary`) and table's cell border (`--border-strong`) — generic Markdown-structural widgets, not code/language concepts, correctly on `theme.css`'s own vocabulary.
- Copy button's `--copied` success tint (`--foreground-primary`) and Format button's `--error` state (`var(--error)`) — generic UI feedback/status tokens shared app-wide, not syntax color, even though they render inside a fenced-code block's own controls.
- `.cm-media-control` chrome (icon sizing/hover), and every non-color primitive on `.tok-code` (`--radius-sm`, `--shadow-inset-default`, `--font-mono`) — shape/effect/typography, out of scope for a *color* token audit by definition.

**`syntax-tokens.css`'s own top-of-file doc comment updated** to describe two kinds of token it now holds — syntax-highlighting colors (language content, VS Code Dark+/Light+-sourced) and code-surface tokens (the chrome around that content, aliasing `theme.css` primitives) — since the file's previous self-description ("matches exactly the color-bearing tag groups `defaultHighlightStyle` defines") predated and no longer covered the gutter/active-line/background/border/label/inline-code tokens.

**Re-audited after implementation**: grepped every `.cm-code-block*`/`.tok-code` rule in `MarkdownEditor.css` for a remaining direct `var(--surface-*|--foreground-*|--border-*|--state-*|--accent*)` reference — none found. The only tokens those rules still reference directly are non-color (`--space-*`, `--radius-*`, `--text-*`) or the shared UI-feedback tokens explicitly kept direct above.

**Verified live in the real Clutter webapp, both themes**: fenced-code background/border/label and inline-code background/foreground all pixel-identical to before the migration (confirmed via `getComputedStyle` returning the same computed values as the pre-migration screenshots — `rgba(255, 255, 255, 0.035)`/`rgb(168, 168, 168)` for inline code in dark mode, `rgba(0, 0, 0, 0.07)` for the active-line background in light mode, matching `--state-selected` exactly); active-line highlight re-confirmed working in both themes after the specificity-tie rule removal. `tsc --noEmit` clean; full `src/features/markdown` suite (2425 tests) passing.

---

## Fenced code block max-height/internal scroll — attempted, found architecturally incompatible with CM6, reverted (2026-09-12)

**Locked finding; the feature itself is NOT implemented.** A `max-height: 800px; overflow-y: auto; overflow-x: hidden;` cap was added to `.cm-code-block` (the `EditorView.blockWrappers` wrapper), committed, then reverted the same day after live testing surfaced a real cursor-placement bug: clicking a visually-scrolled line (e.g. what looked like line 98 after scrolling) placed the caret on a completely different, wrong document position (e.g. line 22) — the line naturally sitting at that same *unscrolled* pixel offset. Initial live verification (auto-scroll-to-caret while typing, Copy button, rounded corners) had all looked correct; the click-mapping bug only surfaced once click-to-position was specifically tested at a scrolled offset, which is why this is recorded as a caught-then-reverted mistake rather than silently fixed.

**Root cause, confirmed by reading the installed source (`@codemirror/view@6.43.9`), not assumed: independently-scrolling editable content inside a single `EditorView` is fundamentally incompatible with CM6's coordinate model.** `posAtCoords` — the function behind every click, drag, and selection placement — computes:

```js
let content = view.contentDOM.getBoundingClientRect(), docTop = content.top + view.viewState.paddingTop;
let yOffset = y - docTop;
block = view.elementAtHeight(yOffset);
```

This is a single, linear pixel-to-document-position mapping: pixels below `.cm-content`'s own top translate directly into a cumulative-height lookup (`heightMap`, built from each line's own natural, unclipped height) to find a document position. There is no concept anywhere in this model of a second, independent scroll offset nested inside `.cm-content`. Once `.cm-code-block` gets its own `scrollTop` (from `overflow-y: auto`), a line's *visual* position is shifted by that inner scroll, but `posAtCoords` never accounts for it — a click resolves against the *unscrolled* cumulative heights, landing on whichever line naturally sits at that pixel offset in the original, unscrolled layout. This is not a CSS mistake fixable by adjusting a click handler; the mismatch is structural, between two scroll positions CM6's model only ever treats as one.

**What would actually provide an independent, correctly-mapped scroll region**: a genuinely separate `EditorView`, the same pattern this codebase already uses for read-only note-embeds (see that entry above) — a second view has its own `.cm-scroller`/coordinate space, so its own internal scrolling is self-consistent. But making such a nested view *editable* and bidirectionally synced with the parent document (shared undo history, cursor/selection continuity across the boundary, IME, copy-paste) is a substantially different, much larger project than capping a code block's height — not something to build as a side effect of this feature. If height-capping a fenced code block is revisited, this is the one path that would give a true persistent inner scrollbar; every other approach that keeps fenced code as live, in-place-editable text sharing the single main `EditorView` cannot have an independently-scrolled sub-region without reproducing this exact bug, regardless of which element the `overflow` is applied to.

**Considered but explicitly deferred, not implemented**: collapsing a long block to a fixed height with `overflow: hidden` (no scrollbar, so no independent scroll offset ever exists) and an explicit expand action that removes the cap entirely before any further interaction — this would be CM6-safe (no scroll offset means no coordinate mismatch), but changes the interaction from "an internally scrollable block" to "a collapsed/expanded toggle," a different UX to be evaluated on its own merits, not adopted here as a substitute.

**Reverted**: `.cm-code-block`'s `max-height`/`overflow-y`/`overflow-x` rules removed entirely; fenced code blocks are back to normal, unbounded document flow (confirmed live: a long block renders at its full natural height, subsequent content flows immediately after it with no gap, clicking/dragging to select any line places the caret/selection exactly where clicked, matching the visual position with no offset). A code comment on `.cm-code-block` in `MarkdownEditor.css` points back to this entry so the same approach isn't silently retried without first reading the root cause above.

**Kept, not reverted**: the gutter's wide `padding-inline-start` reservation is still scoped away from `.cm-code-block-line--first`/`--last` (those two lines never carry a line number — they're the fence-marker lines, excluded from `counter-increment`/the gutter `::before`) — a real, independent correctness fix unrelated to the scrolling attempt, so the language-label row and the closing-fence row correctly stay flush left at plain `--space-12` regardless of whether height-capping ever returns.

**Verified live in the real Clutter webapp after the revert**: an 11-line block (10 content lines) renders at full height with no clipping; clicking directly on a specific visual line (line 7) places the caret on that exact line, confirmed via the active-line class; a drag from line 1 to line 10 selects exactly that range, confirmed via the selection highlight matching the drag exactly; borders, rounded corners, and Copy/Format/Actions hover-reveal all behave normally in both themes. `tsc --noEmit` clean; full `src/features/markdown` suite (2425 tests) passing.

---

## Caret visually jumps to Copy/Format/Actions buttons — fixed via `WidgetType.coordsAt()` (2026-09-12)

**Locked.** Reported bug: typing the third backtick of an opening fence (or placing the caret at the end of the language-info text, e.g. right after `js` in `` ```js ``) visually snapped the caret to the far right of the card, near the Copy/Actions buttons — even though typing continued to insert text in the right place, and the jump self-corrected the instant more content was typed or the caret moved elsewhere.

**Root cause, confirmed via direct instrumentation before any fix was written — the selection was never wrong.** Got a handle on the live `EditorView` (`.cm-content`'s own internal `cmTile.view`, confirmed present in the installed `@codemirror/view@6.43.9`) and compared `state.selection.main.head` against `view.coordsAtPos(head, side)` at the exact repro position:
- `state.selection.main.head` — always correct, exactly matching the document position right after the typed text.
- `coordsAtPos(head, -1)` (side = before) — correct, right after the visible text.
- `coordsAtPos(head, 1)` (side = after) and `coordsAtPos(head)` (CM6's own default side) — **both wrong**, matching the Copy button's own screen position exactly.

**Why**: `fencedCodeCopyButtonDecoration.ts`/`fencedCodeFormatButtonDecoration.ts`/`fencedCodeActionsButtonDecoration.ts` all insert their button via `Decoration.widget({ widget, side: 1 }).range(pos)` at the exact same document position — `codeInfo.to` (right after the language info) or `openMark.to` (right after the opening fence, before a language is typed). Reading the installed CM6 source directly (`WidgetTile.coordsInWidget`, `@codemirror/view/dist/index.js`): when nothing overrides it, CM6's default cursor-coordinate resolution at a widget boundary measures the widget's own `dom.getClientRects()` — normally correct, since a widget usually renders where it's anchored. These three don't: `MarkdownEditor.css` gives them `position: absolute`, pulling them out of flow to the card's top-right corner. CM6 does insert a `cm-widgetBuffer` marker at this exact boundary specifically to give the caret an accurate in-flow anchor — confirmed via direct DOM inspection that it sits at the correct position (`left: 390`) while the Copy button's own rect sits at `left: 685` — but CM6's default widget-boundary resolution uses the *widget's* rect, not the buffer's, once a widget owns that side of the boundary.

**The fix**: `WidgetType.coordsAt(dom, pos, side)` — confirmed present in the installed source (`@codemirror/view/dist/index.js:169`) as the exact, sanctioned override point for a widget to report its own cursor coordinates instead of falling back to `dom.getClientRects()`. Added one shared helper, `fencedCode/fencedCodeControlWidgetCoords.ts`'s `coordsAtFencedCodeControlWidget(dom)`, used identically by all three widget classes (`FencedCodeCopyButtonWidget`, `FencedCodeFormatButtonWidget`, `FencedCodeActionsButtonWidget`) via a one-line `override coordsAt(dom) { return coordsAtFencedCodeControlWidget(dom); }` — a shared function, not a shared base class or a larger abstraction, since the only thing duplicated across the three was this exact traversal, nothing else about the widgets is shared. The helper walks `dom.previousSibling` backward past any number of `.cm-media-control` siblings (Format/Copy/Actions can all be anchored at the same position simultaneously, rendered left-to-right as adjacent DOM siblings) until it finds the one real, non-widget node before the cluster — usually the `cm-widgetBuffer` marker, occasionally the fence-marker or language-info text directly — and returns that node's trailing rect (a `Range`-based fallback handles a bare text node with no wrapping element). Returns `null` (CM6's own "use the default" signal) if no such node is found, degrading to the pre-existing behavior rather than throwing.

**Deliberately not done, per explicit instruction**: no change to `state.selection`, no forced/rewritten cursor position, no change to the widgets' `side: 1`, and no change to the buttons' `position: absolute` visual layout — the fix is purely "tell CM6 the correct coordinates for this widget," nothing about the editing model or the buttons' own appearance.

**Testing**: real CSS layout (`getBoundingClientRect()`/`position: absolute`) doesn't exist in jsdom, so the actual bug — a caret rendering at the wrong *screen pixel* — isn't reproducible or provable in a unit test; that was verified live, in the real webapp, via the same `coordsAtPos`/`.cm-cursor` instrumentation used to diagnose it. `fencedCodeControlWidgetCoords.test.ts` (jsdom) instead covers the one part of this that *is* meaningfully jsdom-testable: the sibling-walking traversal logic itself — a single widget, three stacked widgets (Format/Copy/Actions all asked individually, all correctly resolving to the same preceding node), a bare-text-node fallback, and the no-preceding-node degrade-to-`null` case.

**Verified live in the real Clutter webapp, both themes**, using the same live-`EditorView` instrumentation throughout (`.cm-content`'s `cmTile.view`, confirmed accessible in the installed CM6 build):
- Opening fence, typed one backtick at a time: caret stays correct after each of the first two backticks (no widgets exist yet — nothing to fix), and after the third (`coordsAtPos` now `374`, matching `.cm-cursor`'s own rendered rect, not `685`).
- Info-string boundary: typed `js` after the fence, caret confirmed at `390`, matching the buffer's own position, both immediately after typing and when explicitly re-clicked back to that exact position later.
- Buttons: Format/Copy/Actions all render at their unchanged absolute-positioned coordinates (`657`/`685`/`713`), Copy's click handler still fires without moving the caret or the document (`mousedown`/`click` `preventDefault`/`stopPropagation` unaffected), Actions still opens `FencedCodeActionsMenu`.
- Delete-back (`` ``` `` → `` `` `` → `` ` ``): caret correct at each step (an unrelated automation-tool quirk surfaced here — `BackSpace` as a key name did nothing at all, `Backspace` worked; not a product bug, noted for future live-testing in this codebase).
- Multiple blocks: a second, independent `` ```py `` block's own language-info boundary clicked directly — caret resolved to the correct position (`28`, matching `x-py` boundary) in both themes, confirming the fix isn't scoped to only the first block on the page.
- `tsc --noEmit` clean; full `src/features/markdown` suite (2425 tests, including the 4 new ones) passing.

---

## Fenced-code opening-fence auto-close (2026-09-12)

**Locked, deliberately minimal.** Typing the third backtick of a bare opening fence (` ``` ` alone on its line) auto-inserts a matching closing fence on the next line, as one transaction — one Undo removes it. Cursor stays right after the typed backtick (not moved onto any new line), so a language identifier (`css`, `js`, ...) can be typed immediately next. No blank code-content line is inserted; `` ```\n``` `` is already a complete, valid, empty fenced block — reaching the code body from there is ordinary editing (the user presses Enter), not something this feature pre-populates or navigates into.

**Not built on `closeBrackets()`**: investigated first — it does support triple-character bracket tokens (`@codemirror/lang-python` uses this for `'''`/`"""`), but tracing its algorithm shows the produced shape is always same-line (`` ```│``` ``), never newline-separated. No configuration produces this feature's shape.

**Mechanism**: `EditorView.inputHandler` (the same facet `closeBrackets()` itself is built on), guarded to a single-character, empty-selection, non-composing backtick insert only. Detection is syntax-tree-driven: a throwaway candidate state (just the plain backtick inserted, never dispatched) is checked for a new `FencedCode` node whose own `.from` is on the cursor's line — this is what tells apart a brand-new opening fence (triggers) from an existing block's closing fence (doesn't; its node's `.from` is an earlier line) and from inline/mid-line backticks (no `FencedCode` node forms at all, since CommonMark requires a fence to start its own line). One text check on the node's own content narrows scope to exactly three backticks with nothing else on the line (no info string yet, no fourth+ backtick) — deliberately not generalized to longer fences. Registered in `buildEditorExtensions.ts` alongside the other editable-only, document-mutating extensions (`markdownEnterKeymap`, `markdownIndentKeymap`, `orderedListStructuralNormalization`).

**Simplified once, substantially, after an initial pass over-built it.** A first implementation also replicated the opening line's own container prefix (leading whitespace, or whatever precedes the fence node) onto the closing line, with a further special case swapping a list marker for matching-width spaces so the closing fence wouldn't start a second list item. Told directly that this was disproportionate to the actual requirement before it had been verified as *necessary* — cut back to the plain case (`insert: '`\n```'`
, no prefix logic at all) and confirmed via test that the plain top-level case, indentation-only case, and every "must not trigger" case all still hold with nothing beyond the core syntax-tree check.

**Container contexts (blockquote, list) — investigated, confirmed broken by the simplified version, not silently accepted.** Two tests assert the real, current gap rather than assuming: typing the third backtick after `` > `` `` `` produces `` > ```\n``` `` — the closing line has no `>` prefix, so `syntaxTree` confirms it no longer resolves inside the `Blockquote` node at all. Typing after `` - `` `` `` produces `` - ```\n``` `` — same result, confirmed via the tree that the closing line falls outside the `ListItem`. **Not fixed here** — flagged as a known, real limitation for a follow-up decision (re-add prefix handling, or accept it as an edge case), rather than pre-built into this pass.

**Verified live in the real Clutter webapp, dark theme**: typed `` ` `` `` ` `` `` ` `` one at a time → `` ```\n``` ``, cursor after the third backtick, confirmed via `state.selection.main.head`/`doc.toString()`; typed `css` immediately after → `` ```css\n``` ``, cursor after `css`; pressed Enter → ordinary new content line 1 opens above the closing fence, active-line highlight and gutter number appear normally, closing fence stays below; typed body content on that line successfully. `tsc --noEmit` clean; full `src/features/markdown` suite (2441 tests, including 12 new ones) passing.

---

## Custom fold-toggle architecture — three phases: headings/lists/fenced-code, embedded-note collapse, indentation-based paragraphs (2026-09-11)

**Locked.** Folding was investigated, found already substantially wired (`codeFolding()`/`foldGutter()` present in `createEditorView.ts` since `b2dd1327`, 2026-08-23 — `docs/editor-feature-matrix.md`'s own prior revision missed this entirely, having only ever checked `buildEditorExtensions.ts`), then rebuilt across three independent phases: a custom UI on top of native CM6 fold mechanics (replacing `foldGutter()`), a widget-level collapse mechanism for note embeds (which cannot use CM6 folding at all), and a genuinely new fold-detection algorithm for indentation-based paragraph hierarchy (which CM6/CommonMark has no native concept of). This entry is the single account of all three; `docs/editor-feature-matrix.md`'s "Foldable headings / lists / paragraphs" and "Embed / transclusion" rows point back here for the "why."

### Phase 1 — custom inline fold toggle (headings, lists, fenced code)

**`foldGutter()` removed, `codeFolding()`/`foldKeymap` untouched.** The native `@codemirror/language` gutter UI (a `.cm-gutters` column) is replaced by `codemirror/fold/FoldToggleWidget.ts` + `foldToggleDecoration.ts` — a `ViewPlugin` that, for every visible line, mirrors `foldGutter()`'s own reference algorithm (confirmed against the installed `@codemirror/language@6.12.4` source): an existing fold (`findFold`, reimplemented locally since `@codemirror/language` never exports its private version — only the public `foldedRanges()` RangeSet it reads from) wins over a fresh `foldable()` query, and a non-owning line gets no decoration. The toggle is a real `<button>`, always present in the DOM for every foldable line (never dynamically mounted/unmounted on hover) — `opacity`/`pointer-events` alone drive hover-reveal, matching the exact pattern this file's own fenced-code Copy/Format/Actions buttons already established (`MarkdownEditor.css`'s `.cm-code-block:hover .cm-code-block-copy` rule). A folded line's own toggle stays visible unconditionally (`[data-folded='true']`), since it's the only way back in. `codeFolding()` (fold *state*) and `foldKeymap` (`Ctrl-Shift-[`/`Cmd-Alt-[` etc.) are completely unmodified — this is a UI-layer replacement only, confirmed by the fact that keyboard folding continued working throughout without any change to either.

**Which constructs use which native mechanism, unmodified by Clutter:**
- **Headings** — `@codemirror/lang-markdown`'s own `headerIndent` `foldService` (baked unconditionally into `markdown()`'s returned `LanguageSupport`), implementing exactly "fold to the next heading of the same or higher level, or EOF." Zero Clutter-authored detection.
- **List items** — the base parser's generic `foldNodeProp` (any `Block`-group node except `Document`/heading/list container itself is foldable) already excludes `OrderedList`/`BulletList` but *not* `ListItem`, so each item with descendant content beneath its own marker line is natively foldable for free.
- **Fenced code** — the same generic `foldNodeProp` branch, `{from: <opening line>.to, to: FencedCode.to}` — already includes the closing fence in the hidden range, exactly matching the required "opening fence stays visible, everything below including the closer is hidden" shape, natively, with no adjustment needed.

**Scope narrowing (`isInScopeFoldOwner`, `foldToggleDecoration.ts`) exists because the generic `foldNodeProp` branch above is *too* generic.** It also treats `Blockquote`, `Table`, and — the actual bug found live during Phase 1 — any multi-line `Paragraph` as foldable. Two ordinary lines with no blank line between them merge into one `Paragraph` node (see Phase 3 below for why), and the native branch happily offered a "fold this ordinary paragraph in half" toggle the moment this widget made that previously-invisible native fold clickable for the first time. `isInScopeFoldOwner` allowlists exactly `ATXHeading1-6`/`SetextHeading1-2`/`ListItem`/`FencedCode` on the *offer-to-fold* path; an *already*-folded range (however it got folded — `foldAll`, a restored serialized fold state) always keeps a working toggle regardless of construct, so this narrowing only limits what the UI offers to *create*, never strands an existing fold.

**Permanent `.cm-line` constraint respected by construction, not by convention alone**: the toggle is inserted via `Decoration.widget({..., side: -1}).range(line.from)` — CM6 places it as the first child *inside* the real `.cm-line` it belongs to; this codebase's own code never calls `classList`/sets a style on `.cm-line` itself anywhere in this feature. Same relationship every other inline widget here already has to its own line.

**`readOnly` behavior**: `codeFolding()`/`indentedParagraphFoldService()`/`foldToggleDecoration()` are all omitted together, unconditionally, for a `readOnly` `EditorView` (`createEditorView.ts`'s one `readOnly` consumer — a note embed's own nested view). Omitted, not hidden: there is no fold *state* to expand either, so a note embed's passage always reads as one continuous, fully-expanded piece of content, with no keyboard fold command reaching it — matching the same reasoning already established for why a note embed shouldn't be independently foldable at all (see Phase 2).

### Phase 2 — embedded-note collapse (independent of CM6 folding entirely)

**Cannot be CM6 folding, by construction, not by choice.** A note embed (`![[Page]]`) is one `Decoration.replace({widget}).range(node.from, node.to)` over the `Embed` node's own span in the *outer* document — confirmed directly in `embedLivePreview.ts`. Everything visible (wavy boundary dividers, header, the nested content) is DOM synthesized by `NoteEmbedWidget.toDOM()`, including a second, fully independent nested `EditorView` built from the *source note's* markdown, not any range of the current document. There is no multi-line outer-document range to fold — `foldService`/`foldEffect`/`unfoldEffect`/`findFold()` have nothing to act on here, and none of them are used anywhere in this mechanism.

**Mechanism**: `ImageUiState` (`imageUiState.ts`) — the same per-position `RangeSet`-backed UI state already shared by `revealed`/`broken`/`pendingFirstLeave` for every Image/PDF/note-embed occurrence — gains a `collapsed: boolean` field (default `false`). A new header chevron button toggles `content.hidden` (the nested `EditorView`'s own container) directly via DOM mutation, dispatching `setImageUiState` purely to persist the fact for whenever this widget's `toDOM()` genuinely runs again for some *other* reason. **`NoteEmbedWidget.eq()` deliberately excludes `collapsed`** from its rebuild-trigger comparison — unlike `revealed` (which switches between two structurally different decoration shapes and does need a rebuild), collapsing only ever hides an already-rendered DOM subtree, so excluding it is what keeps the nested `EditorView` alive, never destroyed/recreated, across every collapse/expand click. Labeled "Collapse note"/"Expand note," not the bare "Collapse"/"Expand" a first draft used verbatim — the existing Expand control (navigate to the real source note) already owns the unqualified "Expand" label, and both are reachable in the same header at once once collapsed.

**Visual contract**: top divider, header (icon/title), and bottom divider are unconditional — only `.cm-note-embed__content` toggles. Never marked `--mutating` (unlike Edit source/More actions): collapsing changes no document text, so it stays reachable at every nesting depth, the same way Expand does, even inside another note embed's own read-only nested view.

**`readOnly` behavior**: a note embed's nested view has no fold toggle and no fold state (Phase 1's `readOnly` gate), but it *can* itself contain another, independently-collapsible note embed — collapse state is per-widget-instance, unrelated to and untouched by the `readOnly` gate that governs CM6 folding specifically.

### Phase 3 — indentation-based paragraph folding

**Cannot be a syntax-tree-boundary computation.** Confirmed directly against the installed `@lezer/markdown@1.7.2` parser: CommonMark's own "a continuation line that doesn't start a new block continues the current paragraph regardless of its own indentation" rule means `Parent paragraph\n    Child paragraph` (no blank line between them) parses as **one** `Paragraph` node spanning both physical lines — there is no tree boundary between "parent" and "child" to fold at. `indentedParagraphFoldService.ts` scans physical lines and their own leading-whitespace width directly instead, using `resolveLineIndentContext` (`markdownIndentContext.ts`) only to confirm a line is genuine paragraph content — never hijacking a list/heading/code/blockquote line, all of which that function already excludes before ever returning `kind: 'paragraph'`. Only the paragraph's own *first* physical line can own a fold (a continuation line resolves to the identical `kind: 'paragraph'` as its owner, so an explicit "is this the enclosing `Paragraph` node's own first line" check disambiguates the two — the same shape `resolveLineIndentContext`'s own `kind: 'list'` restriction to exactly the `ListItem`'s marker line already established).

**Two decisions this feature's own investigation flagged as open, resolved against existing convention rather than invented:**
- **Blank line: a hard stop, never absorbed.** The blank-line absorption Clutter already grants elsewhere (`orderedListStructuralNormalization.ts`'s "a blank line between two same-delimiter list items never splits the list") is backed by a real, continuing CommonMark container — an `OrderedList`/`BulletList` node genuinely spans across that blank line in the parse tree. No analogous container exists for bare indented paragraphs; nothing in the document marks a blank line as "still part of this outline branch," so absorbing it would be a Clutter invention with zero textual signal, not a reuse of a principled existing rule.
- **Indentation threshold: "strictly greater than the parent's own," a plain character count** — matching `markdownIndentContext.ts`'s own stated model for the identical leading-whitespace quantity (`computeIndentChange`'s doc comment: "never `countColumn`/`tabSize`-aware column math"), not a stricter multiple-of-`INDENT_STEP_SPACES` rule invented separately for folding.

**Why generic native paragraph folding is intentionally bypassed, not merely narrowed.** `foldToggleDecoration.ts`'s `isInScopeFoldOwner` allowlist (Phase 1) *never* gains `'Paragraph'`. Doing so would re-admit the exact Phase 1 bug: `foldable()` tries every registered `foldService` first, and only falls back to the generic native `foldNodeProp` branch if none matched — since `indentedParagraphFoldService` correctly returns `null` for a non-qualifying paragraph, that fallback would still fire and re-offer "fold this ordinary paragraph in half." Instead, `computeOwnedRange` (`foldToggleDecoration.ts`) special-cases `kind: 'paragraph'` lines to call `computeIndentedParagraphFold` *directly*, completely bypassing `foldable()`'s own native fallback for paragraphs specifically; headings/lists/fenced-code are unchanged from Phase 1 (still `isInScopeFoldOwner` + native `foldable()`). `indentedParagraphFoldService()` is still registered as a real `foldService` (for `codeFolding()`'s own state/`foldKeymap` keyboard commands to work on a genuinely-qualifying paragraph) — safe, since it only ever returns non-null for the same lines the toggle already agrees are legitimate owners.

**Known, accepted limitation, not silently fixed**: the native generic `foldNodeProp` branch for `Paragraph` was never disabled at the grammar level (`markdownGrammarExtensions.ts` was deliberately left untouched) — only this toggle UI's own offer-to-fold path routes around it. Pressing `Ctrl-Alt-[` (`foldAll`) with the cursor on an ordinary, non-qualifying multi-line paragraph could in principle still trigger the native fallback via that separate keyboard path, exactly as it always could before this feature existed. Out of this feature's stated scope (which was specifically about the toggle's own correctness); flagged here rather than silently expanded into a shared-grammar change.

**Verified live in the real Clutter webapp** for all three phases: heading/list/fenced-code fold-and-unfold round-trips (including nested-heading absorption through to EOF, and a fenced code block's bottom border/radius composing correctly once collapsed — a real bug found and fixed the same session, see `fencedCodeBlockLineDecoration.ts`'s `isFencedCodeBodyFolded`); note-embed collapse with the nested `.cm-content` DOM node's identity confirmed unchanged across the toggle (proving no destroy/recreate) and independent collapsed state across multiple embeds, including nested ones; every paragraph-folding case from the original bug report (two ordinary paragraphs, consecutive non-indented lines, blank-line-separated pairs in both directions) confirmed to produce zero toggles, and a genuine parent/child pair confirmed to fold to exactly the expected range, composed correctly alongside headings/lists/fenced-code in one document. `tsc --noEmit` clean and the full `src/features/markdown` suite passing after each phase (2513, 2522, and 2544 tests respectively).

### Phase 1's "List items" bullet — superseded (2026-09-15): lazy continuation, then a single fold-semantics authority

**`listItemFoldService.ts` (superseding Phase 1's "List items" bullet above).** Phase 1 stated the generic native `foldNodeProp` branch was sufficient for list items ("each item with descendant content beneath its own marker line is natively foldable for free"). That's only true when the item's own children are the *only* thing lazy continuation can attach to a `ListItem` node's span — in practice, CommonMark's lazy-continuation rule (a non-interrupting line silently extends the *current* block's node span regardless of its own indentation) lets an unrelated, unindented sibling paragraph get absorbed into the preceding list/task item's own `ListItem` span, so the native fold range included content that was never structurally the item's child. Concretely: `- [ ] Parent\nSibling paragraph` (no indentation on the second line) parsed `Sibling paragraph` as part of `Parent`'s own `ListItem` node, and the generic native fold offered to hide it. `listItemFoldService.ts` replaces the native `foldNodeProp` answer for list items with an indentation-based physical-line scan (clamped by the native node's own span as an upper bound, so it can never *widen* what the parser already considers part of the item — only narrow it to genuine indented descendants), establishing the invariant this session's later bug reports also relied on: **a block is foldable only when it has actual structural descendants**, never merely "whatever lazy continuation silently attached." Registered as a real `foldService` (`codeFolding()`'s own state/keyboard commands still work on it) and, from the refactor below onward, is also the function `foldSemantics.ts`'s `getFoldRange` calls directly for `kind: 'list'` lines — never through `foldable()`'s own generic fallback, for the same "a declining service still lets the native fallback fire" reason Phase 3 already established for paragraphs.

### Single fold-semantics authority + Option-Arrow line-move fix (2026-09-15)

**Bug**: `Alt-ArrowUp`/`Alt-ArrowDown` (`Option-Arrow` on macOS) — bound in `@codemirror/commands`' `defaultKeymap` to `moveLineUp`/`moveLineDown` — corrupted a fold on the physically-adjacent line instead of relocating it. Root cause, confirmed by dispatching the real transaction rather than theorized: `moveLine()` swaps the current line with the next/previous *physical* line by deleting one line's raw text and re-inserting an identical copy elsewhere — two independent `ChangeSpec` entries, not a position-preserving move. `RangeSet.map` (`foldState`'s own remapping mechanism, itself completely unmodified and correct) can only preserve a range through content that survives *unmoved*; content that is deleted and independently reinserted elsewhere — even when byte-identical — has no traceable position identity for `RangeSet.map` to follow. Confirmed via a live dispatch: a fold `{from: 6, to: 23}` on `Parent\n    hidden child\nCurrent line` became a meaningless one-character residual `{from: 6, to: 7}` after `Alt-Up`, with `hidden child` reappearing fully visible, unfolded, in its new position.

**Fix — `foldAwareMoveLineKeymap.ts`.** A fold-preserving replacement for `moveLineUp`/`moveLineDown`, bound ahead of `defaultKeymap` (same precedence pattern `foldAwareArrowKeymap.ts` already established for ArrowLeft/ArrowRight skipping folds). When the physically-adjacent line is fold-hidden, or is itself a fold's owner line whose fold reaches further, the command treats the *entire* folded span — owner line through the fold's own `to` — as the unit to swap, constructs the same delete/reinsert `ChangeSpec` shape `moveLine` itself uses but against that widened unit, and recomputes the fold's new position by **plain arithmetic against the transaction this command itself just built** (never `ChangeSet.mapPos` — mapping a position through a transaction that deletes and independently reinserts the very content being tracked has the identical "no traceable identity" problem the bug itself has). Declines (falls through to native `moveLineUp`/`moveLineDown`, completely unmodified) for every case that isn't a genuine fold adjacency: multiple selection ranges, no fold anywhere near the move, or the document boundary — so the overwhelming majority of line moves, which never touch a fold, are unaffected. Verified live in the real Clutter webapp (not only jsdom): folded task item, adjacent-line move in both directions, and moving the folded block itself in both directions, all relocate correctly with the fold intact; a folded fenced code block behaves identically and unfolds afterward with its content uncorrupted.

**Known, accepted limitation, not fixed by this pass**: the embed card's own *outer* collapse (`ImageUiState.collapsed`, see "Two independent fold mechanisms" below) is a separate `RangeSet`-backed `StateField`, keyed the same position-tracking way, and is vulnerable to the identical root cause — `foldAwareMoveLineKeymap.ts` only consults `foldSemantics.ts`'s `findFold` (CM6 `foldState`), so a line moving past a *collapsed* embed card currently falls through to native `moveLine` and loses the embed's collapsed state (confirmed live: the embed card comes back expanded after the adjacent line relocates correctly). Flagged here rather than silently expanded into this pass's scope — fixing it means teaching `foldAwareMoveLineKeymap.ts` a second, parallel check against `imageUiStateField`, re-applying its range the same way `ownFold`/`neighborFold` are re-applied, without merging the two mechanisms together.

**`foldSemantics.ts` — the single authority `getFoldRange`/`isFoldable`/`findFold`, replacing three previously-scattered call sites.** Before this pass, "what is foldable and what is its range" was computed by `foldToggleDecoration.ts`'s own `computeOwnedRange` (the toggle UI), and separately, natively, by `@codemirror/language`'s `foldCode`/`unfoldCode`/`foldAll` (Clutter's then-still-native `foldKeymap`) — the latter calling `foldable()` directly, which tries every registered `foldService` and then falls back to the generic native `foldNodeProp` answer the instant every service declines. Since `listItemFoldService`/`indentedParagraphFoldService` correctly `null`-decline for a non-qualifying line (a `foldService`'s `null` means "ask someone else," never "definitively nothing here"), that native fallback could still fire for the very constructs Phase 1/3 had already special-cased — confirmed directly: dispatching native `foldable()` for `- [ ] Parent\nSibling paragraph` still returned a real range covering `Sibling paragraph`, even with `listItemFoldService()` registered, reachable only through the keyboard shortcuts, never through the toggle (which never called `foldable()` generically to begin with). `foldSemantics.ts` consolidates the toggle's own range-computation logic (moved verbatim, unchanged behavior) into one exported set of functions; `foldToggleDecoration.ts` and a new `foldSemanticsKeymap.ts` (replacing native `foldKeymap` entirely, not sitting alongside it) both consume it exclusively, closing that loophole for every entry point. Headings and fenced code are unchanged — `getFoldRange` still delegates to native `foldable()` for them, since native semantics were already correct there; only list items and indented paragraphs are special-cased, exactly as before, just from one place instead of two.

**Test-construction pitfall found and fixed while verifying `foldSemanticsKeymap.ts` under jsdom, worth recording since it is not obvious and not specific to this codebase.** A synthetic `KeyboardEvent('keydown', { key: '[', shiftKey: true, ctrlKey: true })` does **not** faithfully reproduce a real Ctrl+Shift+[ keypress: `@codemirror/view`'s own key-resolution (`runHandlers`) deliberately omits the `Shift-` prefix on its first lookup for any single-character key, trusting that a real browser's `.key` already reflects the shifted character (Shift+[ reports `.key: '{'` on a US layout, never `'['`). A synthetic event that instead sends the bare, unshifted character with `shiftKey: true` bolted on is looked up as plain `Ctrl-[` in that first pass — which collides with `@codemirror/commands`' own `defaultKeymap` entry `Mod-[` (`indentLess`, which returns `true` unconditionally whenever the document is editable, even as a no-op with nothing to dedent) — so the event is fully consumed before CM6 ever reaches the `Ctrl-Shift-[` lookup `foldSemanticsKeymap` is registered under. `foldSemanticsKeymap.test.ts`'s own `pressFoldShortcut` helper now dispatches the realistic shifted character (`{`/`}`, with `keyCode: 219`/`221` so CM6's `keyCode`-based fallback branch also resolves correctly) — matching what every real browser actually sends, and exercising the genuine binding path instead of an artifact of how the synthetic event was built.

### Two independent fold mechanisms — restated, unchanged by this pass

Document-content folding (`codeFolding()`'s `foldState`, everything above in this entry) and a note embed's own *outer* collapse (`image/imageUiState.ts`'s `ImageUiState.collapsed` field, a separate `StateField<RangeSet<...>>` keyed by the embed's own `[from, to)` position in the *host* document — see this file's "embedded-note collapse" Phase 2 entry above) remain two deliberately separate mechanisms, not merged into one abstraction by this pass. `FoldStateStore` (`core/application/editor/FoldStateStore.ts`, see ADR-033) persists both, but as two independent maps/top-level keys (`foldState`/`embedCollapse`) — this pass touched neither `FoldStateStore` itself nor its persisted shape.
