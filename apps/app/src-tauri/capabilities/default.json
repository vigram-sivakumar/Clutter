{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "enables the default permissions",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "fs:allow-home-read-recursive",
    {
      "identifier": "fs:scope",
      "allow": ["$HOME/**/.folder.md"]
    }
  ]
}
